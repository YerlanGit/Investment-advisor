# REPORT_SECTIONS_AUDIT.md — посекционный аудит живых отчётов BASE и DEEP

> **Источник:** прод-отчёты `r/148046720/2026-06-12/base.html` (138 КБ) и `deep.html` (226 КБ),
> сгенерированные **после** коммитов Sprint 5 / 5.1 (`1dae3bd`), портфель U148046720, **с маржой**.
> **Метод:** скачаны по подписанным ссылкам, распарсены посекционно, каждое значение сверено
> с builder'ом и движком (маппинг — `REPORT_SECTIONS.md`).
> **Результат:** найдено **8 реальных дефектов**, все исправлены в Sprint 5.2 (коммит этой ревизии).
> Легенда: ✅ корректно · 🔧 дефект найден и исправлен · 📝 наблюдение (не дефект).

---

## Сводка по секциям

| # | Секция | BASE | DEEP | Вердикт |
|---|---|---|---|---|
| 1 | Обложка: вердикт + инсайты | 🔧 | 🔧 | H1 был захардкожен; `ai_bullets` не рендерились нигде |
| 2 | Риск-гейдж + мандат-бейдж | 🔧 | 🔧 | Бейдж «Умеренный» противоречил панели «Умеренно-агрессивный» |
| 3 | KPI-strip (CVaR/Sharpe/MaxDD) | ✅ | ✅ | Числа консистентны ($-оценки сходятся с NAV) |
| 4 | Панель «Соответствие мандату» | ✅ | ✅ | Работает вживую; маржа −16% видна; нарушение Stocks_US 80%>60% поймано |
| 5 | Секторный пай + бейдж плеча | ✅ | ✅ | 131% gross / 1.16x с подписями баз (5.1) — вживую |
| 6 | Margin-Call предупреждение ИИ | 🔧 | ✅ | Haiku писал «прибыль удваивается» при 1.16x — матнеправда |
| 7 | Рыночный режим (квадрант+R3) | n/a | ✅ | Точка живая (+0.11/+0.07 = Expansion ✓), R3 «согласовано» |
| 8 | 4-Pillar Scoring | ✅ | ✅ | Рендер корректен; live-SEC-когорт активен с 5.1 |
| 9 | AI Ideas / «Идеи ИИ» | 🔧🔧 | ✅ | BASE: пики = фолбэк-каталог без пометки + сырые ключи в заголовках |
| 10 | Action Plan | n/a | ✅ | Присутствует; mandate-scale уровней активен с 5.1 |
| 11 | Ожидаемый эффект | n/a | 🔧 | «+0.0 пп» горел зелёным; `it_share` нейтрален ✓; вердикт «компромисс» честен ✓ |
| 12 | Бенчмарки | 🔧 | 🔧 | Карточка называлась безлико «Профильный бенчмарк» |
| 13 | Стресс-тесты | n/a | ✅ | Секция присутствует, дисклеймер капа на месте |
| 14 | Прозрачность (CoVe/integrity) | ✅ | 🔧 | CoVe: «Anthropic Claude claude-sonnet-4-6» (сырой id) |

---

## 1. Обложка: вердикт + инсайты — 🔧 ИСПРАВЛЕНО (главная находка)

**Живой факт (оба тира):** H1 «Вердикт по портфелю» — **захардкоженный прототип**
«Портфель в зоне умеренного риска — держать можно, но почти весь риск собран в двух позициях»,
одинаковый для любого портфеля. `data.ai_verdict` в DEEP не рендерился **нигде**;
`data.ai_bullets` (4–7 оплаченных инсайтов Sonnet/Haiku) не рендерились **ни в одном тире**.

**Фикс 5.2:** H1 привязан к `ai_verdict` (статик — только no-AI fallback, без выдуманных фактов);
под ai-box добавлен компактный список `ai_bullets` (top-4 BASE / top-5 DEEP).
**Файлы:** `report_basic_v3.html`, `report_deep_v3.html`. **Тест:** `CoverVerdictBindingTest`.

## 2. Риск-гейдж + мандат-бейдж — 🔧 ИСПРАВЛЕНО

**Живой факт:** бейдж «Индекс рассчитан для вашего профиля: **Умеренный**», а панель мандата
на той же странице: «Соответствие мандату · **Умеренно-агрессивный**». Причина: бейдж брал
3-бакетную калибровку (`normalize_risk_mandate("Умеренно-агрессивный") → MODERATE`), панель — реальный профиль.

**Фикс 5.2:** при наличии `user_profile` бейдж показывает **реальное имя профиля**; бакетная
калибровка гейджа не тронута (это внутренняя шкала). **Файл:** `pdf_payload.py`. **Тест:** `MandateBadgeConsistencyTest`.

## 3. KPI-strip — ✅

CVaR −3.2% · ≈$365 · CI −3.9%…−2.6% (bootstrap), Sharpe 0.65 / Sortino 0.91, MaxDD −24.2% ≈ $2 722.
Перекрёстная сверка: $-оценки соответствуют NAV; CI асимметричен (признак реального bootstrap, не ±k·σ).
Идентичны между тирами (один движок) — паритет ✓.

## 4. Панель «Соответствие мандату» — ✅ (работает вживую)

Профиль «Умеренно-агрессивный»: Stocks_US **80%** (лимит 30–60) → нарушение ✓; Облигации 25% (10–30) ✓;
Сырьё 11% (0–15) ✓; Global ETFs 0% (10–40) → недовес ✓; «Маржинальный долг −16% — вне лимитов классов» ✓.
Сумма классов 116% NAV консистентна с гроссом 131% и маржой −16% (кэш не искажает классы — фикс 5.0/5.1 работает).

## 5. Секторный пай + бейдж плеча — ✅

«Gross Exposure: 131% (лонг+|маржа| к капиталу) · Плечо: 1.16x (лонг ÷ капитал)» — подписи баз (L1) вживую,
пирог нормализован к лонгу. Числа взаимно согласованы: 1.16 ≈ 1.31 − 0.16 + 0.01 (округления).

## 6. Margin-Call предупреждение ИИ — 🔧 ИСПРАВЛЕНО (BASE)

**Живой факт:** DEEP (Sonnet) — корректно («плечо 1.16x… умножает и прибыль, и убыток»). BASE (Haiku):
«**прибыль удваивается**» при плече 1.16x — математически неверно.

**Фикс 5.2:** `leverage_rule` теперь диктует формулировку: «умножаются на коэффициент ≈{ratio}x,
НЕ пиши „удваивается", если плечо не ≈2x». **Файл:** `ai_narrative.py`.

## 7. Рыночный режим (DEEP) — ✅ (фиксы 5/5.1 подтверждены прод-данными)

Ярлык **Expansion**, уверенность 65%, точка квадранта **(198.6, 86.0)** = Growth +0.11 / Cycle +0.07 —
верхний правый квадрант, **совпадает с ярлыком** (R1-фикс работает; захардкоженной (177,110) больше нет).
R3-плашка: «Макро-сигналы согласованы» — детерминированная сверка FRED↔моментум вживую.

## 8. 4-Pillar Scoring — ✅

Таблица рендерится в обоих тирах; N/A-классы с прочерками; hotspot-флаги из движка.
Live-SEC-когорт (5.1) активен в проде (статик-фолбэк логируется на сервере — в отчёте не видно, и не должно).

## 9. AI Ideas / «Идеи ИИ» — 🔧🔧 ИСПРАВЛЕНО (BASE), ✅ DEEP

**DEEP (Sonnet) — образцовый результат data-driven правила 5.1:** MSCI, ANET, META, SPGI, VRSK, CTAS —
свежие имена с привязкой к портфелю: «недовесен в сетевой инфраструктуре AI (0% веса)»,
«отсутствует в портфеле при перевесе Tech», «P/E ниже NVDA». Зацикленность ушла.

**BASE (Haiku) — два дефекта:**
1. Пики = **в точности фолбэк-каталог июня** (JNJ/PLTR/MTUM/KO — ротация month=6): Haiku потратил пики
   на уже-имеющиеся тикеры, held-фильтр их вычистил, бэкфилл подставил каталог — **без пометки**,
   маскируя каталог под анализ ИИ.
2. Заголовки карточек — **сырые ключи** `boost_alpha`/`rebalance`/… (Haiku не вернул label,
   нормализатор подставил ключ).

**Фиксы 5.2:** (a) `held_rule` — промпт перечисляет тикеры портфеля и запрещает их в пиках
(устранение причины осушения); (b) бэкфилл помечается «· резервный каталог» (честность);
(c) label==ключ → каноническое русское имя категории (в `_build_ai_ideas` и в бэкфилле).
**Тесты:** `HeldRuleInPromptTest`, `IdeaTitleRawKeyLeakTest`.

## 10. Action Plan (DEEP) — ✅

Секция присутствует, уровни от ATR/SMA; mandate-scale (×0.75/1.0/1.25) активен с 5.1
(для этого юзера MODERATE → числа равны историческим).

## 11. Ожидаемый эффект (DEEP) — 🔧 точечно

✅ `it_share` **нейтрален** вживую (−14.7 пп без окраски) — A2 работает.
✅ Вердикт «Компромисс: снижение волатильности за счёт роста концентрации» — честный
(vol −3.8 пп improved vs Max TRC +12.2 пп worsened).
🔧 «Ожид. доходность **+0.0 пп**» горела **зелёным**: дельта +0.04 пп округлялась в +0.0 на дисплее,
но флаг оставался pos. **Фикс 5.2:** |delta_pp| < 0.05 → favourable=None (нейтрально) на уровне
флэттенера. **Тест:** `RoundedZeroDeltaNeutralTest`.
📝 Наблюдение (не дефект): BL-таргеты под кап весов 20% всё же подняли Max TRC до +12.2 пп —
кап на ВЕС ≠ кап на ВКЛАД В РИСК; TRC-aware ограничение — кандидат в следующую фазу.

## 12. Бенчмарки — 🔧 ИСПРАВЛЕНО

**Живой факт:** карточка называлась буквально «Профильный бенчмарк» — пользователь не видит,
КАКОЙ это бенчмарк. **Фикс 5.2:** имя дополняется реальным ETF: «Профильный бенчмарк · Nasdaq 100».
Выбор юзера при этом реально активен (Sprint 5/Task 8 — подтверждено: слот присутствует и первичен).

## 13. Стресс-тесты (DEEP) — ✅

Секция присутствует; AI-комментарий с гарантией упоминания выпуклого капа (валидатор M1).

## 14. Прозрачность: CoVe / integrity — 🔧 точечно

✅ Integrity: «RAG: банк. отчёты» — 3-state статус работает; модель помечена (BASE: Haiku 4.5).
🔧 CoVe-строка: «Anthropic Claude **claude-sonnet-4-6**» — сырой id с двойным «Claude».
**Фикс 5.2:** display-имя через `_model_display_name` → «Anthropic · Claude Sonnet 4.6».
**Тест:** `LineageModelDisplayTest`.

---

## Что осталось (после 5.2) — точный список

| Задача | Статус | Блокер |
|---|---|---|
| Открыть PR `claude/stoic-mccarthy-LM18P → main` | ⏳ ждёт | PAT без `Pull requests: write` (или 1 клик: compare-ссылка) |
| Отозвать засвеченный PAT | ⏳ на стороне владельца | — |
| RAG re-sync из GCS без рестарта | 📋 спроектировано | отдельный коммит за env-флагом (риск ChromaDB) |
| TRC-aware ограничение в BL (см. §11) | 📋 кандидат | дизайн (кап вклада в риск, не веса) |
| Hash-lock зависимостей через CI-сборку | 📋 отложено (F-8) | CI-валидация образа |
| Cloud SQL/Firestore для денежного леджера | 📋 стратегическое (F-5) | инфра-решение |
| Миграция DEEP → Opus (guard готов, 5.1) | 📋 за флагом | решение по бюджету |
| MCP-фаза (`MCP_STRATEGY.md` §5.3/5.5/5.6) | 📋 следующая фаза | по команде владельца |
