# AUDIT_360_2026-09-23 — сквозной аудит: математика против денег, проза против правила, вёрстка против замера
<!-- nav | area:audit | code:src/finance/period_returns.py,src/finance/engine/risk_engine.py,src/finance/engine/portfolio_manager.py,src/finance/simulate.py,src/finance/action_plan.py,src/ai_narrative.py,src/premium_payload.py,src/templates/report_basic_v3.html,src/templates/report_deep_v3.html,design/premium_v2/deep/deep-plan.jsx,design/premium_v2/deep/deep-factors.jsx | read-before:правка свёртки доходности, BL-доходности, уровней Action Plan, правила плеча в прозе, мобильной вёрстки фолбэка -->

> **Статус:** 🟢 свежий (раунд `§−121`, полная запись — `rounds/ROUNDS_122-92.md`).
> **Метод:** каждая находка сверена с ИСТИНОЙ, посчитанной НЕЗАВИСИМО от формулы
> движка: капитал книги по дням, эталонные книги `tests/golden_support.py`
> (`base` · `leveraged_fx` · `manual`), DOM после монтирования React, headless-замер
> на 320/360/390/414. Ссылок на живые отчёты в задании не было — поэтому артефактом
> служили эталонные книги и демо-витрина, а не прод.

---

## 1. Итог в одном абзаце

Предыдущий сквозной вывод «ошибок в числах нет» (`§−102`) не подтвердился: он сверял
формулу с формулой. Сверка с деньгами нашла **четыре дефекта в числах** — все исправлены,
у каждого гейт против истины и мутация. Правило §−13 (плечо — только при кэше < 0)
держала вёрстка, но не проза модели. Два параметра считались и терялись (VaR, зона
входа Buy). Jinja-фолбэк на телефоне был сломан на ВСЕХ ширинах — его просто никто не
мерил. Архитектура двух ботов изолирована по процессу, токену и хранилищу; уровень кода
теперь тоже под гейтом.

## 2. Было / Стало

| # | Где | Было | Стало | Гейт |
|---|---|---|---|---|
| M-1 | свёртка книги: `risk_engine`, `period_returns`, Marginal VaR, `simulate`, `portfolio_series` | `Σ wᵢ·ln(1+Rᵢ)` — средний темп роста бумаг; демо: **9.47%** годовых, MaxDD −15.1% | `ln(1 + Σ wᵢ·Rᵢ)` — рост книги; демо: **13.36%**, MaxDD −13.8% | `test_phase60` · капитал деньгами · мутация 5/5 |
| M-2 | панель «Эффект», BL | `Σw·μ_BL` печаталась как полная доходность, Sharpe = (er − rf)/σ — rf дважды; все 3 эталона: «доходность ниже безрисковой» | `Σ w·(rf + μ) + min(0, w_кэш)·rf`; ложная записка исчезла у всех трёх | `test_phase60` · мутация: 6 падений |
| M-3 | маржа: реализованная панель + форвард | заём под 0%: плечо 1.24× получало ≈0.56 пп даром | заём по rf валюты отчёта; книги без маржи — бит-в-бит | `test_phase60` · мутации 1/1 и 1/1 |
| M-4 | `action_plan.compute_levels` | стоп Buy от РЫНКА: над всей зоной входа при цене ≥ SMA50+ATR; SMA-якоря выше рынка; зона выше рынка | стоп от ВХОДА; якоря только ниже; зона клипается к рынку | 3000 геометрий · монотонность по мандату |
| L-1 | проза модели (§−13) | `ai_leverage_warning` насквозь; промпт отдавал «плечо 1.0x» книге без маржи; баннер — по непустоте | факта нет, запрет в промпте, поле гасит движок, фильтр предложений, баннер по `is_leveraged` | непослушная модель в тесте |
| P-1 | VaR 95% | считался, в payload — и нигде не показан | подпись CVaR-карточки в Premium и Jinja | DOM-тест |
| P-2 | зона входа Buy (Premium) | терялась: колонка занята take-profit | второй строкой под ценой | DOM-тест |
| P-3 | `ai_action_impact` | модель «прогнозировала CVaR/Vol/Sharpe после плана»; нигде не рендерилось | снят с промпта и схемы; ключ пуст | тест схемы |
| U-1 | Jinja-фолбэк на телефоне | 13–15 нарушителей на КАЖДОЙ ширине (и на `HEAD`) | 0 / 0 на 320–414 | `test_phase62` + контрольный опыт |
| U-2 | Premium DEEP 320 px | чип двойника с тикером AIX за краем | 0 | `test_phase62` |
| U-3 | сценарный тир | не измерялся (стенд ждал React) | 0 / 0 | `test_phase62` |
| A-1 | изоляция ботов, уровень кода | `IsolationTest` пуст для подмодулей | полные имена + манифест корзин | мутации |
| D-1 | доки/конфиг | README: «dynamic RFR (FRED)»; `.env.template` включал KZT/14% локально; `ROUNDS_91-76.md` держал §−76…§−120 | исправлено; файл разрезан, имя = диапазон под гейтом | `test_repo_hygiene` |

## 3. Что проверено и ВЕРНО (без правок)

- **Base Currency Approach:** матрица цен переводится в USD ДО доходностей (FX с лагом
  T−1), rf подбирается под валюту отчёта; Sharpe/Sortino/VaR/CVaR — в одной валюте.
- **Euler:** Σ ERC = 100.000000 на всех эталонах; **факторное разложение:**
  систематика + специфика = 100, identity gap 0, σ из разложения = σₚ.
- **4-Pillar:** пороги `action_from_total` и hotspot-override совпадают с `MATH_ENGINE §10`.
- **Режим:** квадранты и confidence совпадают с `MATH_ENGINE §12`.
- **Два бота:** разные процессы и event loop, разные токены (общий — отказ на старте),
  один писатель на файл, CAS, ro-копия у читателя; тяжёлое — в executor/to_thread.
- **Manual Portfolio и Freedom warehouse** разведены по отдельным планам
  (`roadmap/manual_portfolio/`, `roadmap/freedom_warehouse/`) и по коду
  (`finance/manual_portfolio.py` против `freedom-etl/`); смешения не найдено.

## 4. План действий (по цене ошибки)

| # | Задача | Почему | Кто решает | Цена |
|---|---|---|---|---|
| 1 🔴 | **Живой прогон BASE+DEEP после деплоя** и сверка трёх чисел: годовая доходность, «Эффект» Sharpe, стоп Buy | M-1…M-4 меняют числа на ЖИВЫХ отчётах: годовая доходность вырастет на доходность диверсификации (≈1–4 пп), записка «ниже безрисковой» исчезнет | оператор | 30 мин |
| 2 🔴 | **rf из FRED (DTB3) с фолбэком на статику** | ставка — оценка «на 2026-06»; каждый Sharpe в отчёте зависит от неё | владелец (сеть в `__init__` движка) | 0.5 дня |
| 3 🟠 | **Одна форвардная доходность на странице** | обложка — факторный форвард (эталон 14.5%), «Эффект» — BL (5.4%); подписи различаются, но читатель видит два «ожидаемых» числа | владелец: выбрать базу или развести подписи | 0.5 дня |
| 4 🟠 | Удалить мёртвые ключи контракта `ai_action_impact`, `factor_scores` | всегда пусты; удаление — изменение контракта | владелец | 1 ч |
| 5 🟡 | Фазы С-1…С-3 физического разделения (`ARCHITECTURE_FOR_AGENTS.md §8`) | удобство навигации; изоляцию уже держит манифест | владелец, отдельными PR | 1–2 дня |
| 6 🟡 | Гейт «ключ payload имеет потребителя» | 24 ключа из 104 не читает ни шаблон, ни маппер; часть — законные числовые близнецы, часть — мёртвые | агент | 0.5 дня |
| 7 🟡 | Свободный кэш под 0% — записать решение владельца | реалистично для брокерского остатка, но Sharpe книги с большим кэшем ниже, чем «кэш в T-bills» | владелец | — |

**Решения, которые нужны от владельца:** №2 (сеть при старте движка), №3 (какой форвард
показывать), №4 (удаление ключей контракта), №7 (конвенция кэша). Остальное — работа.
