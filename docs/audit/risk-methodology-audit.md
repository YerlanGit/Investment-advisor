# Аудит методологии риска и прибыльности: молодые листинги и leveraged ETP

> **Дата:** 2026-07-13 · **Тип:** верификационный аудит (verify-first, без правок расчётных модулей)
> **Эталонные кейсы:** `SPCX` (IPO 2026-06-12, ~20 торговых наблюдений) и `CONL` (2× COIN, daily reset, ER 1.04%)
> **Ветка:** `claude/audit-risk-methodology-6cfoz3` (задание: `audit/risk-methodology`)
> **Тесты:** `tests/test_phase28_risk_methodology_audit.py` — 10 passed / 8 xfailed (спецификации недостающих возможностей)
> **Связанные документы:** `docs/METHODOLOGY_SPARSE_AND_LEVERAGED.md` (реализация F-20…F-23), `docs/AUDIT_360_2026-07-10.md`

---

## 1. Карта модулей (Discovery)

| Модуль | Что считает | Где вызывается |
|---|---|---|
| `src/finance/investment_logic.py` | Ядро MAC3: Ridge-беты (`:883`), Σ = B·F·Bᵀ + D (EWMA hl=63 ⊕ Ledoit-Wolf 70/30, `:918-934`, `:978`), σ_p, Euler ERC/MCTR (`:997-1004`), historical VaR/CVaR (`:1096-1097`), bootstrap-ДИ CVaR (`:446-505`, вызов `:1104-1107`), MaxDD (`:1109-1119`), Marginal VaR (`:509-548`), Sharpe/Sortino (`:1147-1150`), композитная реализованная серия F-22 (`:1033-1062`), форвард β·μ + LETF-drag F-23 (`:1537-1606`), реестр LETF (`:113-131`) | `UniversalPortfolioManager.analyze_all` → `tg_bot` (все тиры BASE/DEEP) |
| `src/finance/period_returns.py` | Sparse-robust портфельная лог-серия (`:159-225`), TE/IR (`:228-270`), мультипериодные доходности в **торговых** днях (`:19-24`); гейты `MIN_OVERLAP_TDAYS=60` (`:150`), `MIN_DAILY_COVERAGE=0.5` (`:156`) | `investment_logic` (composite basis, benchmark stats), sklearn-free тесты |
| `src/finance/portfolio_series.py` | 12-мес KPI-спарклайны CVaR/Sharpe/MDD из готового композита (`:92-198`), фильтр колонок <60 дн в фолбэке (`:160-161`) | `tg_bot` / рендер отчёта |
| `src/finance/stress.py` | Параметрические факторные шоки: **линейный** β·shock (`:10-21`), convex-cap ±35% (`:85-117`), эвристика est_dd = pnl − σ_quarterly (`:23-29`), дисклеймер (`:88-95`) | `analyze_all` (`investment_logic.py:1760`) |
| `src/finance/simulate.py` | «До/после» по плану: √(w'Σw) на структурной Σ, реплей CVaR/Sharpe/MDD на дневных лог-доходностях с гейтом n≥60 (`:125-127`); Monte-Carlo НЕ реализован (явно, `:30`) | `analyze_all` (`:2061`) |
| `src/finance/scenario_engine.py` | Сценарный тир: pairwise-ковариация `min_periods=252` (`:54`, `:106-127`), σ_p=√(w'Σw)·√252 (`:130-138`), MCTR (`:141-172`), пер-именная σ с гейтом 252 (`:187-191`) | `finance/scenario_report.py` → scenario-тир |
| `src/finance/scoring_orchestrator.py` + `src/finance/scoring.py` | 4-пиллар скоринг; guard «нет отчётности» для commodities/sovereign ETF (`scoring_orchestrator.py:56-91`, `:571`, `:628`); P/E только при NI>0, P/B при equity>0 (`:210-280`); robust z = median+MAD (`scoring.py:66-99`) | `analyze_all` (`:1884`) |
| `src/finance/sec_edgar.py` | Фундаментал ТОЛЬКО из reported 10-K/10-Q XBRL: ROE при equity>0 (`:336-337`), OpM при revenue>0 (`:332-333`), Altman-Z/Piotroski (`:460-530`) | `analyze_all` (`:1507`) |
| `src/finance/action_plan.py` | План сделок; аннотация исключённых имён «вне модели: история < 60 торг. дней» (`:305-306`) | `analyze_all` (`:1994-2005`) |
| `src/finance/asset_taxonomy.py` | Классификация Equity/ETF/Bond/… (`:25-33`) — класса LEVERAGED_ETF **нет** | брокерский адаптер, отчёт |
| `src/pdf_payload.py` | Integrity-панель (покрытие серии, базис Sharpe, CVaR-CI; `:1826-2021`), по-активный фундаментал-слой (`:1137-1153`), passthrough `portfolio_metrics` (`:1213`) | `html_renderer` → отчёт |
| `src/finance/data_lineage.py` | CoVe-таблица: риск-метрики (`:609-616`), SEC-покрытие «N тикеров без SEC покрытия» (`:126-183`) | DEEP-отчёт |
| `src/freedom_portfolio/history.py` | Загрузка цен: параметр `days` — **календарный** (`:82`, `:135`), дефолт 1825 ≈ 1260 торговых | `MAC3RiskEngine.get_market_data` (`investment_logic.py:653-659`) |

**Хранение длины истории:** отдельного поля «T инструмента» нет — T выводится по месту как число non-NaN наблюдений колонки (`investment_logic.py:789`, `period_returns.py:186`) либо длина серии доходностей (`realized_window_days`, `investment_logic.py:1167`; `expected_return_window_days`, `:1629`). Все статистические окна — в торговых днях; календарные дни используются только в параметре запроса данных (задокументировано, `investment_logic.py:634-643`).

**Классификация leveraged:** только реестр имён `_LEVERAGED_ETP_BASES` + env `LEVERAGED_ETP_EXTRA` (`investment_logic.py:113-131`). Множитель L, направление (inverse) и базовый актив не хранятся.

**Источник фундаментала:** исключительно reported SEC XBRL (10-K/10-Q); analyst estimates в системе отсутствуют как источник. Отсутствие метрики = отсутствие ключа/NaN; строки без покрытия выпадают из фундаментал-слоя отчёта (`pdf_payload.py:1140-1141`) и считаются в CoVe (`data_lineage.py:158-160`).

---

## 2. Сводная таблица проверок

Вердикты: **PASS** — реализовано корректно · **PARTIAL** — реализовано частично/с пробелом · **FAIL** — не реализовано при наличии потребности · **N/A** — неприменимо (функциональности нет как класса, и её отсутствие само по себе не искажает числа).

### Блок A — малая выборка (молодые листинги)

| # | Проверка | Вердикт | Код | Последствие для советника |
|---|---|---|---|---|
| A1 | T в торговых днях | **PASS** | `period_returns.py:19-24` (окна 21/63/126/252), `investment_logic.py:1167` (T = строки дневных доходностей); календарные дни — только в запросе данных (`investment_logic.py:653-659`, `history.py:82`) | — |
| A2 | Минимальный порог T | **PARTIAL** | Гейты: 60 торг. дн — sparse-guard структурной модели (`investment_logic.py:786-800`), серия доходностей (`period_returns.py:150`), bootstrap-CI (`:1104`), M-VaR (`:1127`), симулятор (`simulate.py:125-127`); 30 — регрессия (`:837-845`); 252 — форвардная панель (`:1628-1635`) и сценарная ковариация (`scenario_engine.py:54`). Ниже 60 имя исключается с аннотацией; книга целиком <60 → метрики не выводятся (проверено тестом). **Пробел:** точечный historical VaR/CVaR печатается при окне 30–99 наблюдений без флага (`:1096-1097` — условие лишь `len>0`) | Книга, вся история которой 60–99 дней, получает VaR/CVaR с шумом ±30-50% без предупреждения — институциональная норма ~100 наблюдений не гейтится |
| A3 | ДИ волатильности (χ²) | **FAIL** | Не найдено. Единственный ДИ в системе — bootstrap-ДИ CVaR (`investment_logic.py:446-505`) | σ на обложке подаётся точечно; при T=20 истинная σ лежит в ×[0.76, 1.46] от оценки — ложная точность |
| A4 | Post-IPO нестационарность | **PARTIAL** | Флаг есть: исключение <60 дн + причина в Action Plan (`action_plan.py:305-306`) + integrity-строка покрытия (`pdf_payload.py:1851-1858`). Нет: исключения первых N дней price discovery, lock-up-осведомлённости, теста стационарности | SPCX честно «вне модели», но после 60-го дня войдёт в модель сразу всей историей, включая волатильные первые недели price discovery |

### Блок B — Beta

| # | Проверка | Вердикт | Код | Последствие |
|---|---|---|---|---|
| B1 | SE(β) и предупреждение | **FAIL** | Ridge без inference (`investment_logic.py:883-908`); `Residual_Vol_Ann` есть (`:898`), но SE(β)=σ_ε/(σ_m·√T) не строится, warning отсутствует | Беты 60–250-дневных окон подаются в Σ, ERC и Black-Litterman как точные; советник не видит, что β=1.8 на 80 днях статистически неотличима от 1.0 |
| B2 | Blume/Bayesian сжатие | **FAIL** (признанный roadmap) | Не реализовано; `docs/METHODOLOGY_SPARSE_AND_LEVERAGED.md §2.2/§5 п.2` — Vasicek запланирован. Ridge alpha=0.001 сжимает к **0** (не к 1) пренебрежимо (`:880-883`); Ledoit-Wolf сжимает ковариацию ФАКТОРОВ, не беты (`:924-930`) | Экстремальные беты коротких окон не стягиваются к рынку → завышенные хвостовые вклады в стрессах и BL-тилты |
| B3 | Proxy-назначение | **PARTIAL** | Есть для облигаций/AIX-структурок: `INSTRUMENT_PROXY_MAP` (`investment_logic.py:261-272`), `resolve_tickers` (`:598-604`). Для молодых equity — нет (исключение вместо proxy); для LETF нет карты базовых активов | SPCX не получает факторный профиль сопоставимого актива — его вес просто разбавляет риск (консервативно, но недооценивает вклад в риск) |

### Блок C — инструменты с плечом (критический)

| # | Проверка | Вердикт | Код | Последствие |
|---|---|---|---|---|
| C1 | Детекция LETF + множитель L | **PARTIAL** | Детекция: реестр имён `_LEVERAGED_ETP_BASES` (CONL включён) + env (`investment_logic.py:113-131`). L **не извлекается**, inverse не различается, базовый актив не хранится; `asset_taxonomy.AssetClass` не имеет класса LEVERAGED_ETF (`asset_taxonomy.py:25-33`) | Всё, что требует знания L (explicit drag, стресс с пересчётом плеча, prior L·β_базы), невозможно; новый LETF вне реестра вообще не детектируется |
| C2 | Многодневная доходность через фактический компаундинг, не L·σ·√T | **PASS** (реализованные) / **PARTIAL** (стресс) | Реализованные метрики считаются с **фактических дневных цен ETP**: лог-доходности (`investment_logic.py:803`), композит (`:1033-1062`) → компаундинг ресета в данных по построению; наивного `L·(кумулятив базы)` в коде нет нигде (закреплено тестом: −24.6%, не −4%). Стресс — линейный β·shock без path-dependence (`stress.py:10-21`) | Реализованная панель честна. Стрессовая просадка 2×-продукта в длинном сценарии недооценивает эрозию пути (см. C6/H2) |
| C3 | Volatility drag −½L(L−1)σ²T | **PARTIAL** | Drag моделируется **эмпирически**: форвард = β·μ + min(α̂,0) для реестровых ETP (`investment_logic.py:134-146`, `:1574-1586`) — α̂ впитывает реализованный drag; explicit-формула с L и σ_u — roadmap (`METHODOLOGY... §5 п.3`) | Направленно верно (прогноз CONL понижен), но α̂ смешивает drag с шумом удачи и оценивается на собственном коротком окне ETP — величина drag не воспроизводима из контрактных параметров |
| C4 | Look-through | **PASS** (с оговоркой) | Замены CONL→1× COIN **нет** (FAIL-мода отсутствует); факторные нагрузки оцениваются регрессией на собственной истории ETP (`:876-908`) и естественно несут ~L× экспозицию. Оговорка: это не «нагрузка базы × L» из реестра — на коротком окне оценка шумная (см. B1) | Выпуклость не теряется через подмену; риск — статистический, не методологический |
| C5 | Разделение cost-of-leverage и volatility drag | **FAIL** | Expense ratio не моделируется нигде (поля нет); funding spread — нет; min(α̂,0) сваливает drag + ER (1.04% CONL) + funding + идиосинкразию в один член (`:134-146`) | Советник не может объяснить клиенту структуру эрозии CONL («сколько съедает комиссия vs механика ресета»); при смене режима σ прогноз не адаптируется |
| C6 | MaxDD достижим компаундингом | **PASS** (реализованный) / **PARTIAL** (стресс) | Реализованный MaxDD — peak-to-trough на фактической equity-кривой exp(cumsum) (`investment_logic.py:1109-1119`): эрозия пути учтена по построению (тест: −97% на 500-дневном пути ресета против −44% «наивных 2×»). Стрессовый est_dd — эвристика σ_quarterly (`stress.py:23-29`) + convex-cap **±35%** (`:85-117`), который для 2×-ETP может ОГРАНИЧИВАТЬ модельную просадку ниже реалистичной | Исторический MaxDD честен; сценарный хвост LETF срезан капом, задуманным против экстраполяции бет обычных акций |

### Блок D — VaR

| # | Проверка | Вердикт | Код | Последствие |
|---|---|---|---|---|
| D1 | Параметрический VaR: fat tails / предупреждение | **N/A** (в плюс) | Параметрический VaR не используется: VaR_95 — исторический перцентиль (`investment_logic.py:1096`), CVaR — хвостовое среднее + stationary block bootstrap, сохраняющий автокорреляцию (`:446-457`) | Гауссова недооценка хвоста не встроена конструктивно; отдельного предупреждения о скошенности LETF нет, но и нормальность не предполагается |
| D2 | Historical VaR: guard по числу наблюдений | **PARTIAL** | Каскад гейтов есть (см. A2): <60 → имя вне модели / серия не строится; книга целиком <60 → метрик нет (тест). Пробел: окно 60–99 печатает точечный VaR/CVaR как валидный; интегрити-чип «точечная оценка (мало данных)» появляется только про bootstrap-ДИ (`pdf_payload.py:1902-1911`), не про сам VaR | На 20 точках распределение не строится (guard срабатывает — подтверждено тестом); на 69 точках — строится и подаётся без вердикта надёжности |
| D3 | Monte-Carlo VaR path-dependent для LETF | **N/A** | MC-VaR не реализован вовсе (явный комментарий `simulate.py:30`); path-dependent симуляция ресета — roadmap (`METHODOLOGY... §5 п.3`) | Нечему искажать; при будущей реализации требование path-dependence уже зафиксировано в roadmap и в xfail-тесте |
| D4 | Σ на несопоставимых историях | **PARTIAL** | Обрезка до общего окна: row-dropna (`investment_logic.py:803`) + sparse-guard 60 (`:786-800`) + гейт регрессии 30 (`:837-845`); сценарный тир — pairwise cov `min_periods=252` с перенормировкой и раскрытием исключённых (`scenario_engine.py:106-127`). Раскрытие: warning в лог + integrity-панель (`pdf_payload.py:1851-1864`). **ДИ корреляции (Fisher z) нет** | SPCX(20 дн) не попадает в Σ вовсе — «нестабильной пары» не возникает. Но ρ̂, оценённые на 60–120 днях, подаются без ДИ: при n=20 ДИ ρ̂=0.30 = [−0.16, 0.66] — знак не определён, код так не сообщает |

### Блок E — прибыльность (агрегация)

| # | Проверка | Вердикт | Код | Последствие |
|---|---|---|---|---|
| E1 | ETF-обёртки исключаются из weighted ROE/margin | **PARTIAL** | Портфельного weighted ROE/EBITDA/EPS **нет как класса** (поиск по коду: агрегатор не найден) — FAIL-мода «ETF портит среднее» отсутствует конструктивно. Фундаментал подаётся по-активно; ETF без SEC-данных выпадают из слоя (`pdf_payload.py:1140-1141`). **Пробел:** guard «нет отчётности» покрывает только commodities/sovereign (`scoring_orchestrator.py:56-64`) — CONL в нём нет → его F-пиллар деградирует к макро-тилту сектора 'Other' (`:571-596`) — фантомный фундаментальный вердикт | Среднее не портится (его нет); но LETF-обёртка может получить ненулевой «фундаментальный» скор из режима, которого у неё нет |
| E2 | Отрицательная прибыль | **PASS** | P/E только при NI>0 (`scoring_orchestrator.py:239-250`), P/B при book equity>0 (`:252-263`), ROE только при equity>0 (`sec_edgar.py:336-337`); отрицательный FCF-yield легитимно допускается (`scoring_orchestrator.py:157-165`) | — (закреплено тестом) |
| E3 | Медианная агрегация | **N/A** / **PARTIAL** | Портфельной агрегации прибыльности нет (см. E1) — нечего заменять медианой. Внутри скоринга робастность есть: z-скоры на median+MAD (`scoring.py:66-99`) | — |
| E4 | Доля покрытия + источник | **PARTIAL** | Покрытие: CoVe-строка «N тикеров без SEC покрытия» (`data_lineage.py:158-160`) + свежесть 10-K per-row (`SEC_Filing_Date`, `:145-167`) — в штуках, **не в % веса портфеля**. Источник: всегда reported SEC (estimates в системе нет — различать нечего, и это честно) | «3 тикера без покрытия» может значить и 5%, и 60% книги по весу — советник не видит разницы |

### Блок F — донесение до пользователя

| # | Проверка | Вердикт | Код | Последствие |
|---|---|---|---|---|
| F1 | Caveat-флаги доходят до advisor-слоя | **PARTIAL** | **Доходят:** sparse-исключения + % покрытого веса (`pdf_payload.py:1851-1864`), базис Sharpe (`:1918-1925`), «точечная оценка (мало данных)» CVaR-CI (`:1902-1911`), «вне модели: история < 60 торг. дней» в Action Plan (`action_plan.py:305-306`), стресс-дисклеймер всегда (`investment_logic.py:1180`), маржинальное плечо счёта (`ai_narrative.py:913-921`, `gatekeeper.py:283-302`). **Гасятся внутри:** `realized_window_days` (`investment_logic.py:1167`) и `leveraged_drag_tickers` (`:1584`) попадают в payload `portfolio_metrics` (`pdf_payload.py:1213`), но ни один шаблон/маппер их не рендерит (потребителей в `templates/`, `premium_payload.py` нет — проверено поиском) | Советник не видит: (а) на каком окне реально посчитаны Sharpe/CVaR/MaxDD; (б) что прогноз CONL уже понижен на drag (и наоборот — что понижение произошло, если удивляет консервативность) |
| F2 | Hardcoded нормальность / i.i.d. без предупреждения | **PARTIAL** | Нормальности в VaR нет (см. D1). **i.i.d.-масштабирование √252 повсеместно и без caveat:** `investment_logic.py:898`, `:1087`; `period_returns.py:259` (TE); `scenario_engine.py:138,159-164`; `portfolio_series.py:187`; эвристика est_dd ≈ 1·σ_quarterly (`stress.py:26-29`). Block-bootstrap CVaR — единственное место, где серийная зависимость учтена явно (`investment_logic.py:450-457`) | Для LETF (значимая отрицательная автокорреляция на коротких горизонтах из-за ресета) годовая σ по √252 систематически смещена; предупреждения нет |

---

## 3. Пробелы по критичности

**BLOCKER — не обнаружено.** Главный кандидат (отсутствие LETF-компаундинга, «в разы» искажающее риск) **не подтвердился**: все реализованные метрики считаются с фактических дневных цен инструмента, наивного `L·кумулятив` или `L·σ·√T` в кодовой базе нет (проверено тестами §4). Bfill-фабрикация истории снята ранее (F-6).

### HIGH

| # | Пробел | Проверки | Суть |
|---|---|---|---|
| H-1 | Нет реестра плеч (L, базовый актив, ER) → drag только эмпирический min(α̂,0), cost-of-leverage не отделён | C1, C3, C5 | Величина эрозии CONL невоспроизводима из контрактных параметров; ER 1.04% и funding не видны; α̂ на коротком окне шумит |
| H-2 | Стресс/сценарии для LETF: линейный β·shock + convex-cap ±35% без path-dependent пересчёта | C2, C6, D3 | Модельный хвост 2×-продукта срезается капом, задуманным для обычных акций; сценарная просадка LETF недооценивается |
| H-3 | Нет SE(β) и сжатия бет (Blume/Vasicek) | B1, B2 | Шумные беты коротких окон входят в Σ/ERC/BL как точные; предупреждений нет |
| H-4 | `realized_window_days` и `leveraged_drag_tickers` не рендерятся | F1 | Ключевые каверты вычислены, но гаснут между payload и шаблоном — наружу выходит «чистое» число |

### MEDIUM

| # | Пробел | Проверки |
|---|---|---|
| M-1 | Нет χ²-ДИ волатильности (T=20 → множитель [0.76, 1.46]) | A3 |
| M-2 | Нет Fisher z-ДИ корреляции (n=20, ρ̂=0.3 → [−0.16, 0.66]) | D4 |
| M-3 | Нет флага `insufficient_history` на точечном VaR/CVaR при окне 60–99 наблюдений | A2, D2 |
| M-4 | LETF-обёртки не входят в guard «нет отчётности» → фантомный макро-тилт в F-пилларе | E1 |
| M-5 | SEC-покрытие раскрывается в штуках тикеров, не в % веса | E4 |

### LOW

| # | Пробел | Проверки |
|---|---|---|
| L-1 | Нет обработки price-discovery-периода / lock-up expiration | A4 |
| L-2 | Нет класса LEVERAGED_ETF в таксономии; inverse не различается | C1 |
| L-3 | √252-аннуализация (i.i.d.) без caveat в отчёте | F2 |
| L-4 | Proxy-назначение не покрывает молодые equity | B3 |

---

## 4. Результаты тестов

Файл: `tests/test_phase28_risk_methodology_audit.py` (конвенция проекта: offline unittest, синтетика, детерминизм).

**Baseline до изменений:** `633 passed, 13 skipped` (полный прогон `PYTHONPATH=src python -m pytest tests/ -q`; локальная среда — CI-паритет 638/8 отличается пропусками env-зависимых тестов).
**После добавления файла:** `643 passed, 13 skipped, 8 xfailed` — существующие тесты не тронуты, регрессий нет.

### Прошли (PASS — закрепляют существующее корректное поведение)

| Тест | Инвариант |
|---|---|
| `test_fixture_reference_values` | Эталон §3.1: база 98.0 / наивные 2× 96.0 / daily-reset 2× **75.4** на 40 днях ±8/−7.5 |
| `test_return_series_layer_compounds_daily_reset` | Слой доходностей проекта даёт **−24.6%** (компаундинг), отвергает −4% |
| `test_engine_realized_metrics_carry_full_decay` | Движок: MaxDD < −90% на 500-дневном пути ресета (наивные 2× дали бы −44%) |
| `test_drag_reproduces_sharpe_identity` | F-23 drag воспроизводит тождество: Sharpe_LETF ≈ **−0.57** (не «удвоенный» +0.11) при L=2, σ=1.35, μ=0.15 |
| `test_drag_never_applied_to_plain_equity` | Drag не задевает обычные имена (SPCX) |
| `test_negative_earnings_yield_no_pe` | P/E при убытке и P/B при отриц. капитале = NaN, не «маленькое число» |
| `test_commodity_and_sovereign_etf_fundamentals_na` | GLD/TLT — фундаментал N/A; AAPL — применим |
| `test_sub60_name_dropped_from_return_series_by_default` | 40-дневная колонка исключается дефолтным гейтом 60 с фиксацией в `info["dropped"]` |
| `test_var_not_reported_for_20day_only_book` | Книга из одного 20-дневного имени: `metrics == {}` — VaR **не** печатается по 20 точкам |
| `test_mixed_book_drops_young_but_keeps_metrics` | SPCX исключён (`_last_sparse_dropped` → аннотация плана), метрики книги живы на зрелом имени |

### xfail (спецификации недостающих возможностей; снятие декоратора = приёмка реализации)

| Тест | Отсутствующая возможность | Эталон для будущей валидации |
|---|---|---|
| `test_missing_capability_leverage_registry_with_multiplier` | Реестр «тикер → L, база» + explicit drag −½L(L−1)σ_u² | leverage_of('CONL') == 2.0 |
| `test_missing_capability_sigma_chi2_ci` | χ²-ДИ σ | T=20 → [0.76, 1.46]; T=31 → [0.80, 1.34] |
| `test_missing_capability_fisher_z_ci` | Fisher z-ДИ ρ | ρ̂=0.30: n=20 → [−0.16, 0.66]; n=31 → [−0.06, 0.59]; оба пересекают 0 |
| `test_missing_capability_weighted_roe_with_etf_exclusion` | Портфельная weighted-profitability с исключением ETF из нормировки | акция ROE 15% w0.5 + LETF w0.5 → ROE **15%**, coverage **50%** |
| `test_missing_capability_leveraged_wrapper_fundamentals_na` | fundamentals_applicable=False для LETF-обёрток | `_is_credit_not_applicable("CONL", "Other") == True` |
| `test_missing_capability_insufficient_history_flag` | Флаг надёжности VaR при окне <100 | metrics содержит `insufficient_history`/`var_reliability` |
| `test_missing_capability_beta_standard_error` | SE(β)=σ_ε/(σ_m·√T) в exposures | колонки Beta_SE_* + warning при большом SE |
| `test_missing_capability_beta_shrinkage` | Blume/Vasicek сжатие | β_adj = ⅔β+⅓ или w=n/(n+120); для ETP prior = L·β_базы |

**Упавших тестов нет.**

---

## 5. Предлагаемые правки (НЕ применены; каждая требует отдельного согласования)

Отсортированы по критичности; расчётные модули в рамках аудита не менялись.

| # | Правка | Закрывает | Объём | Риск |
|---|---|---|---|---|
| P-1 | **Реестр плеч ETP**: `{base: (L, underlying, expense_ratio)}` в `investment_logic.py` (расширение существующего `_LEVERAGED_ETP_BASES`); форвард ETP = β·μ − ½L(L−1)σ_u² − ER (σ_u с истории базы/самого ETP÷L), `min(α̂,0)` оставить фолбэком для имён без параметров; QC-ключи с разложением drag/fees | H-1, C3, C5, часть C1 | ~150 строк + 10-15 тестов | Средний: меняет форвардные числа реестровых ETP — нужна сверка на живом payload (процедура F-23 повторима) |
| P-2 | **Рендер кавертов**: `realized_window_days` («метрики за N торг. дней») и `leveraged_drag_tickers` («прогноз X, Y понижен на drag ресета») как чипы integrity-панели (`pdf_payload._build_integrity_checks`) + premium-маппер | H-4, F1 | ~40 строк + 3 теста | Низкий: presentation-only |
| P-3 | **Флаг var_reliability**: в `portfolio_metrics` — `'ok' / 'insufficient_history'` при `realized_window_days < 100`; em-dash/⚠ у VaR-KPI в шаблонах | M-3, A2, D2 | ~30 строк + 3 теста | Низкий |
| P-4 | **SE(β) + Vasicek-сжатие** (за env-флагом `BETA_SHRINKAGE`, default off до валидации): SE из OLS-остатков на окне регрессии; сжатие w=n/(n+120) к prior (Market 1.0 / стили 0; для ETP после P-1 — L·β_базы); warning в exposures при SE>|β|/2 | H-3, B1, B2 | ~200 строк + 15 тестов | Средний-высокий: меняет Σ на ступенчатых книгах — уже помечено в roadmap §2.2 как требующее отдельной валидации |
| P-5 | **χ²-ДИ σ и Fisher-ДИ ρ**: чистые функции в `finance/scoring.py` или новом `finance/inference.py` (без scipy — инверсия χ² по Wilson-Hilferty либо табличные квантили), рендер в integrity/CoVe | M-1, M-2, A3, D4 | ~80 строк + 6 тестов (эталоны уже в phase28) | Низкий: аддитивно |
| P-6 | **LETF-обёртки в guard фундаментала**: переиспользовать `_is_leveraged_etp` внутри `_is_credit_not_applicable` → fundamentals/credit N/A для обёрток | M-4, E1 | ~10 строк + 2 теста | Низкий: скоринг CONL-типа имён станет честнее (F=0 вместо макро-тилта) |
| P-7 | **Path-dependent стресс для LETF** (после P-1): для реестровых имён разбивать сценарный шок на дневной путь и компаундить (1+L·r_день) вместо линейного β·shock с капом ±35% | H-2, C2/C6-стресс, D3 | ~120 строк + тесты | Средний: методология сценариев — требует ревью владельца |
| P-8 | **SEC-покрытие в % веса**: в CoVe-строке «N тикеров без SEC покрытия (X% книги по весу)» | M-5, E4 | ~15 строк | Низкий |

Рекомендуемый порядок: P-2/P-3/P-6/P-8 (быстрые, presentation/guard) → P-1 → P-5 → P-4 → P-7.

---

## Итоговый вывод

**Реализованным (историческим) риск-метрикам — σ, historical VaR/CVaR, MaxDD, Sharpe — на книгах с CONL-подобными инструментами доверять для risk-based sizing МОЖНО (компаундинг дневного ресета учтён по построению, наивного L-масштабирования в коде нет), но форвардные оценки и стресс-сценарии по leveraged ETP, а также любые метрики книг, где окно истории < ~100 торговых дней или доминируют молодые листинги, до внедрения P-1…P-4 пригодны только как directional indicators — без ДИ на σ/ρ/β и без явного разложения drag система подаёт эти числа с ложной точностью.**
