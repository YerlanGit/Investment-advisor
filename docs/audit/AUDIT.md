# AUDIT.md — навигатор аудита RAMP
<!-- nav | area:audit | code:(сквозной) | read-before:ЛЮБАЯ крупная правка — здесь открытые задачи, правила раундов и карта истории -->

> **Это навигатор, а не журнал.** Полные записи раундов живут в
> `docs/audit/rounds/`. Файл держится маленьким намеренно: он читается ЦЕЛИКОМ
> и в каждой сессии, поэтому всё, что можно не читать, отсюда убрано.
>
> **Версия:** 2026-08-25 · последний раунд `§−108` · раундов всего 117.

---

## 0. Как этим пользоваться (30 секунд)

| Твой вопрос | Куда смотреть |
|---|---|
| Что сейчас открыто и за что браться? | **§1 Доска задач** |
| Насколько проект хорош и что держит оценку? | **§2 Оценка** |
| Что значит ссылка `§−45` из комментария в коде? | `grep` ниже, либо **[`rounds/INDEX.md`](rounds/INDEX.md)** |
| Какие правила родились из прошлых ошибок? | **§3 Правила** (кратко — в `CLAUDE.md`) |
| Я закончил раунд — куда писать? | **§5 Протокол** — соблюдай дословно |

**Как найти раунд по номеру.** `§−N` лежит в файле `rounds/`, чей диапазон
накрывает `N`. Одна команда — один ответ:

```bash
grep -n "^## −45\." docs/audit/rounds/*.md
```

🔴 **Нумерация раундов НИКОГДА не меняется.** На неё ссылаются ~540 мест в
коде, тестах и доках; переименование рвёт их молча.

🔴 **Статус в §1 верен только если проверен ПО КОДУ.** Раунд `§−91` поймал
обратное: доска утверждала «`manual_portfolio.py` и `stooq_provider.py`
ОТСУТСТВУЮТ», хотя оба файла существовали, а фазы MP-06 и MP-09 были закрыты
раундами `§−84`/`§−85`. Устаревшая доска увела за собой план работ. Перед тем
как поверить строке — проверь файл.

---

## 1. Доска задач

> Сгруппировано по **цене ошибки для пользователя**, а не по трудоёмкости.
> Строка уходит в «закрыто», только когда есть (а) код, (б) тест, (в) запись раунда.
> **Статус ниже сверен по коду 2026-08-18** (`§−96`) — каждая строка проверена
> по файлу, а не по документу.

### ⚡ Очередь: что брать сейчас

> Единственное место, где задачи РАЗНЫХ проектов стоят в одном порядке.
> Порядок — по цене ошибки для пользователя, не по трудоёмкости.
> Здесь только НОМЕР и повод; разбор задачи — там, куда ведёт последний столбец.

| # | Задача | Почему именно сейчас | Где разбор |
|---|---|---|---|
| **1** 🔴 | **IB-7** · привести секрет задания в соответствие и пройти приёмку | задание заведено и `ENABLED`, планировщик в 10:02 отвечал **200** — значит OIDC, аудитория и `run.invoker` исправны. Но после ротации секрета конфиг задания остался ДОРОТАЦИОННЫМ, и в 10:39–10:41 планировщик получил **401** ×4. Расписание сейчас НЕ РАБОТАЕТ | `roadmap/ingest_bot/GCP_SETUP.md §2.5б` |
| **3** 🔴 | **R-9** · включить статический egress в GCP | живой путь: каждый рестарт — бросок кубика против WAF брокера (`§−94`). Репозиторная часть сделана в `§−97`: состояние доезжает до контейнера и диагностика его называет. Осталась ОДНОРАЗОВАЯ операция в GCP | §1 P2 ниже |
| **4** 🔴 | **MP-B** · живой прогон ручной книги | единственный незакрытый чекпойнт MP-06; паритет доказан тестом, не наблюдением | `roadmap/manual_portfolio/PLAN_2026-08-12.md` |
| **5** 🟠 | **FW-1** · тенговый остаток без курса | при недоступном FX строка входит в NAV по номиналу | `roadmap/freedom_warehouse/PLAN_2026-08-12.md` |
| **6** 🟠 | **MP-A** · провести замер оси ДИВИДЕНДОВ | инструмент написан и проверен (`§−97`), сам замер требует оператора и второго источника | `roadmap/manual_portfolio/PLAN_2026-08-12.md` |
| **7** 🟢 | **MP-07** · загрузка портфеля файлом | удобство, не блокер — но уже обещано пользователю вслух (`§−96` D-1) | `roadmap/manual_portfolio/PHASE_07_FILE_UPLOAD.md` |
| **8** 🔴 | **Инфраструктура** · Cloud SQL вместо SQLite-на-gcsfuse | потолок ВСЕЙ оценки: `max-instances=1`, платежей нет | отдельный проект |

**Владелец отложил осознанно:** MP-B, FW-1, MP-07 и Cloud SQL (решение 18.08).
Они остаются в очереди, но за них не берутся, пока владелец не скажет.

**IB-F2 переведена в очередь (`§−109`).** Прежнее основание — «пока писателя
нет, радиус поражения не расширен» — отменено фактом: писатель задеплоен и
пишет 25.08. Проверка стоит одну команду (`scripts/verify_ingest_iam.sh`).

**Что НЕ является задачей.** Фаза 10 (EODHD) — осознанная отсрочка до +100
пользователей и коммерческого договора, а не пробел. Проверка глубины базы
Stooq — приёмка (`verify-universe`), а не блокер: посылка «в базе ~7 торговых
дней» отозвана владельцем (`AUDIT_360_2026-08-17 §1`, разбор — `§−96` D-4).

### 🔴 P0 — показывает неверное число или ломает отчёт

*(пусто)*

### 🟠 P1 — вводит в заблуждение, деньги или комплаенс


- [x] **IB-F2 · ЗАКРЫТА 26.08 замером.** `scripts/verify_ingest_iam.sh` на живом
  проекте: `roles/storage.objectAdmin` С условием `stooq-only`, политика ПРОЕКТА
  чиста, широких привязок нет. Радиус писателя узок — подтверждено, а не обещано.
  История задачи ниже оставлена: она объясняет, ЗАЧЕМ этот замер нужен.

- [ ] ~~**IB-F2 · база котировок живёт в бакете с балансами и ключами брокера.**~~
  🔴 **СТАТУС ИЗМЕНИЛСЯ 25.08: писатель ПОЯВИЛСЯ.** Формулировка «пока писателя
  нет — безопасно» больше не действует. Загрузчик задеплоен и публикует
  `gs://ramp-bot-state/stooq/prices.sqlite` — тот же бакет, где лежат
  `tokenomics.db` (балансы) и `users_vault.db` (ключи брокера). Кольцо на этом
  и замкнулось (`§−103`), то есть выбор осознанный, но КОМПЕНСИРУЮЩИЙ КОНТРОЛЬ
  теперь единственный: условие IAM на префикс `stooq/`.
  🔴 **Репозиторий это условие проверить НЕ МОЖЕТ** — шаг деплоя лишь ПЕЧАТАЕТ
  команду его завести, а выдаёт права оператор вручную.
  **`§−109` превратил обещание в измерение:** `scripts/verify_ingest_iam.sh`
  читает политику бакета И ПРОЕКТА (грант уровня проекта перекрывает условие
  целиком, и проверка одного бакета его не увидит) и отличает четыре дыры от
  здорового состояния. 🔴 Опасен не отсутствующий доступ, а доступ БЕЗ
  УСЛОВИЯ: бот при нём работает штатно, симптома нет — класс `§−90` A-3.
  Осталось ЗАПУСТИТЬ: `./scripts/verify_ingest_iam.sh` (0 — узко, 1 — дыра).

### 🟡 P2 — сопровождаемость и косметика

- [ ] **Доля IT: карточка нейтральна, когда план УХУДШАЕТ нарушенный лимит.**
  В живом DEEP мандатная панель отмечает Tech-комплекс 60% против мягкого
  лимита 40%, а панель «Эффект» показывает рост доли IT 59.6% → 68.1% с
  НЕЙТРАЛЬНЫМ тоном. Сам по себе нейтралитет осознан (Sprint-5.1 A2: доля
  сектора — не универсальное благо/зло, и прежний «любой срез техно = зелёный»
  был хуже), поэтому `§−102` его НЕ трогал: флип вернул бы закрытый дефект.
  Открыт вопрос УЖЕ: тон должен знать про НАРУШЕННЫЙ лимит — рост сверх
  превышенного порога не нейтрален. Цена ошибки — читатель не видит, что план
  усугубляет то, на что отчёт же и жалуется. Найдено `§−102`.

- [ ] **Штамп «статус сверен по коду <дата>» протухает быстрее доски.**
  `§−96` уже ловил этот класс: устаревшая доска увела за собой план работ.
  Штамп стоит на 2026-08-18, при этом `§−102` сверял другое подмножество.
  Лечение: либо штамп на КАЖДОЙ строке, либо снять его совсем и оставить
  правило «перед тем как поверить — проверь файл». Найдено `§−102`.

- [ ] **R-9 · статический egress не включён.** 🔴 P1 по цене ошибки.
  `scripts/setup_static_egress.sh` есть с `§−53`, но `configure-egress` в
  `cloudbuild.yaml` — no-op при пустом `_VPC_CONNECTOR` (дефолт). Без него
  каждый новый инстанс берёт IP из общего пула Cloud Run, который отбивает WAF
  брокера (`INFRA_NETWORKING.md §1`) — рестарт становится броском кубика
  (`§−94`). **`§−97` сделал репозиторную половину:** состояние доезжает до
  контейнера (`STATIC_EGRESS`), и отказ брокера теперь НАЗЫВАЕТ причину, по
  которой блок не пройдёт сам. Осталась одноразовая операция в GCP плюс
  подстановка в Build Trigger — правкой репозитория она не делается.

### 🟢 Открыто по проектам (детали — в планах проектов)

| Проект | Открытое | План |
|---|---|---|
| Manual Portfolio | MP-A замер дивидендной оси (инструмент готов) · MP-B приёмка · MP-07 файл (Фаза 10 отложена осознанно) | `roadmap/manual_portfolio/PLAN_2026-08-12.md` |
| Ingest Bot | IB-0…IB-8 доставлены офлайн (20.08); открыт ДЕПЛОЙ: бакет, секреты, Scheduler | `roadmap/ingest_bot/PLAN_2026-08-17.md` |
| Freedom API / витрина | FW-1 тенге без курса · FW-2 сплиты · FW-3 приёмка · FW-4 алертинг | `roadmap/freedom_warehouse/PLAN_2026-08-12.md` |
| Инфраструктура | 🔴 SQLite на gcsfuse ⇒ `max-instances=1`, платежей нет | — |

### ✅ Закрыто

- **Индекс раундов вынесен в `rounds/INDEX.md` (`§−108`).** Он занимал **127 из
  534 строк навигатора (23%)** при потолке 600 и запасе 66 строк — примерно 13
  раундов, а ОДНА сессия 25.08 добавила шесть. Индекс — данные для ПОИСКА, а
  навигатор читается ЦЕЛИКОМ в каждой сессии. После переезда: **439 строк,
  запас 161** — втрое больше. Гейт полноты переехал ВМЕСТЕ с данными.

- **IB-RING — закрыта ЖИВЫМ НАБЛЮДЕНИЕМ (25.08), а не рассуждением.**
  `/status` задеплоенного загрузчика напечатал
  `gs://ramp-bot-state/stooq/prices.sqlite` — тот же объект, что отчётный бот
  читает через gcsfuse `/mnt/state`. Значит `_QUOTES_BUCKET` переопределён в
  Build Trigger и кольцо замкнуто. Первая ОНЛАЙН-заливка прошла: поколение
  сменилось, `20260824_d.txt` применён, C-1 10/10. Этим же подтверждено право
  ЗАПИСИ — до того была проверена только половина, чтение. Сводка сошлась до
  строки: 631 + 11 559 + 166 + 154 + 43 + 1 = 12 554 прочитанных.

- **`§−102`** — аудит живых отчётов 24–25.08 ОТ АРТЕФАКТА. Математика сверена
  и верна (Base Currency/RFR, Sharpe/Sortino, Euler, факторы, 4-Pillar, стресс,
  режим), правок в числах нет. Найдено другое: у высвобожденного веса НЕ БЫЛО
  адресата — бинарный гейт `not have_buy` прятал строку «Кэш», 20% NAV
  исчезали из панели, и дыру заполняла модель выдуманной покупкой
  («в качественные диверсификаторы вне техно»). Лечится ОСТАТОЧНЫМ гейтом;
  инвариант «Σ delta_pp панели = 0» под гейтом, 4 мутации из 4. Второе:
  подпись, срезанная ВНУТРИ колонки, — переполнения нет, а имени показателя
  читатель не видит (`Real GDP growth (SAAR)` — 1 px из 141); заведена вторая
  мобильная метрика `measure_clipped`, 6→0 и 4→0 обрезок.

- **`§−101`** — ГЛАВНЫЙ бот переехал на имя OMBRI. Разбор до правки показал,
  что слово `ramp` — это ЧЕТЫРЕ разные сущности (бренд · ресурс GCP · контракт
  `df.attrs['_ramp_*']` · хелпер `scoring._ramp`), поэтому замена по слову
  запрещена. Состояние ключуется `telegram_id` ПОЛЬЗОВАТЕЛЯ и переезд
  переживает; необратима одна вещь — deep-link в уже выпущенных отчётах.
  Имена вынесены в `src/branding.py`; имя секрета и хэндл переключаются
  ТОЛЬКО парой, и это тест. Дефолт подстановки — прежний секрет: привязка к
  несуществующему уронила бы весь деплой. Заключительная проверка нашла
  унаследованный дефект: `@OMBRI_support_bot` разъезжался в Markdown —
  Telegram схлопывал `_support_` в курсив, и копировался несуществующий
  контакт (`tg_bot.md_safe`).

- **`§−100`** — токен загрузчика переехал на `OMBRI_INGEST_BOT_TOKEN`, имя
  СЕКРЕТА вынесено в подстановку `_INGEST_BOT_TOKEN_SECRET`; прежнее имя
  переменной принимается с предупреждением, секреты ГЛАВНОГО бота не тронуты.
  Кольцо «загрузчик → отчёт» замкнулось на общем бакете с условием IAM;
  равенство имени объекта у писателя и читателя закреплено тестом.

- **`§−99`** — стык двух ботов разобран перед вливанием. Самое дорогое: новые
  тесты читали `cloudbuild.yaml` без `skipTest` и в образе валили ВЕСЬ деплой
  (5 failed), включая главный бот; правило «читаешь файл вне образа → skipTest»
  стало исполняемым гейтом. Дальше: гонка импорта `§−98`
  закрыта и у загрузчика (`_WORKER_HEAVY_IMPORTS`), общий токен теперь
  отказ на старте, а не упавший главный бот, и снят красный `test_repo_hygiene`
  после слияния веток. Гейт `§−98` расширен на оба бота и на полные имена
  модулей; 9 мутаций из 9. **Поправка `§−100`:** переезд `STOOQ_DB_PATH`, что
  висел здесь как открытый, НЕ НУЖЕН — владелец свёл оба бота в `ramp-bot-state`
  с условием IAM на `stooq/`, и кольцо замкнулось.

- **`§−98`** — старт контейнера перестал быть броском кубика: демон-поток RAG
  поднимается ПОСЛЕ тяжёлых импортов главного потока, гонка импорта numpy
  (`partially initialized 'numpy._typing'`) закрыта структурным гейтом
  `tests/test_phase58_boot_import_race.py` (доказан 4 мутациями из 4).
  Попутно: выгрузка Cloud-Run-логов больше не режет traceback на 500 символах —
  без этого причина не находилась вообще.

- **27 из 27** позиций доски прежних раундов (последняя — Арх-3, `§−56…§−68`).
- **`§−90` A-1…A-8** — восемь дефектов слоя показа по живым отчётам 12.08.
- **`§−91` B-1…B-5** — фолбэк Jinja v3, мёртвые ключи контракта, дрейф образцов.
- **R-8** (`§−95`) — литерал «GS / MS / JPM» убран из бандлов, Jinja и образцов;
  идентичность эмитента сведена в один реестр, потребители связаны тестом.
- **`§−92` C-1…C-3, C-5** — паритет доведён до СЛОЯ ОТЧЁТА, `hy_oas` связан с
  каталогом, знаменатель доли IT сведён с панелью секторов, подпись окна
  спарклайна стала видимой строкой (на телефоне наведения нет).
- **`§−93`** — синк ChromaDB только на буте; Jefferies перестал быть `Unknown`.
- **`§−94`** — «Freedom Broker недоступен» разложен на три причины; «пройдёт за
  5–15 минут» больше не обещается при `waf_block` и `parse_error`.
- **`§−97` E-1…E-11** — полоса KPI стала ОДНОЙ на два тира (BASE считал заметку
  ИИ и ряд спарклайна и терял их в маппере); нарезка чанков RAG режет по
  границам фраз и больше не теряет короткие секции; счётчик отрывков и формула
  отбора печатаются по коду, а не литералом.
- **R-2 · R-3 · R-4 · R-5 · R-6 · R-7 · IB-F1 · MP-D** (`§−97`) — восемь задач
  доски. Замер по IB-F1: деплой-гейт видел **1339** тестов, стал видеть
  **1640**. Замер по R-5: прежняя метрика переполнения была обнулена
  страничным `overflow-x: hidden` и не ловила даже заведомо широкий блок.
- **Manual Portfolio, фазы 00…09** — доставлены (`§−70`…`§−86`); проверено по
  коду: `manual_portfolio.py`, `stooq_provider.py`, `stooq_store.py`,
  `stooq_ingest.py` существуют, `provider_for_source("manual") → StooqProvider`.

---

## 2. Строгая оценка проекта

**Итог: 8.0 / 10.** Шкала: 10 — можно продавать институциональному клиенту без
оговорок; 7 — работает и честно, но есть структурный долг; 5 — работает на удаче.
Разбор — `AUDIT_360_2026-08-12.md §6`.

| # | Подсистема | | Что держит оценку |
|---|---|---|---|
| 1 | Квант-математика | **8.5** | Проверена от артефакта назад — ошибок в числах нет. Нет бэктеста сигналов |
| 2 | Честность отчёта | **8.5** | CoVe 19 строк, провенанс раскрыт. `§−102`: панель «Эффект» теперь СХОДИТСЯ (Σ движений = 0) |
| 3 | Слой данных | **7.0** | Stooq доставлен; корпоративные события не корректируются |
| 4 | Отчёт (UI/UX) | **9.0** | `§−102`: к замеру переполнения добавлен замер ОБРЕЗКИ — подпись в 1 px из 141 проходила мимо первой метрики |
| 5 | ИИ-нарратив | **7.0** ↓ | `§−102`: модель ВЫДУМАЛА адресат 20% NAV, потому что движок его не сообщил. Anti-fabrication держит только то, что доехало в промпт |
| 6 | Телеграм-бот и биллинг | **7.5** | Списание после доставки, слот освобождается на всех ветках. FSM в памяти |
| 7 | **Инфраструктура** | **5.5** | 🔴 Потолок для всей оценки: одноинстансность, нет платежей |
| 8 | Архитектура кода | **8.0** | `tg_bot.py` 3 819 строк — крупнейший модуль без контракта |
| 9 | Тесты | **9.0** | 1 899 (образ: 1 808). Доставка до глаз проверяется по DOM; гейты `§−102` доказаны 4 мутациями из 4 |
| 10 | Документация | **8.0** ↑ | `§−91`: журнал расслоён, 10 висячих ссылок починены |

**Что мешает выше:** инфраструктура (5.5) · промпт вне тестового контура ·
ось дивидендов Stooq не измерена. Вёрстка и доставка до глаз закрыты гейтами
в `§−97`.

---

## 3. Правила, рождённые раундами

- **Тяжёлый импорт — только в ГЛАВНОМ потоке и ДО старта фоновых.** Демон-поток
  `rag-boot-ingest` поднимался раньше `from tg_bot import main`, и два потока
  входили в numpy с разных сторон; CPython отдаёт одному из них
  полуинициализированный модуль. Проиграл фоновый — строка в логе; проиграл
  главный — контейнер не стартует (`§−98`). Лечится ПОРЯДКОМ, а не ретраем:
  всё тяжёлое грузит главный поток, фоновый стартует после.

> Каждое правило — след конкретной ошибки. Ссылка ведёт к раунду, где она
> стоила денег или доверия. Краткая выжимка продублирована в `CLAUDE.md`
> (он читается всегда); здесь — с обоснованием.

**Числа и вердикты**
- Числа считаются ТОЛЬКО в `finance/*`; `pdf_payload` форматирует, шаблон/JSX
  показывает (`§−54`).
- **Статус/тон карточки — ВЕРДИКТ, а не оформление.** Литерал статуса в
  маппере запрещён: Sharpe 0.56 годами носил ярлык «good» (`§−90` A-1).
  Неизвестный вход → «внимание», НИКОГДА не «ok».
- **Счётчик печатается по данным.** «9 позиций», «7 сценариев», «4 идеи» —
  один и тот же дефект трижды (`§−90` A-8, `§−91` B-4, `§−48`).

**Провенанс и честность**
- **У высвобожденного веса ВСЕГДА есть адресат, и панель обязана сходиться.**
  Бинарный гейт «есть ли покупка вообще» пропускал строку «Кэш», 20% NAV
  уходили в никуда, и адресат доставался фантазии модели (`§−102`). Гейт по
  СОСТОЯНИЮ («сколько веса осталось непоглощённым»), а не по факту наличия
  события. Инвариант: Σ движений панели = 0.
- **Пустая коллекция ≠ «всё хорошо».** Пустой `fx_conversion` означал и
  «нечего конвертировать», и «курса не дали» — печаталось зелёным (`§−90` A-3).
- **Один цвет — один факт.** Свежесть данных и направление величины — разные
  каналы, иначе замедляющийся ВВП зеленеет (`§−90` A-4).
- **Прокси влияет ТОЛЬКО на риск**; цена и P&L — реальный тикер (`§−38`, `§−72`).

**Контракт отчёта**
- **Новый ключ design-data обязан иметь потребителя в бандле тира** и быть
  СМОНТИРОВАН, а не только объявлен (`§−90` A-2, `§−91` B-1…B-3). Тест читает
  `src/premium_assets/*.js` — поставляемый артефакт, а не исходник `.jsx`.
- **Факт из базы, а не литерал в шаблоне.** Блок «ИСТОЧНИКИ» BASE печатал ТРИ
  ВЫДУМАННЫХ названия банковских отчётов — несуществующий факт ровно там, где
  читатель проверяет происхождение данных (`§−95`). Нечего назвать — молчи.
- **Идентичность сущности описывается ОДИН раз.** Имя эмитента жило в четырёх
  местах и разошлось шестью способами, включая пять банков, молча ставших
  `Unknown` на конвенции имён самого проекта (`§−95`). Факты — в реестре,
  решения — у потребителей, связь между ними — под тестом.
- **Починка обязана накрывать фолбэк.** `§−90` вылечил Premium и оставил
  Jinja v3 с тем же дефектом; отключение флага возвращало «закрытый» баг (`§−91` B-1).
- **Образец `*-data.sample.json` — это фолбэк рендера**, а не картинка (`§−91` B-3).

**Промпт**
- **Запрет неисполним без факта.** Если правило ссылается на состояние движка
  (погашенная дельта Sharpe), состояние обязано доехать до модели (`§−90` A-5).
- ИИ не считает сам; суммы и адресаты приходят готовыми (`§−44` R-5).
  Разность двух похожих чисел — тоже вычисление: модель описала наклон
  ПОРТФЕЛЯ числом БЕНЧМАРКА и получила противоположный вывод (`§−97` E-5).
- **Правило, которого нет в промпте, модель не исполняет.** Короткие имена
  банков в прозе запрещены с `§−14`, но запрет жил в коде аудитора, а промпт
  своими примерами `[GS]/[JPM]` УЧИЛ короткой форме (`§−97` E-7).

**Показ и вёрстка**
- **Молчание — худший ответ бота.** Доставка обязана быть ЛЕСТНИЦЕЙ (разметка →
  голый текст → уведомление): любой отказ Telegram, стоящий вне `try`, даёт
  оператору тишину вместо ответа, и три команды загрузчика так и молчали
  (`§−104`). Гард длины при этом обязан ограничивать ОТДАННОЕ: обрезка до
  экранирования превышала потолок в 4.8 раза.
- **Подменённый сервис в стенде обязан УМЕТЬ ОТКАЗЫВАТЬ.** `FakeBot`,
  принимающий что угодно, доказывает лишь то, что хендлер дошёл до `answer` —
  и `§−103` из-за этого промахнулся мимо дефекта целиком (`§−104`).
- **Одна сущность — ОДНА реализация, включая вёрстку.** Карточка KPI жила в
  двух копиях, и BASE потерял график динамики с заметкой ИИ, хотя данные для
  них приезжали (`§−97` E-1). Копия расходится всегда — вопрос только когда.
- **Обрезка ВНУТРИ колонки — не переполнение, и первой метрикой не ловится.**
  «0 px переполнения» был честен, пока `Real GDP growth (SAAR)` показывался в
  1 px из нужных 141 (`§−102`). Текст, срезанный в своей колонке, за экран не
  выходит. Мерить надо ОБА класса: уход за экран и потерю букв в листе.
- **Метрика, обнулённая тем, что она измеряет, — не метрика.** «0 px
  переполнения» мерилось `scrollWidth`, который страничный `overflow-x: hidden`
  делает равным экрану при ЛЮБОЙ вёрстке (`§−97` E-6). Отсечение на уровне
  страницы обрезает содержимое, а не помещает его.

**Процесс**
- **Гейт читает КОД, а не комментарии.** Текстовые гейты по бандлу дважды за
  раунд упали на разборе дефекта, который сами же охраняют (`§−97` E-9). Гейт,
  срабатывающий на объяснении, учит удалять объяснения — то есть ровно те
  записи, ради которых ведётся журнал.
- **Инструмент проверки сам обязан быть проверен.** За Арх-3 самописные
  детекторы соврали восемь раз (`§−68`); в `§−90` детектор перекрытий дал 20
  ложных срабатываний; в `§−91` мутация оказалась невалидной, а `.pyc` —
  устаревшим. Правило: сперва воспроизведи известно-верный результат.
- **Устаревшее обоснование опаснее отсутствующего** — оно убеждает не
  проверять (`§−64`, `§−91` B-2 и стухшая доска). Тот же класс в `§−96`:
  навигатор звал свежим предыдущий сквозной аудит, а флаг ручного ввода
  объяснял себя фазой, закрытой двенадцатью раундами раньше.
- **Задача описывается ровно настолько, насколько она открыта.** MP-A стояла на
  доске как «корпоративные события» ценой в два дня, хотя ось сплитов была
  закрыта замером (`§−81`) и двумя слоями коррекции на чтении, а открыта —
  только дивидендная, и она ЗАМЕР (`§−96` D-2). Задача, записанная шире
  открытого, стоит в очереди с чужой ценой и вытесняет настоящую.
- Мобильная вёрстка правится ПО ЗАМЕРУ (320/360/390/414), а не на глаз;
  детектор обязан отсеивать свёрнутые аккордеоны (`§−90` §3).

---

## 4. Индекс раундов

> Полный индекс — **[`rounds/INDEX.md`](rounds/INDEX.md)**: одна строка на
> раунд, свежие сверху, с файлом диапазона в последнем столбце.
>
> Он переехал туда в `§−108` по замеру: 127 из 534 строк этого файла (23%) при
> потолке 600 и запасе в 66 строк. Индекс — данные для ПОИСКА, а не для чтения
> подряд; навигатор же читается целиком и в каждой сессии, поэтому платить за
> них каждый раз незачем. Дублировать сюда «последние N» тоже нельзя: одна
> мысль живёт в одном месте (§5.2).

**Найти раунд по номеру** — одна команда, индекс для этого открывать не нужно:

```bash
grep -n "^## −45\." docs/audit/rounds/*.md
```

Гейт полноты («каждый раунд из `rounds/` имеет строку в индексе») переехал
вместе с данными: он читает `rounds/INDEX.md`. Правило без гейта отрастает
обратно — это уже проверено на `CLAUDE.md` (`§−54`) и на самом журнале (`§−91`).

## 5. Протокол обновления

> Этот раздел существует потому, что прошлый порядок дал сбой измеримо:
> журнал вырос до **382 КБ**, шапка — до **76 КБ**, одна строка — до **27.6 КБ**,
> а 14 раундов вообще не получили секции, хотя на 10 из них ссылается боевой код.
> Разбор — `§−91`.

### 5.1 Куда писать результат раунда

1. **Полная запись — в `rounds/`**, в файл своего диапазона, СВЕРХУ:
   ```markdown
   ## −92. Короткий заголовок по сути (YYYY-MM-DD)

   ### Что изменилось ПО СУЩЕСТВУ
   ### Верификация
   ```
   Номер = предыдущий + 1. Никогда не переиспользуй и не переставляй номера.

2. **Строка в [`rounds/INDEX.md`](rounds/INDEX.md)** — ровно одна, в том же формате.

3. **Доска §1** — только если изменился СТАТУС задачи. Закрыл — перенеси в
   «Закрыто» одной строкой. Нашёл новое — заведи строку с ценой ошибки.

4. **Правило §3** — только если родилось НОВОЕ правило. Правило без ссылки на
   раунд не принимается: оно перестанет быть проверяемым.

5. **Оценка §2** — только если подсистема реально сдвинулась, с обоснованием.

### 5.2 Что сюда НЕ пишется

🔴 Эти запреты — прямые следствия того, как файл разрастался:

- **Нет блока «Версия».** Он был препенд-логом и превратился в дубликат
  раундов: тот же факт в двух местах, причём читаемый — худший.
- **Нет пересказа раунда в шапке.** Одна мысль живёт в одном месте.
- **Нет «текущей ветки», «текущего аудитора», «даты последней верификации»** —
  это протухает быстрее всего и врёт громче всего (в старой шапке висела
  ветка трёхмесячной давности).
- **Нет длинных абзацев в одну строку.** Перенос по ~100 символов: строку в
  27 КБ невозможно ни ревьюить, ни дифать.

### 5.3 Гейт

`tests/test_repo_hygiene.py` проверяет форму этого файла: размер, отсутствие
блока «Версия», длину строк и то, что **каждый раунд из `rounds/` имеет строку
в индексе**. Правило без гейта отрастает обратно — это уже проверено на
`CLAUDE.md` (`§−54`) и на самом журнале.

```bash
PYTHONPATH=src python -m pytest tests/test_repo_hygiene.py -q
```

---

## 6. Сквозные аудиты

| Документ | Что внутри |
|---|---|
| `AUDIT_360_2026-08-17.md` | 🟢 Свежий. Pre-flight ручного ввода + КАРТА ПРОИСХОЖДЕНИЯ ДАННЫХ (§2) |
| `AUDIT_360_2026-08-12.md` | 🗄 Разбор ОТ АРТЕФАКТА: живые BASE/DEEP, мобильный замер, строгая оценка |
| `AUDIT_360_2026-07-30.md` | 🗄 Снимок: 27 611 строк `src/`, батарея враждебных портфелей |
| `AUDIT_360_2026-07-10.md` | 🗄 Снимок |
| `PRODUCTION_READINESS.md` | 📌 Готовность к проду |
| `risk-methodology-audit.md` | 📌 Обоснование `leveraged.py` / `inference.py` |
