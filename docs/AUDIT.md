# AUDIT.md — RAMP Bot · Институциональный аудит и стратегия

> **Версия:** 2026-07-18 (§−25 логика отчёта: BASE-бенчмарк, Action Plan↔количество, Effect Продать/Купить, риск-гейдж-аггравторы, ИИ-цепочка L-1…L-5) · 2026-07-17 (§−24 сквозная связь «Мандат → Отчёт»: беты бенчмарка в факторной секции B1-A…B1-C + /mandate-меню + тариф 2 500 ₸) · 2026-07-16 (§−23 бенчмарк equity-curve следует выбору пользователя + готовность/IBKR-роадмап · §−22 источник портфеля: UPSERT-хранение режима, recovery по ключам, бесплатное демо M-3…M-5) · 2026-07-14 (§−21 multi-user: тихий откат в демо + защита сервисного ключа M-1…M-2) · 2026-07-11 (§−20 композитная база метрик + drag плечевых ETP F-20…F-23 · §−19 post-release hotfix сломанного DEEP F-14…F-17) · 2026-07-09 (§−18 ревью-2 прод-отчётов R2#1–R2#7 · §−17 ревью прод-отчётов #1–#6) · 2026-07-05 (§−16 финальный 360-аудит + фиксы «Режима» R-1…R-9 · §−15 RAG-наблюдаемость/инфляция-overlay/un-cut/миграция бакетов · §−14 Roadmap A/B/C) · **Базовый коммит:** `ed85a8d` (merge PR #47 → `main`)
> **Текущая ветка:** `claude/bank-rag-deep-analysis-cf106s` — Premium V2 в проде; коммиты на GitHub **Verified ✅** (`Claude <noreply@anthropic.com>`)
> **Аудитор:** CTO / Lead Quant Architect / Lead UI / DevSecOps
> **Верификация:** живые прод-отчёты 2026-06-09 → **2026-07-05** (`base/deep.html`, U148046720); рендер-верификация Playwright/Chromium (390/360px); **раунд 16 — pytest 552 passed, 12 skipped** (полный прогон после каждого изменения ядра)
> **Карты:** `REPORT_SECTIONS.md` (секция→builder→шаблон) · `REPORT_SECTIONS_AUDIT.md` (посекционный аудит живых отчётов)

---

## −25. Логика отчёта: BASE-бенчмарк · Action Plan↔количество · Effect Продать/Купить · риск-гейдж · ИИ-цепочка (2026-07-18, раунд 25)

> **Контекст:** аудит живого DEEP/BASE (r/148046720) после B1 — владелец нашёл 5 расхождений логики/связности. Все закрыты; **pytest 743 passed, 1 xfailed** (+16: phase32 риск-аггравторы/action-qty/sector-helper, phase3 action↔BL, phase19 контракт 34→35).

| # | Было | Стало |
|---|---|---|
| L-1 🔴 | BASE «Рост против рынка» подписан «S&P 500», хотя данные — профильного бенчмарка (Nasdaq); подпись захардкожена в `portfolio-performance.jsx` в 5 местах | `build_payload` переименовывает слот «Профильный бенчмарк» в реальное имя; `performance.benchmarkName` → компонент рендерит `{bench}`; v3 `_bench_keys[0]`. Цифры и подпись согласованы (см. §8 REPORT_SECTIONS) |
| L-2 🟠 | Action Plan: «+1 шт» под SELL — «действие» из 4-Pillar, «количество» из Black-Litterman, знаки расходятся | `build_action_plan`: противоречие знака → `qty=None` + «BL расходится с сигналом (+Xпп)»; согласующиеся — как есть |
| L-3 🟠 | Effect не показывал ЧТО продаётся/покупается — только чип тикеров | `effectActions` (Продать/Купить + Δпп) → `EffectGrid` красный/зелёный блоки; метрики «до/после» — результат этих сделок |
| L-4 🟠 | Риск-гейдж 48 «Умеренный» для 73%-tech книги с плечом и −43.5% просадкой — занижен | `composite_risk_score` +bounded аггравторы MaxDD/сектор/плечо (только повышают, default None=без изменений); verdict+effect согласованы через `simulate`; живая книга 48→70 «Агрессивный» |
| L-5 🟡 | ИИ-цепочка verdict→…→action не держала бенчмарк/мандат явно | Бенчмарк+мандат вплетены в «ЦЕПОЧКУ АНАЛИЗА» обоих промптов; `mandate_rule` несёт лимиты классов + целевую vol; Action Plan обязан вести долю в лимит |

> **Не тронуто по запросу владельца:** режим `Expansion` при confidence 8% — дано детальное объяснение (низкая уверенность честно показана в гейдже/идеях/CoVe; менять классификатор преждевременно без валидации). Аггравторы НЕ дублируют плечо/сектор из отдельных панелей — композит агрегирует их в ОДНО число гейджа, панели остаются детализацией.

---

## −24. Сквозная связь «Мандат → Отчёт»: беты бенчмарка в факторной секции + /mandate-меню + тариф 2 500 ₸ (2026-07-17, раунд 24) — B1

> **Контекст (ТЗ B1, P1):** клиент сменил бенчмарк S&P 500 → NASDAQ через
> /mandate, но секция «Факторное разложение (β)» DEEP по-прежнему сравнивала с
> S&P 500 (скриншот `r/148046720/2026-07-17/deep.html`) — отчёт внутренне
> противоречив (returns-слой уже следовал выбору после §−23). Root-cause — ТРИ
> независимых хардкода: фронт (подписи в `deep-factors.jsx`/`deep-components.js`),
> payload (`_BENCH_FACTOR_BETAS` = константа, профиль не читался), движок (ось
> Market = SPY.US — НЕ баг, каноническая ось). Принят **Вариант A ADR**
> («бенчмарк — отдельная сущность»): модель не тронута, столбец = реальные беты
> бенчмарка. **pytest 727 passed, 1 xfailed** (+29,
> `tests/test_phase31_benchmark_factor_propagation.py`; DEEP-контракт 32→34).

| # | Было | Стало |
|---|---|---|
| B1-A 🔴 | Фронт: `<th>S&P 500</th>`, легенда «Рынок (S&P 500)», плашка «Почему у S&P 500…» захардкожены | `FactorTable({factors, benchmarkName})`, легенда «Бенчмарк ({name})», плашка переписана под общий случай («Наклон Δ = β портфеля − β бенчмарка»); пересборка `build.sh`, тест-греп артефакта на литералы |
| B1-B 🔴 | Payload: `_BENCH_FACTOR_BETAS = (Market=1, rest=0)` — профиль клиента не читался | `_build_factor_betas_table` читает `results["benchmark_factor_profile"]`; константа оставлена ЯВНЫМ фолбэком; `benchmark_name/ticker` в payload следуют ЗА ФАКТОМ (фолбэк → подпись «S&P 500» вместе с числами — отклонённый «Вариант C» невозможен by construction) |
| B1-C 🟠 | Движок не умел считать беты произвольного тикера | `MAC3RiskEngine.fit_factor_betas` — тот же Ridge-пайплайн, что у активов (панель `factor_tickers`, ортогонализация BLOCK 3.5, α=0.001, guard ≥max(2K,30) obs); `analyze_all` → `benchmark_factor_profile{ticker,name,betas}`; для SPY беты ≈ (1,0,…) — обратная совместимость по построению |
| B1-D 🟠 | /mandate = принудительная 6-вопросная ре-анкета (первопричина UX-инцидента) | Меню точечных правок: бенчмарк за 2 тапа (`edit_mode`-ветвление `ob:*`), классы активов, риск-профиль (бенчмарк юзера НЕ перетирается), ре-анкета как опция; `/help`, `set_my_commands`, хинты; биллинга в mandate-флоу НЕТ (тест `MandateEditNoBillingTest`) |
| B1-E 🟡 | ИИ: «относительно рынка» абстрактно; идеи могли предлагать запрещённые мандатом классы | Промпты называют бенчмарк по имени (`summary.benchmark_profile`), `SYSTEM_PROMPT.md §8`; детерминированный гард `_remove_mandate_banned_picks` вычищает классы с лимитом 0–0 ПОСЛЕ backfill |

> **Тариф (ВАЖНО, 2026-07-17):** цена токена **500 → 2 500 ₸** (пакет 10 =
> 25 000 ₸); источник правды `tg_bot.TOKEN_PRICE_KZT`, копирайт /balance /topup
> /help читает константы; экономика пересчитана (`ECONOMICS.md`: маржа ~98%,
> безубыточность ~2 платящих юзера; риск фрода welcome-бонуса ↑ — в P0).

---

## −23. Бенчмарк отчёта следует выбору пользователя + оценка готовности + IBKR-роадмап (2026-07-16, раунд 23)

> **Контекст:** владелец сменил бенчмарк на NASDAQ, но DEEP-отчёт (и лог
> `Equity curve benchmark = S&P 500`) остался на S&P 500. Разбор: таблица
> бенчмарков честно считала против профиля (`Профильный бенчмарк`), а **линия
> equity-curve** выбирала бенчмарк перебором фиксированного `_BM_CONCRETE_MAP` и
> всегда брала первый доступный тикер — `SPY.US` (всегда есть как рыночный
> фактор). Факторное разложение «Рынок (S&P 500)» — это отдельный рыночный
> фактор модели (по построению S&P 500), НЕ баг. **pytest 688 passed** (+4,
> `tests/test_phase30_benchmark_equity_curve.py`).

| # | Было | Стало |
|---|---|---|
| B-1 🔴 | `compute_equity_curve_series`: при наличии профильного бенчмарка перебирала весь `_BM_CONCRETE_MAP` по порядку словаря → всегда `S&P 500`; выбор NASDAQ игнорировался | `analyze_all` кладёт `results['profile_benchmark_ticker']`; линия рисуется именно по нему (`QQQ.US`→«Nasdaq 100» через `BENCHMARK_LIST`), фолбэк на первый доступный concrete-бенчмарк сохранён; тест доказывает, что линия = QQQ, а НЕ SPY |

> **Пост-ревью фиксы того же раунда (−22, code-review #1…#5):** try/except вокруг
> резолюции источника в `cb_confirm` (утечка single-flight-слота на I/O-сбое vault),
> удаление мёртвых `_build_analysis_payload`/`_fetch_and_analyze_sync` (закрыт S-1),
> ранний выход резолвера без открытия vault на горячем пути freedom, мёртвая
> переменная `charged`, неверный ценник в сообщении протухшего сценарного кэша.

> **Оценка готовности обновлена** (`PRODUCTION_READINESS.md ⟳ 2026-07-16`): 6.9→~7.2/10
> после персистентности (GCS-том, Postgres удалён) + multi-user фиксов; вердикт по
> сценариям без изменений (бета ✅, открытый запуск — P0 платежи/масштаб/алертинг).
> **Спроектирован трек IBKR** (`ROADMAP_IBKR_INTEGRATION.md`): второй брокер через
> абстракцию `BrokerConnector`, read-only паритет с Freedom, OAuth Web API,
> предусловие — второй источник котировок.

---

## −22. Источник портфеля: UPSERT-хранение режима · recovery по ключам · бесплатное демо (2026-07-16, раунд 22) — M-3…M-5

> **Контекст:** доведение фикса раунда −21 до первопричины (ТЗ владельца). Раунд −21 сделал
> потерю режима ВИДИМОЙ; раунд −22 делает её НЕВОЗМОЖНОЙ и убирает оба следствия инцидента:
> «юзер с ключами уводится в демо» и «токен списывается за демо, в которое юзер попал не по
> своей воле». Продуктовые решения владельца (2026-07-16): **демо бесплатно**; **ключи всегда
> побеждают**. **pytest 684 passed, 8 skipped, 1 xfailed** (+13, `tests/test_phase29…` → 17 тестов).

| # | Было | Стало |
|---|---|---|
| M-3 🔴 | `connection_mode` — колонка тяжёлой строки `user_profiles` (NOT NULL score/…): UPDATE-запись без строки профиля терялась; M-1 сделал это видимым (WARNING), но режим всё равно не сохранялся | отдельная таблица **`user_connection`** (PK telegram_id) + **UPSERT** (`ON CONFLICT DO UPDATE`) — запись физически не теряется; `get_connection_mode_explicit` → `'freedom' \| 'template' \| None` (None = «не выбирал»); идемпотентный backfill в `init_db`: legacy-`freedom` переносится, legacy-`template` — НЕТ (это DEFAULT старой колонки, не явный выбор), существующие значения не перезаписываются |
| M-4 🔴 | Даже с валидными ключами в vault юзер уводился в демо, если режим ≠ freedom (потерян/сброшен) | `SecureVault.has_user` (existence-check, БЕЗ расшифровки — переживает ротацию мастер-ключа) + `_resolve_portfolio_source`: **ключи всегда → freedom** + само-лечение (`CONN MODE RECOVERED` + пере-persist); vault пуст при режиме freedom → re-link сообщение (admin — сервисные ключи), НЕ тихое демо |
| M-5 🔴 | Токен списывался за демо-отчёт в обоих случаях — и за осознанный выбор, и за «свалились в демо из-за бага» | «демо по умолчанию» исключено: `undetermined` (нет ни выбора, ни ключей) → отчёт НЕ строится, «Источник портфеля не выбран», токен не списан; **явное демо** (клик «Демо-режим») → **бесплатно** (`_effective_cost`=0: deduct и баланс-гейт пропускаются, работает и при 0 токенов) + пометка «ДЕМО-портфель, отчёт бесплатный» в активации/превью/финале; сценарный отчёт из демо-кэша тоже бесплатен (маркер `_demo_portfolio` в кэше) |

> Семантика денежных операций не тронута (`deduct_tokens`/charge-only-post-report); freedom-юзеры
> с ключами — поведение без изменений (проверено полной сюитой). Ключи нигде не логируются;
> `has_user` не расшифровывает. UPSERT/backfill совместимы с SQLite ≥3.24 и Postgres.
>
> **Пост-ревью фиксы (2026-07-16, xhigh code-review + security-review):** security-уязвимостей
> HIGH/MEDIUM не найдено; применены 5 находок ревью: (1) резолюция источника в `cb_confirm`
> обёрнута try/except с release слота + support-id — I/O-сбой vault/БД больше не запирает юзера
> в «У вас уже выполняется анализ» до рестарта; (2) удалён мёртвый `_build_analysis_payload` +
> `_fetch_and_analyze_sync` (закрыт и S-1: латентный `{exc}` брокера в мёртвом коде); (3) резолвер
> сперва читает stored-режим и не открывает vault на горячем пути `freedom`-юзеров; (4) удалена
> мёртвая переменная `charged`; (5) из сообщения о протухшем сценарном кэше убран неверный для
> демо ценник «(тоже 1 токен)». Сюита: 684 passed без изменений.

---

## −21. Multi-user: тихий откат в демо + защита сервисного ключа (2026-07-14, раунд 21) — M-1…M-2

> **Контекст:** владелец сообщил «после ввода 2-м пользователем Freedom API проект возвращает
> демо-портфель». Разбор прод-логов Cloud Run за 14.07 (893 записи, все уровни; workflow
> `Cloud Run Logs Check`): user 88202680 (не-админ) в 13:32 получил `Демо-режим: используется
> шаблонный портфель` → `MAC3 ▸ Portfolio source: DEMO (template)`, тянул историю по 16 шаблонным
> тикерам; админ 148046720 в 08:34/11:50 — успешные live-запросы (`Загружено 15 позиций`). Демо
> рождается ТОЛЬКО при `api_key=="demo"`, что бывает лишь когда `connection_mode != "freedom"`.
> Живой путь `cb_confirm` брал эту ветку **без единого лога** (логирование `KEY SOURCE` жило в
> мёртвой `_build_analysis_payload`) → диагностировать было нечем. **pytest 671 passed, 8 skipped,
> 1 xfailed** (+4, `tests/test_phase29_multiuser_connection.py`).

| # | Было | Стало |
|---|---|---|
| M-1 🔴 | `save_connection_mode` — голый `UPDATE … WHERE telegram_id` игнорировал результат: если строки профиля нет, запись «freedom» терялась ТИХО, `get_connection_mode` → дефолт `template`, юзер (уверенный, что привязал брокер) молча получал демо | проверка `rowcount`: 0 строк → `return False` + WARNING (потеря видима, не маскируется под демо); штатный поток (строка создаётся на mandate-approve до привязки) не меняется |
| M-2 🔴 | `cb_confirm`: (а) решение об источнике портфеля без логов; (б) `keys is None` при `freedom` → подстановка **общего сервисного `FREEDOM_API_KEY`** любому юзеру → риск показать чужой (сервисный) счёт | (а) `PORTFOLIO SOURCE`/`KEY SOURCE` логируются во всех ветках; (б) сервисный ключ подставляется **только админу** (`_is_admin`/`ADMIN_USER_IDS`), обычному юзеру — «Брокер не подключён, привяжите заново» (слот освобождён, токен НЕ списан) |

> **Проверено и НЕ является проблемой (изоляция состояния корректна):** брокер-клиент/сессия
> создаются per-call с ключами конкретного юзера; vault — строки по `user_id`; отчёты пишутся
> `<user_id>_<дата>_<tier>.html` (без коллизий); FSM — `MemoryStorage` с ключом (chat,user);
> concurrency-слот `_IN_FLIGHT_USERS` — per-user. `--max-instances=1` уже в `cloudbuild.yaml`
> (`TelegramConflictError` в логах — транзиент при перекате ревизий, не баг кода).
>
> **Открытые (не multi-user, вынесено в рекомендации):** chromadb-posthog телеметрия (23×ERROR-шум),
> RAG boot-ingest пропущен (`numpy._typing` circular import), FRED `DEXKZUS` 400 → `KSPI.KZ` выброшен,
> `BTC-USD.US` не резолвится в Tradernet.

---

## −19. Post-release hotfix сломанного DEEP-отчёта (2026-07-11, раунд 19) — F-14…F-17

> **Контекст:** живой DEEP от 10.07 (первый после мержа Спринта-1, PR #103) вышел с дефектами:
> обложка «ожид. дох. 218.4% · Sharpe 11.29», строки «12М +0.0%», мусор в RAG-выдержках.
> Мат-фиксы Спринта-1 корректны — честное окно F-6 (без bfill) вскрыло три скрытых дефекта
> потребителей ниже по конвейеру; четвёртый (RAG) независим. Детальный разбор:
> `docs/AUDIT_360_2026-07-10.md` ЧАСТЬ 6. **pytest 626 passed, 8 skipped** (+14,
> `tests/test_phase26_report_fixes.py`).

| # | Было | Стало |
|---|---|---|
| F-14 | Форвардная E[r] аннуализировала Ridge-альфу молодого 2x-ETF (окно ≈120 дн) → «218.4% · Sharpe 11.29» | форвард = **β·μ без альфы** (реализованный дрейф остаётся в `Alpha_Specific`); per-asset кламп [−50%,+100%] + QC-счётчик; панель `Expected_Return_Annual`/`Expected_Sharpe` гейтится на окно регрессии ≥252 дней (телеметрия `expected_return_window_days`; короткое окно → «—») |
| F-15 | `build_portfolio_log_returns` row-dropna обрезал 5-летнюю серию до лайфспана молодого листинга → 12М/6М = None, плоская первая половина equity-curve | маскированная композитная серия: по-дневная ренормализация весов реально торговавшихся имён (composite-backfill, без синтетики и look-ahead), день входит при покрытии ≥50% kept-веса |
| F-16 | premium-mapper коэрсил `portfolio_num=None` → 0.0 → «12М +0.0% · +0.0пп vs S&P» + фантомная плоская кривая | строки без данных пропускаются; summary откатывается на самое длинное реальное окно |
| F-17 | RAG-выдержки: «Morgan Putting…» (обрывок шапки), «mod- estly» (PDF-перенос), «12% 10% 8% 6% 4% 2%…» (оси графика) | срез обрывка имени банка (possessive-хвосты: «Morgan Stanley expects…» как подлежащее НЕ режется), склейка переносов, отсечение осевых рядов, prose-gate <55% словесных токенов |

> **Проверено по скриншотам и НЕ является проблемой:** атрибуция банка рядом с RAG-выдержкой
> (берётся из метаданных ретривала, не из текста чанка — механизм корректен); KPI-спарклайны
> «нет истории» (первый отчёт — MoM-снапшотов ещё нет); суммирование факторной дисперсии в 100%.

---

## −20. Композитная база метрик · спарклайны · drag плечевых ETP (2026-07-11, раунд 20) — F-20…F-23

> **Контекст:** DEEP от 11.07 (первый на исправленном коде PR #104) — F-14…F-17 подтверждены
> в проде; ядро математики сошлось (Σ ERC=100%, decomp 35.48−16.64=18.84 ✓, стресс-USD ✓).
> Владелец: «почему графики и ожид. дох./фвд-Sharpe не показываются» + «проанализируй методы
> для новых бумаг (SPCX) и плечевых ETF (CONL)». Разбор: «—» на обложке — осознанный гейт F-14
> (окно регрессии < 252 дн из-за молодых листингов), а «нет истории» в спарклайнах — остаточный
> дефект класса F-15. **pytest 638 passed, 8 skipped** (+12, `tests/test_phase27_composite_metrics.py`).
> Методология: **`docs/METHODOLOGY_SPARSE_AND_LEVERAGED.md`**.

| # | Было | Стало |
|---|---|---|
| F-21 🔴 | Спарклайны KPI: `compute_kpi_trend_series` строил своё dropna-пересечение ВСЕХ позиций — SPCX (<60 дн) обнулял совместное окно → все карточки «нет истории» (до F-6 «работали» на bfill-синтетике) | серия из готового композита `results["port_log_returns"]` (тот же ряд, что кривая/периоды); легаси-фолбэк фильтрует колонки <60 дн |
| F-22 🔴 | Реализованные метрики (Sharpe-числитель, CAGR, VaR/CVaR, MaxDD) — на окне пересечения = возраст самого молодого имени (суб-годовое окно) | маскированный композит полной панели ×Σw (кэш-разбавление сохранено); полные панели — легаси бит-в-бит; телеметрия `realized_window_days`; β/Σ и F-14-гейт не тронуты |
| F-23 🟡 | Форвард β·μ плечевых daily-reset ETP игнорировал variance drag −½k(k−1)σ² → завышение E[r] CONL/XNDU | `apply_leveraged_drag`: реестровые ETP получают + min(α̂,0) (drag из данных, только вниз); env `LEVERAGED_ETP_EXTRA` |
| F-20 🟢 | «SELL SPCX» с qty=null без объяснения (имя вне структурной модели) | строка аннотируется «вне модели: история < 60 торг. дней» (`uncovered` ← `_last_sparse_dropped`) |

> Открытые из раунда: F-18 ($404 vs ≈411 в одной CVaR-карточке — два округления), F-19 (BL-«Ожид.
> доходность» в «Эффекте» под тем же лейблом, что факторная панель обложки; excess-базис).

---

## −18. Ревью-2 прод-отчётов BASE+DEEP (2026-07-09, раунд 18) — 7 замечаний владельца

> **Контекст:** второй проход по живым BASE+DEEP (после раунда 17). Семь замечаний закрыты;
> **pytest 598 passed, 8 skipped**; бандлы пересобраны; смоук-рендер 3 тиров зелёный.

| # | Было | Стало |
|---|---|---|
| R2#1 | ИИ писал «почти 8 из каждых 10» — `currency_rule` давал этот пример-фразу, модель копировала дословно | пример убран; правило описано абстрактно (доли — в процентах, БЕЗ «N из каждых 10») |
| R2#2 | DEEP «Частичное подтверждение ИИ»: нота «Макро-сигналы FRED согласуются» — непонятно КАКИЕ сигналы | `_build_regime_consistency` теперь НАЗЫВАЕТ проверенные сигналы + значения (кривая 10Y−2Y, HY-спред, VIX, инфляц. ожидания) |
| R2#3 | «Из банковской аналитики · RAG»: выдержки обрывались («ury…», «…mod-»), все из ОДНОГО JPMorgan | `_clean_rag_excerpt` — цельный блок чанка, обрезка по границе слова + «…»; n_results=6 с приоритетом РАЗНЫХ банков |
| R2#4 | DEEP: «Применить идею» в шапке Action Plan; bot-ссылка вела на несуществующий `RampBot` | кнопка перенесена в баннер «AI Ideas · 4 идеи»; `BOT_USERNAME` default → **`KEN_investment_bot`** (BASE+DEEP + docstring + JSX-фолбэки) |
| R2#5 | «Δ по идеям: MSFT · ORCL · …» — захардкожено в `EffectGrid`, непонятно | DATA-DRIVEN «Меняем позиции: …» из `expected_effect.high_priority_tickers` (`effectScope`/`effectScoped`) — позиции Action Plan, по которым посчитан «до/после» |
| R2#6 | CoVe «Bank RAG · база: 48 отчётов» при реальных 29 | pre-fix `tmpXXXX.pdf`-имена задваивали счёт; `list_documents()` их не считает (`_is_temp_source`), boot-ingest чистит (`purge_temp_sources`), счётчик из реальных отчётов |
| R2#7 | BASE «Открыть бумагу» — ссылки не работали | кнопка УДАЛЕНА из раскрытой строки holdings целиком |

---

## −17. Ревью прод-отчётов BASE+DEEP (2026-07-09, раунд 17) — 6 замечаний владельца

> **Контекст:** ревью живых BASE+DEEP от 09.07. Шесть замечаний закрыты; **pytest 594 passed,
> 8 skipped**; бандлы пересобраны (`build.sh`), смоук-рендер 3 тиров зелёный.

| # | Было | Стало |
|---|---|---|
| 1 | ИИ-комментарий BASE писал «8 из каждых 10 **рублей**» на USD-портфеле (промпт содержал «на каждый **рубль** риска» → учил модель валюте); модель BASE = Sonnet 4.6 | нейтрализовано на «единицу риска» + общий `currency_rule` в ОБА промпта (запрещает выдумывать валюту, доли — без валюты). **BASE → `claude-sonnet-5`** (owner-approved); Sonnet 5 добавлен в `_TEMPERATURE_UNSUPPORTED_PREFIXES` (иначе `temperature=0.7` → HTTP 400) |
| 2 | Кнопка «Открыть бумагу» (BASE holdings) — мёртвая (без onClick) | `<a>` на страницу бумаги TradingView (`securityUrl` срезает биржевой суффикс Tradernet, хранит внутрисимвольные точки), новая вкладка |
| 3 | Кнопка «Применить идею» — мёртвая (BASE); в DEEP её не было | `ApplyIdeaModal` (BASE+DEEP): 4 идеи → выбор → «Scenario Analysis для идеи «…» — спишется 1 токен» ДА/НЕТ. **ДА** → deep-link `t.me/<bot>?start=scn_<n>`; `cmd_start` ловит `scn_` → сценарный tier (`kb_confirm`→`cb_confirm`); токен списывается в боте, не в статичном HTML |
| 4 | `Bank RAG · база: 20 отчётов` не обновилась после 8 новых PDF | корень — Cloud-Function segfault (§ INFRA_NETWORKING, память 1→4Gi, закрыт ранее). Счётчик читается вживую (`collection.count()`); `_download_chroma_db` зеркалит STORE на каждом буте. **boot-ingest теперь инкрементальный** — ингестит INBOX-PDF, которых НЕТ в store (сравнение по `source`), а не только при пустом store → счётчик восстанавливается на рестарте |
| 5 | DEEP «Частичное подтверждение ИИ»: RAG-выдержки без ссылки на банк; нет чек-пойнта сигналов | `regime_rag_confirm` несёт `{text, bank}` (банк восстановлен из retrieval-хедера, раньше срезался); `ragSignals[]{text,bank,ok}` → бейдж банка + ✓/⚠ (⚠ при `stance=diverges`) в `deep-stress-regime.jsx` |
| 6 | CoVe — 24 строки, список визуально перегружен | `data_lineage.build_lineage` **24 → 16**: (Риск-метрики + Euler-TRC/MCTR)→1, (SEC Z-scores + Altman/Piotroski/Coverage)→1, (6 FRED-серий)→1 агрегат (худший статус + note), (2 LLM-чекера)→1 |

> **Сопутствующий фикс:** `build.sh` Tailwind-шаг запускался из `design/premium_v2/`, но `content`-глоб
> в `tailwind.config.js` — корне-относительный (`./design/**/*.jsx`) → сканировал 0 файлов и выдавал
> пустой reset-CSS. Теперь шаг идёт из корня репо (subshell) — CSS снова полный (30 КБ).

---

## −16. Финальный 360-аудит live BASE+DEEP + фиксы секции «Режим» R-1…R-9 (2026-07-05, раунд 16) — Lead UI/UX / Quant / AI

> **Контекст:** исчерпывающий аудит по свежим live-отчётам BASE 05.07 07:39 (`window.PORTFOLIO`)
> и DEEP 04.07 18:03 (`window.DEEP`); Playwright/Chromium мобильная верификация 390/360px.
> **pytest 552 passed, 12 skipped.**

### Математика (сверено скриптом на live-данных) — ВСЁ СХОДИТСЯ
- **DEEP:** Σвес=100.00% · Σrisk(TRC)=100.00% · waterfall 33.98+(−13.23)==20.75 (точно) ·
  факторные доли 82.1+17.9≈100 · 4-Pillar max|Δ|=0.000 · effect-сетка after−before==Δ (8 карт) ·
  режим==квадрант(g,c) (Expansion, g=+0.063 c=+0.024, conf 29% — инфляционная надбавка §−15 живёт) ·
  stress sign(Δ%)==sign(Δ$) · leverage-правило: маржа 5.3% видима ТОЛЬКО из-за leveraged=True.
- **BASE:** Σвес=100.00% · waterfall идентичен DEEP · performance excess=p−s по всем 5 окнам ·
  маржа-чип по правилу. Base Currency: CoVe «Валютный слой» = USD, RFR 4.50%, одна валюта ✓.
- **AI==цифрам:** CVaR-комментарий цитирует −3.15% при KPI −3.2% (одно число, округление);
  regimeAI цитирует conf и реальные RAG-имена (ANET/CLS/CIEN — из наполненной базы!); ВСЕ
  комментарии завершены (grace §−15 работает: factorAI 724, effectAI 300, без «…»).
- **🎉 RAG ЖИВ в проде:** DEEP 18:03 — `✓ Bank RAG · прочитано 87 отрывков · база: 20 отчётов ·
  2106 чанков`; «ИИ-цитирование» → `ok · база RAG активна`.

### Было / Стало — секция «Режим» (закрыты ВСЕ дефекты R-1…R-9 из docs/REGIME_SECTION_DEEP §6)

| # | Было | Стало |
|---|---|---|
| R-1 | Подписи RECOVERY/SLOWDOWN на квадранте ПЕРЕПУТАНЫ vs `finance/regime.py` (оба чарта: премиум JSX и v3 SVG) — в не-Expansion фазе точка попадала бы в чужой квадрант | подписи синхронизированы с движком (top-left=SLOWDOWN, bottom-right=RECOVERY) + заливки следуют за подписями + тест-пин на JSX/бандл/v3 |
| R-2 | хардкод «as_of 2026-06-22» в компоненте | `regime.driversAsOf` из `macro_drivers.as_of` |
| R-3 | баннер рендерил сырой токен `stance` («diverges») | абзац = `rc.summary`; `confirmStance` красит заголовок: confirms ✓ сейдж / partial ⚠ золото / diverges ⚠ ржавый |
| R-4 | хардкод «Рост»-чипа, `+`-знака, сейдж-цвета и подписей осей | всё от данных: чип по фазе, `%+.2f`, цвет/подпись по знаку |
| R-5 | «N подтверждающих» считал и контр-сигналы | только ✓-буллеты |
| R-6 | чипы «RAG ·» показывали ETF-моментум; реальные `regime_rag_confirm` — dead key | чипы из настоящих банк-выдержек (метка «RAG»), фолбэк на моментум с честной меткой «Моментум» (`ragBacked`) |
| R-7 | «Expansion · Expansion» (нет `label_ru`) | `pdf_payload.regime_block.label_ru` (Экспансия/Восстановление/Замедление/Рецессия) |
| R-8 | детерминированная сверка R3 (`regime_consistency`) видна только в v3 | замаппена в премиум (`regime.consistency`), строка «✓/⚠» под буллетами |
| R-9 | пилюли статусов драйверов — сырые токены `ok`/`stale` | RU: «актуально»/«устарело»/«частично»/«нет данных» |

### Мобильная верификация (Playwright/Chromium, headless)
- **0px горизонтального оверфлоу** на 390px И 360px: live DEEP 18:03, live BASE 07-05,
  локальные рендеры deep/base с ПЕРЕСОБРАННЫМ бандлом. React монтируется, JS-ошибок нет.
- Скриншот секции «Режим» @390px: исправленный квадрант, data-driven чипы/подписи — эталон ОК.
- CSS-переделка НЕ потребовалась: существующая сетка (mob-scroll-x, media-запросы §−4.5/−9)
  держит вёрстку; принцип Do-No-Harm — не переписывать работающее.

### Инфраструктурная находка (задокументировано)
`build.sh` запускает Tailwind из `design/premium_v2`, а `tailwind.config.js` содержит глобы от
КОРНЯ репо (`./design/**`) → CSS собирается ПУСТЫМ (4.6 КБ reset). Правильный порядок: Tailwind —
из корня репо (см. CLAUDE.md); бандлы babel — где угодно. Пересборка выполнена корректно,
`report.compiled.css` байт-идентичен, изменился только `deep-components.js`.

---

## −15. RAG-наблюдаемость · инфляция в regime · un-cut ИИ · миграция бакетов (2026-07-04, раунд 15) — Lead Quant / DevSecOps

> **Контекст:** серия live-верификаций DEEP U148046720 (2026-07-03/04). **pytest 544 passed, 10 skipped**
> (+17 к базе −14). Ветка `claude/bank-rag-deep-analysis-cf106s` (PR #82…#85 в `main`).

### Было / Стало

| # | Узел | Было | Стало |
|---|---|---|---|
| RAG-1 | **Миграция бакетов GCS** | `ramp-bot-ingest` / `ramp-bot-chroma-db` (+рассинхрон код↔доки: docs звали inbox `ramp-bot-chroma-db-inbox`) | Единые глобально-уникальные namespace: INBOX `ramp-bot-chroma-db-inbox-investadv`, STORE `ramp-bot-chroma-db-investadv`. Правки: `entrypoint`, `cloud_function/main`, `cloudbuild`, `Dockerfile`, `ingest_bank_report`, `RAG_INGESTION`. Только строки-дефолты — математика/логика не тронуты. |
| RAG-2 | **«RAG не читает отчёты?»** — нет наблюдаемости | строка CoVe = бинарный `used_rag`; счётчик отрывков жил только в `detail` (премиум-маппер его отбрасывал) | `_fetch_rag_context` отдаёт инвентарь базы (`docs/chunks`); строка Bank RAG = 3-state + «прочитано N отрывков · база M отчётов/K чанков»; **новая CoVe-строка `ИИ-цитирование банк-аналитики`** (`_rag_citation_status`) + integrity-пилл `ИИ↔банк-аналитика`. |
| RAG-3 | **ИИ цитирует `[GS][Barclays][JPM]` при пустом RAG** | воспринималось как баг | **By design:** системный промпт (стр. 1086–1088) и спека `ai_regime_comment` (1018–1020) ЯВНО велят давать банк-консенсус из знаний модели «даже без RAG». Голые банк-теги ≠ `[RAG:файл]` (последние гейтит `_strip_unverified_rag_citations`). Теперь ЧЕСТНО флагается: `⚠ из общих знаний модели, не подтверждено отчётами`. |
| REG-1 | **Инфляция в regime-overlay** (BLOCK 3.4) | overlay = GDP (→growth) + безработица (→cycle) | + третья надбавка **10Y breakeven (T10YIE) → growth-ось** (канал политики/ставки): уровень vs таргет 2% ⊕ **темп ре-анкоринга** за ~1 мес, ограничена ±0.05, ключ `macro_inflation_nudge`. Growth-список стал 5-компонентным (доля макро ≤2/5 — tilt, не override). Отсутствующая серия → нет надбавки. `_macro_nudges`→4-кортеж. |
| UI-1 | **ИИ обрывает мысль** («…это компромисс, а…») | потолки `_soft_trim` впритык к промпту → обрезка на каждом прогоне (factor 640, regime 250, effect 250) | потолки подняты (factor 900, regime 400, effect 400 для DEEP) + `_soft_trim` grace: предложение, пересекающее лимит, завершается целиком в пределах +25% (40–120 симв.); вердикт/саммари сохраняют жёсткий кап (`allow_grace=False`). |
| UI-2 | **Двойной символ в футере** | «Контроль качества» хардкодил зелёную `✓` → рендерилось `✓ ✗ RAG` (зелёная на ошибке) | футер красит СОБСТВЕННЫЙ символ строки (✓ sage / ✗ rust / ⚠ gold / — neutral) — правки в `deep-app.jsx` + скомпилированном `deep-components.js`. |

### Диагностика (без правок кода)
- **`docs/RAG_TROUBLESHOOTING.md`** (новый) — 8-шаговый runbook «почему Bank RAG пуст, хотя PDF в GCS»:
  сверка имён бакетов (INBOX vs STORE), деплой/триггер функции (fail-soft в cloudbuild проглатывает 403),
  логи функции (`[Ingest] Added N chunks`), бут-синк бота (только на буте!), env-дрейф, быстрый CLI-обход.
- **`docs/BANK_RAG_COVE_DIAGNOSIS.md`** — рекомендации P1/D-4/D-5 из раунда закрыты (см. статус в самом файле).

### RAG-4: STORE пуст, хотя PDF в INBOX и триггер настроен вручную — **надёжный fix**
> Прод: бакеты в `europe-west3`, STORE `ramp-bot-chroma-db-investadv` ПУСТ несмотря на ручной триггер.
> Диагноз: Cloud Function → Eventarc — хрупкая цепочка (регион триггера ≠ регион бакета; embedding-модель
> не «запечена» в функции и качается в рантайме; `cloud_function/rag_engine.py` был устаревшей копией
> src на 112 строк).

| Узел | Было | Стало |
|---|---|---|
| **Boot-ingest бота (путь B)** | RAG зависел ТОЛЬКО от Cloud Function/Eventarc | `entrypoint._boot_ingest_from_inbox` (daemon-поток, не блокирует health-probe): если STORE пуст, а в INBOX есть PDF — бот ингестит **в контейнере** (там deps + pre-baked ONNX-модель + запиненный chromadb) и публикует базу в STORE. Обходит функцию/регион/скачивание модели. Гейт `RAG_BOOT_INGEST=0`, бакет `RAG_INBOX_BUCKET`. |
| **Дрейф движка функции** | `cloud_function/rag_engine.py` (29-06, 290 стр.) без секционного чанкинга/метаданных | синхронизирован с `src/agent/rag_engine.py` (402 стр., IDENTICAL). |
| **Дрейф версии chromadb** | функция `chromadb>=0.5.0` (плавающий) vs бот `==0.6.3` | функция запинена `chromadb==0.6.3` (одинаковая схема записи/чтения). |
| **Регион (docs)** | не отмечено | `RAG_TROUBLESHOOTING §3`: Eventarc-триггер обязан быть в регионе бакета (europe-west3), иначе события не доходят; путь B региону безразличен. |

Тесты: +7 (`test_phase22_rag_boot`: env-гейт, skip при непустой базе, ингест при пустой, no-raise).

---

## −14. Исполнение Roadmap 360-аудита: спринты A/B/C (2026-07-02, раунд 14) — CTO / Lead Quant

> **Контекст:** исполнение Action Plan из повторного 360-аудита с предварительным ре-анализом каждого
> пункта. **pytest 511 passed, 10 skipped** (+6). По ходу найден и исправлен скрытый блокер CI.

### 🔴 Бонус-находка: CI-workflow был НЕВАЛИДНЫМ YAML
`python-ci.yml` был целиком обёрнут в кавычки «…» (строки 1/67) → GitHub **молча не запускал workflow
вообще** — PR-гейт, объявленный в F-9, фактически не существовал. Исправлено вместе с B-3.

### Sprint A — контракты данных
| # | Что | Решение |
|---|---|---|
| A-1 | **Числовые двойники** | `pdf_payload` эмитит `*_num` рядом с каждой форматированной цифрой, которую читает премиум-маппер: `assets[]` (pnl_pct/pnl_abs/beta/atr), `action_plan[].price_num`, top-level (cvar/sharpe/sortino/var/mdd/volatility), `period_returns` (portfolio/benchmark/excess). Маппер предпочитает `*_num`, string-parse — legacy-fallback. Регресс-тест: строки НАМЕРЕННО противоречат числам → маппер берёт числа; `None`-период → twin `None`, не 0. |
| A-2 | **Источник Δw в Action Plan** | Строка Δ веса помечена «BL …» (Black-Litterman) с тултипом; при противоречии знака чипу действия (TRIM + BL+0.3пп) — нейтральный ink-тон вместо зелёного. Данные двух движков честно различимы. |

### Sprint B — воспроизводимость / операционка
| # | Что | Решение |
|---|---|---|
| B-3 | **Hash-lock** | `requirements.lock` (2.7k строк, `pip-compile --generate-hashes`, linux/py3.11 == база прод-образа); Dockerfile: `--require-hashes`; CI: тесты ставятся из лока, новый job `lock-consistency` (интент ↔ лок), **pip-audit переведён в blocking** с фильтром принятого класса. Капп `cryptography<46`→`<49` (старый капп ПИНИЛ уязвимую 45.x; лок → 48.0.1, PYSEC/CVE закрыты). **Принятый риск (задокументирован):** aiohttp 3.9.5 (31 CVE) зажат `aiogram==3.7.0` — фреймворк бота требует staged-валидации, не слепого бампа; aiohttp здесь — только исходящий long-polling. Гейт фейлит ЛЮБУЮ уязвимость вне этого класса (поштучные ignore отвергнуты — whack-a-mole). |
| B-4 | Наполнить RAG | **Операторский шаг** (нужны PDF + GCS-креды): `python scripts/ingest_bank_report.py reports/*.pdf --upload` (см. RAG_INGESTION.md). Из кода невыполнимо. |
| B-5 | **Freshness-шум FRED** | Корень найден: freshness меряется от даты НАБЛЮДЕНИЯ, а FRED штампует медленные серии НАЧАЛОМ периода (май-UNRATE=01.05 публикуется ~5 июня → «свежайший» принт уже 35–65 дн; Q1-GDP third estimate → 150–180 дн). Окна 45/120 флагали актуальные серии stale бОльшую часть цикла. Стало: UNRATE 75 дн (месяц+лаг+буфер), GDP 210 дн (квартал+3-я оценка+буфер). |

### Sprint C — полировка
| # | Что | Решение |
|---|---|---|
| C-6 | UI-мелочи | Русская плюрализация «1 нарушение / 2 нарушения / 5 нарушений»; футер DEEP заявляет ChromaDB-источник ТОЛЬКО при реально консультированном RAG (`✓` в quality); мёртвые env-ручки `REPORT_DEEP_VERSION`/`REPORT_BASIC_VERSION` удалены (`REPORT_VERSION` жив — гейт v1 в tg_bot). |
| C-7 | Моки из рендерера | 246 строк `_MOCK_*` → `src/report_mocks.py`; re-export в `html_renderer` — API тестов/CLI не изменился. |
| C-8 | Bank-паттерн | `\bms\b` удалён (ловил «MS» в любом тексте); Morgan Stanley — только полное имя или префикс `ms_`. Тесты. |

**Верификация:** live-данные d0702 через новый бандл — плюрализация/BL-метка/условный футер/маржа-бейдж OK,
0 утечек/ошибок; lock-гейт просимулирован (`accepted 31 aiohttp, blocking 0 → GREEN`); YAML валиден.

---

## −13. Финальный 360-аудит live-отчётов + leverage-правило (2026-07-02, раунд 13) — Lead UI/Quant

> **Контекст:** полный аудит живых BASE (07-01) / DEEP (07-02): извлечён `window.PORTFOLIO`/`window.DEEP`,
> сверена ВСЯ математика и AI-тексты против цифр; 3 потерянных параметра восстановлены. **pytest 505 passed, 10 skipped.**

### Математика (сверено на live-данных) — ВСЁ СХОДИТСЯ
- **Waterfall**: 34.06 − 13.13 = 20.93 == Итог (оба отчёта) ✓ · **4-Pillar**: F+V+T+C == total для всех 12 бумаг ✓
- **Факторы**: Δ = port − bench, S&P 500 β_Market=1/стили=0 по построению ✓ · **Σвес=100%, ΣTRC=100%** ✓
- **Top-вкладчики** == top-5 холдингов по TRC ✓ · **Performance**: excess = p − s по всем 5 окнам ✓
- **Base Currency/RFR**: CoVe-чекер «Валютный слой» = USD, RFR 4.5%, «цены и ставка в одной валюте» ✓
- **AI-комментарии == цифрам**: 77% tech, NVDA 27.6% TRC, CVaR 3.14%/$417, Шарп 0.43, conf 37%, плечо 1.05x —
  все числа LLM совпадают с payload; effectVerdict честно объясняет trade-off Max TRC ✓
- `it_share` tone=flat — **осознанный дизайн** (`_NEUTRAL_METRICS` в simulate.py, документировано) — не тронут.

### Было / Стало

| # | Узел | Было | Стало |
|---|---|---|---|
| L1 | **Leverage-правило (§3 ТЗ)** | кэш −5.28% (маржа!), но премиум НИГДЕ не показывал плечо; поля `leveraged`/`margin_debt_pct` терялись в маппере | `_leverage_info`: маржа-чип в BASE-hero + бейдж в DEEP-мандате, рендер **ТОЛЬКО при отрицательном кэше** (fallback по знаку cash-веса); на обычном счёте плечо скрыто. Контракт BASE 11→12 ключей (`leverage`) |
| L2 | **BASE factorPills = [] (потерянный параметр)** | `factor_betas` в tg_bot строится ТОЛЬКО для DEEP → hero-строка пилл в проде ПУСТАЯ | `_base_factor_pills`: fallback синтезирует пиллы из РЕАЛЬНОГО payload — Tech-доля (77%, warn), Hotspots (28% риска), Диверсификация (39% эффект), Beta (1.04) |
| L3 | Отрицательная ширина баров | маржинальный USD (вес −5.3%) давал `width:-31%` в HBar/MiniBar (невалидный CSS) | clamp `Math.max(0,…)` в 3 компонентах |
| L4 | «Перевес IT» (DEEP SectorMix) | хардкод-бейдж, рисовался всегда | условный «Перевес» только при warn-секторе/предупреждении; sectorWarn `'–'` отфильтрован |
| L5 | Чип «0 нарушение» (мандат) | rust-чип даже при 0 нарушений | 0 → sage «соответствует» |
| L6 | Итог декомпозиции BASE «20.88%» | сырой float | `.toFixed(1)` → 20.9% (как в DEEP) |

**Верификация:** live-данные обоих отчётов прогнаны через НОВЫЙ бандл (Playwright/Chromium): 0 JS-ошибок,
0 утечек (`undefined/NaN`), `scrollWidth==390` @390px (нет гор. переполнения), скриншоты desktop+mobile.

---

## −12. Внедрение рекомендаций a/b/c (2026-07-01, раунд 12) — Lead Quant / Systems

> Реализованы все три рекомендации §−11 (4-Pillar momentum, regime-overlay, RAG-ingest) под Do-No-Harm:
> все изменения аддитивны/опциональны, полный прогон после каждого. **pytest 505 passed, 10 skipped** (+12).

### (а) 4-Pillar — фундаментальный моментум в F-pillar
- Обнаружено: **Piotroski-F уже используется в C-pillar (Credit)** — добавлять его в F = двойной счёт. Поэтому
  добавлен **ДРУГОЙ** сигнал: `SEC_Fundamental_Momentum` — тренд опер./чистой маржи YoY (`sec_edgar`, из уже
  тянущихся 2-летних данных, без доп. запросов), ∈[-1,1].
- `fundamentals_score(..., momentum=None)` — **ограниченный бонус ±0.5**; `None` → **байт-идентично** старому
  (обратная совместимость). Wired в `scoring_orchestrator`. Итог F по-прежнему клипуется ±2.
- Тесты (`test_phase21`): None==baseline, знак, границы ±0.5, NaN-guard, клип итога.
- **Forward-оценки (P/E fwd, EPS-рост)** — требуют нового источника (IBES/брокер), оставлено Phase-2 (из SEC нельзя).

### (б) Режим — макро-overlay (валидация)
- Overlay **уже DEFAULT-ON** и проброшен: `RegimeClassifier().classify(all_data, macro=macro_drivers)`. Реализует
  **LEVEL ⊕ темп изменения** (`series_trend`, ≥3 последовательных изменения, МНК-наклон), GDP→growth,
  безработица→cycle, каждый ограничен ±0.05 (TILT, не override) → анти-шум встроен.
- Добавлены валидационные тесты: bullish-макро тянет обе оси вверх и в границах; **растущая** безработица
  опускает cycle сильнее падающей (rate-of-change работает); env-гейт `REGIME_MACRO_OVERLAY` (default-on).
- Классификатор НЕ переписан (не ломаем логику).

### (в) RAG — ingest + чанкинг + админ-загрузка
- **Чанкинг:** был split только по заголовкам (длинные секции → огромные чанки, обрезка эмбеддинга). Стало
  **секция-осведомлённый + ограничение размера** (≤1200 симв., overlap 150), заголовок → метаданные.
- **Метаданные:** `bank` (детект по имени/обложке), `section`, `tickers` (обёрнутая строка `,AAPL,` для точного
  фильтра). Retrieval: мягкий фильтр по тикеру (`get_market_sentiment(query, ticker=…)`), контекст показывает банк+секцию.
- **Админ-загрузка:** `scripts/ingest_bank_report.py` (ингест локально + `--upload` в GCS + `--list`).
- **Робастность:** `chromadb` импорт сделан ленивым (как `pymupdf4llm`) — модуль/чистые хелперы работают без dep.
- **Инструкция:** `RAG_INGESTION.md` — пошагово (GCS-триггер vs админ-CLI, конвенция имён, проверка, влияние на подбор).
- Тесты: чанкинг (sub-split длинной секции, drop коротких), детект банка, извлечение тикеров.

---

## −11. Прод-отчёты 2026-07-01: фильтры, чекеры, режим, 4-Pillar, RAG (раунд 11) — Lead Quant / UI

> **Контекст:** проверка живых BASE/DEEP (U148046720, 2026-07-01, извлечён `window.PORTFOLIO`/`window.DEEP`).
> Один баг исправлен в коде; остальное — аудит логики + выводы/рекомендации (запрос был «проанализируй»).
> **pytest 493 passed, 10 skipped.**

### Исправлено (код)
| # | Узел | Было | Стало |
|---|---|---|---|
| F1 | **Фильтры «Что вы держите» (BASE)** | «Технологии»→`h.cls==='Технологии'`, но в проде `cls`=КЛАСС актива («Акции США»), не сектор → **«Ничего не подходит»**; «Защитные» так же ловило только облигации | Проброшен per-asset `sector` (`pdf_payload`←`Fundamental_Sector` → оба маппера), матчеры `_isTech`/`_isDefensive` читают `sector` ⊕ fallback на `cls`, толерантны к EN/RU. Проверено: Технологии→AAPL/NVDA, Защитные→bonds/gold/cash |

### Аудит чекеров (Item 1) — «Источник данных» / CoVe
- **23 чекера в DEEP** (`cove`) — **корректны и честны**: Quant/Euler/факторная коллинеарность (κ=9.06, orthogonalized), цены (Tradernet), валютный слой (USD, RFR 4.5%), SEC Z-scores, Altman/Piotroski/IntCov, CDS (10/11, 1 gated), action-levels, BL (τ=0.05), режим, стресс (7, 2 proxy), 6×FRED, Smart-Money (OFF), Bank RAG (**not available**), AI-вердикт, 2×LLM-чекера (CoVe).
- **Честность подтверждена:** `quality` показывает `✗ RAG: банк. отчёты` и `warn` на Smart-Money/CDS/стресс/stale-макро — не приукрашивает.
- **Мелочь (не баг):** футер «Источники» статически перечисляет `ChromaDB (GS/MS/JPM)` даже когда RAG off — стоит делать условным (рекомендация, не блокер).
- **stale-warn на Unemployment/GDP** — ложноватый шум: месячные/квартальные серии «устаревают» против дневного freshness-гейта. Рекомендация — freshness-порог по частоте серии.

### Режим (Item 2) — вывод
- **Логика верна:** детерминированная 2-осевая модель Growth×Cycle из моментума ETF (SPY/IEF, cyclicals/defensives), 4 квадранта, confidence = |вектор|. Прод: Expansion, **confidence 42% (низкая → сигнал слабый, честно)**.
- **Не все «драйверы» участвуют:** 4 дневные FRED-серии (кривая, HY OAS, VIX, breakeven) **только ОТОБРАЖАЮТСЯ** (контекст), в классификацию НЕ входят. В классификацию могут входить только GDP + Unemployment — через overlay `REGIME_MACRO_OVERLAY` (**OFF by default**).
- **Overlay уже реализует то, что просил пользователь:** LEVEL ⊕ **темп изменения** (≥3 последовательных изменения), ограничен ±0.05 (TILT, не override) — анти-шум встроен.
- **Рекомендация:** (а) включить `REGIME_MACRO_OVERLAY=1` после валидации (даёт rate-of-change из GDP/безработицы, шумо-защищён); (б) в UI пометить дневные FRED как «макро-контекст/подтверждение», а не вход классификации; (в) freshness-гейт по частоте серии. НЕ переписывать классификатор.

### 4-Pillar (Item 3) — вывод
- **Логика качественная:** institutional 4-factor (F Quality / V Value / T Technical / C Credit), robust MAD-z, **секторно-нормирована** (не относительно своего же портфеля), macro-alignment ±0.5, каждый компонент клипован ±1.
- **Rate-of-change уже есть:** F включает `Revenue_Growth_YoY` (темп), T — трендовый моментум (SMA/RSI/MACD).
- **Ключевая упущенная возможность:** **Piotroski-F считается** (см. чекер), но **НЕ входит** в `fundamentals_score`. Piotroski-F = 9 бинарных сигналов УЛУЧШЕНИЯ фундаментала YoY (ROA↑, CFO↑, leverage↓, маржа↑…) — это ровно «темп изменения параметров», который спрашивает пользователь.
- **Рекомендация (валидировать до внедрения — меняет Buy/Sell):** (а) добавить Piotroski-F в F-pillar как ограниченный бонус (±0.5, как macro-alignment); (б) добавить ускорение маржи/ROE из 2-летних SEC-данных (уже тянутся `n=2`); (в) **прогнозные значения** (forward P/E, EPS-рост) требуют НОВОГО источника (IBES/брокер-консенсус) — Phase-2, из SEC невозможно.

### RAG (Item 4) — как настроить (архитектура уже есть)
Пайплайн есть: **PDF в GCS-бакет → Cloud Function (`cloud_function/main.py`+`rag_engine.py`, pymupdf4llm) → ChromaDB → синк в GCS (`ramp-bot-chroma-db/chroma_db/`) → бот на буте тянет в `/app/data/chroma_db` → `_fetch_rag_context` запрос по косинусу**. Сейчас пусто/выкл (`RAG not available`).
- **Постоянные обновления:** загрузка PDF в бакет `ramp-bot-chroma-db` (или отдельный `-inbox`) авто-триггерит Cloud Function. Для «онлайн быстро»: admin-команда бота / signed-URL upload → триггер; либо Cloud Scheduler + скрапер витрин GS/MS/JPM.
- **Надёжность:** ChromaDB персистится в GCS (переживает рестарт Cloud Run) — как токен-БД. Убедиться: бакет создан, Function задеплоена, embedding-модель задана.
- **Качество чтения:** pymupdf4llm (PDF→markdown) ок; улучшить — **секция-осведомлённый чанкинг** + метаданные (bank/date/ticker/секция) + **freshness-фильтр** (свежие записки выше) + тикер-фильтр при retrieval, чтобы записки реально влияли на выбор активов.

---

## −10. Roadmap-рефакторинг: 3 спринта (2026-06-29, раунд 10) — Lead Quant / Systems Architect

> **Контекст:** исполнение собственного 360-аудита (Roadmap из 11 пунктов, 3 спринта). Принцип **Do No Harm**:
> перед удалением/слиянием — доказательство безопасности. Установлен зелёный baseline **484 passed** → после
> всех изменений **493 passed, 10 skipped** (+9 новых тестов; ядро не тронуто по логике).

### Sprint 1 — Architectural Consolidation

| # | Что | Решение | Обоснование безопасности |
|---|---|---|---|
| 1 | Premium V2 как прод-путь | `PREMIUM_REPORT_ENABLED` default **false→true**; v3 Jinja СОХРАНЁН как авто-fallback (try/except) | **v3 НЕ удалён**: 16 тест-файлов пинят контракт `pdf_payload→v3`, и это сеть безопасности при отсутствии premium-ассета. «Единственный путь» реализован как «default + fallback». |
| 2 | Слияние `premium_payload`+`pdf_payload` | **НЕ выполнено** (физическое слияние) — оставлен чистый 2-стадийный адаптер: `pdf_payload`=compute/format, `premium_payload`=view-map | Слияние вырезало бы наиболее покрытый тестами compute-слой (16 файлов) и увеличило бы связность. Дублирования-вреда нет — это тонкий view-слой. Do-No-Harm: отклонено как net-negative. |
| 3 | Вынести математику из `tg_bot` | Новый `finance/portfolio_series.py` (`compute_kpi_trend_series`, `compute_equity_curve_series`); бот только рендерит SVG | Чистый SoC; H3-инвариант теста усилён (бот делегирует, finance читает `port_log_returns`). |
| 4 | Удалить мёртвые прототипы | `report_basic_v1_design_prototype.html` + `report_deep_v1_design_prototype.html` удалены | 0 ссылок в `src/`+`tests/`. |

### Sprint 2 — Reproducibility & Infra

| # | Что | Решение |
|---|---|---|
| 5 | Пины зависимостей | Добавлены **верхние границы** (major-cap) для numpy/pandas/sklearn и всех либ (`<3.0`/`<2.0`…). `==`+hashes — оставлено CI (`pip-compile` против деплой-образа); ручной lock против локального numpy2/pandas3 отгрузил бы непроверенные версии. |
| 6 | Fail-fast для `DB_PATH` | Уже был raise в проде; усилено — теперь проверяется и **резолвнутый** `DB_PATH.resolve()` (не только env-строка). |
| 7 | Smoke-тест SIGKILL | Новый `test_phase20`: субпроцесс держит hardened-транзакцию, SIGKILL в известной точке → commit **переживает** (durability), un-commit **откатывается** (atomicity). |

### Sprint 3 — Quant-Polish

| # | Что | Решение |
|---|---|---|
| 8 | Док-знаменатель Sharpe + диапазон Credit | Тултип KPI (структурная σ=√(w′Σw), Σ=B·F·Bᵀ+D) + подпись Credit «−2…+1, асимметрично» в PillarLegend. |
| 9 | BL `inv`→`solve` + nearest-PSD | BL переведён на **He-Litterman closed form** (solve k×k, а не inv n×n) — алгебраически идентично, строго стабильнее; `_nearest_psd` (Higham eigen-clip) применён к блендованной F (no-op когда PSD). |
| 10 | Bootstrap seed `hash()`→`hashlib.sha256` | Кросс-интерпретаторно стабильный seed из байтов округлённого вектора. |
| 11 | Векторизация `iterrows` в адаптере | **Отклонено** (обоснованно): N≤50 → выигрыш микросекунды; `itertuples`/`getattr` теряет defensive `row.get(col,default)` (толерантность к отсутствующим колонкам) → корректностный риск в money-path. Аудит сам оценил как LOW-value/MEDIUM-risk. |

**Итог:** 9/11 выполнено; 2 (#2 слияние, #11 iterrows) **осознанно отклонены** с доказательством вреда/бесполезности —
это и есть Do-No-Harm. Логика ядра (ковариация, Euler, Sharpe/Sortino, BL, CVaR, 4-Pillar) не изменена по
результату — только численно стабилизирована (BL/PSD) и сделана детерминированной (seed).

---

## −9. Premium V2 DEEP — гейдж в hero, графики KPI, фундаментал, факторы, план (2026-06-29, раунд 9) — Lead UI/Quant

> **Контекст:** замечания пользователя по DEEP-отчёту (скриншоты list1–5): перенести «Индекс риска», добавить
> графики к KPI, починить/раскрыть фундаментал, объяснить нулевые бета-факторы рынка, раскрыть F·V·T·C и убрать
> дубль ИИ-комментария, добавить колонку «Количество» в Action Plan, общая чистка + мобайл. **pytest 480 passed.**

### Было / Стало

| # | Узел | Было | Стало | Файлы |
|---|---|---|---|---|
| D1 | **«Индекс риска» (DEEP)** | высокая карточка в сетке; справа от заголовка пусто | компактный `HeroGaugeCard` в правой колонке hero; ряд → Verdict(7)+Mandate(5) | `deep-overview.jsx` |
| D2 | **KPI-карты (CVaR/Sharpe/MaxDD)** | пустой бокс «здесь должны быть графики» (`pts:[]`) | крупный тренд 12 мес через `<Sparkline>`; движок отдаёт сырые `*_pts`, фоллбэк на SVG | `deep-overview.jsx`, `tg_bot.py`, `premium_payload.py`, `html_renderer.py` |
| D3 | **Фундаментал-подписи** | «Долг/А», «Маржа», «Рост г/г», «Altman-Z» — непонятно | «Долг/Активы», «Опер. маржа», «Рост выручки», «ATR·день» + тултипы-расшифровки | `deep-holdings.jsx` |
| D4 | **Вывод по фундаменталу** | не было | `fundNote` — правило-ориентированная фраза из метрик SEC (сильные стороны / риски) | `premium_payload.py`, `deep-holdings.jsx` |
| D5 | **Долг/Активы = 0% (ORCL)** | тег `us-gaap:Liabilities` отсутствует у части эмитентов → 0% | фоллбэк `Liabilities = Assets − Equity` (балансовое тождество) | `finance/sec_edgar.py` |
| D6 | **Факторы: «Рынок» = 0** | колонка «Рынок» 0 у всех кроме Market — вопрос пользователя | колонка «S&P 500» + сноска: Market = S&P 500 по построению, сигнал — `Наклон Δ`. Данные корректны | `deep-factors.jsx` |
| D7 | **F·V·T·C нераскрыты** | «F Фундамент · V Оценка…» | полные Fundamentals·Valuation·Technical·Credit + `PillarLegend` с метриками каждого столпа | `deep-factors.jsx` |
| D8 | **Дубль ИИ-комментария (4-Pillar)** | `scoresNote` == `scoresAI` (один `ai_4pillar_comment` дважды) | `scoresNote=""`, рендерится один раз | `premium_payload.py`, `deep-factors.jsx` |
| D9 | **Action Plan: нет количества** | непонятно «сколько продать/сократить» | колонка **«Количество»** — units (`qty_delta`) + Δ веса (`delta_w_pp`, пп), цвет по знаку | `deep-plan.jsx`, `premium_payload.py` |
| D10 | Мобайл (DEEP) | — | hero/`clamp()`/`grid-cols-2`; 4-Pillar и Legend ровно стекаются; нет гор. переполнения @390 | `deep-*.jsx` |

**Логика ИИ-комментариев (Note 1):** структурный дубль был один — `scoresNote` (4-Pillar). Остальные секции
тянут РАЗНЫЕ ключи `ai_*` (risk/holdings/factor/stress/effect/action/regime) — не дублируются. Фундаментал-вывод
`fundNote` детерминирован (правила, не LLM) → не конфликтует с метриками в той же карточке. Факторная «нулевая
бета рынка» — не баг, а свойство модели (Market = S&P 500); снято пояснением, движок не трогали.

> **Деплой:** ветка `claude/base-report-improvements-z9ndut`; пересборка `design/premium_v2/build.sh`
> (Tailwind из КОРНЯ), синк base+deep в `src/premium_assets/`.

---

## −8. Premium V2 BASE — перенос «Индекс риска», периодный график, чистка hero/мобайл (2026-06-29, раунд 8) — Lead UI/Quant

> **Контекст:** замечания пользователя по BASE-отчёту (скриншоты): перенести «Индекс риска» в пустое
> пространство hero; убрать «Прочие метрики» + пустой бокс; выровнять «Декомпозиция риска» и «Структура по
> секторам»; график доходности НЕ обновлялся при смене периода, YTD стоял точкой на оси, конечные значения
> обрезались; общая чистка + мобильная адаптация; сверка математики. **pytest 480 passed, 10 skipped.**

### Было / Стало

| # | Узел | Было | Стало | Файлы |
|---|---|---|---|---|
| R1 | **«Индекс риска»** | отдельная высокая карточка в нижней сетке; справа от заголовка — пустота | компактный `HeroRiskGauge` перенесён в правую колонку hero (под hero-статами), заполняет пустоту | `portfolio-overview.jsx` |
| R2 | «Прочие метрики» + штрих-бокс | декоративная заглушка без данных | удалены | `portfolio-overview.jsx` |
| R3 | Нижняя сетка обзора | 4 равных колонки (с гейджем) | 3 выровненных: hotspot(3)·decomp(5,шире)·sector+AI(4), `items-stretch` | `portfolio-overview.jsx` |
| R4 | `RiskDecomp` / `SectorMix` | разнобой отступов; «Перевес IT» всегда; легенда иконками | единая типографика/«Итог» справа; точки `s.hue`=столбику; бейдж «Перевес» только при `warn` | `portfolio-overview.jsx` |
| R5 | `HeroStat` длинный профиль | «Умеренно-агрессивный» рендерился 5xl-числом → ломал hero | проп `small` → компактный `text-base` с переносом | `portfolio-overview.jsx` |
| **G1** | **График доходности vs период** | брал статичные `months/port/spx` → **НЕ менялся** при смене периода | кривая окна строится ВЛОЖЕНИЕМ точек периодов `cum[старт…−h]=(1+r_окна)/(1+r_h)−1` → реагирует | `portfolio-performance.jsx` |
| G2 | YTD на оси | стоял точкой среди 1М/3М/6М/12М (нелогично) | YTD — самостоятельное окно; ось `старт·−6М·−3М·−1М·сейчас` | `portfolio-performance.jsx`, `portfolio-charts.jsx` |
| G3 | Конечные значения графика | пилюли обрезались у правого края (не видны) | подписи сдвинуты вправо внутрь `padR` + развод port/spx при коллизии | `portfolio-charts.jsx` |
| G4 | Убыточное окно (1М −10.8%) | домен/оси «+t%» без знака, ноль не виден | знако-зависимый домен с базовой 0%, подписи со знаком, линия вниз | `portfolio-charts.jsx` |
| G5 | Порядок периодов | YTD между 3М и 6М | сортировка `1<3<6<12, YTD последним` (селектор+таблица) | `portfolio-performance.jsx` |
| G6 | `undefined%` волатильности | `summary.volPort` отсутствовал в sample | `summary.volPort ?? vol.port`, иначе `—` | `portfolio-performance.jsx` |
| G7 | Маппер `_map_performance` | строил мёртвую кривую `months/port/spx` | только `periods`/`vol`/`summary` (кривую строит компонент); `d=p−s` сверен | `premium_payload.py` |
| MOB | Мобильная адаптация | факторные пиллы налезали; «пп» переносилось | пиллы `grid-cols-2 sm:flex`; `whitespace-nowrap` на «пп»; `clamp()`-заголовки; нет гор. переполнения @390 | `portfolio-overview.jsx`, `portfolio-performance.jsx` |

**Математика (сверено на реальном payload NVDA-портфеля):** конец нести-кривой == headline == строка таблицы во
ВСЕХ окнах (1М/3М/6М/12М/YTD); `excess = p − s` (−8.9/−5.0/−11.6/−6.9/−11.6 пп — совпадает со скриншотом);
водопад сходится `Σstandalone − divers = total`. Вложение точно (без интерполяции): `(1+cum_{−h})·(1+r_h)=1+r_окна`.

> **Деплой:** ветка `claude/base-report-improvements-z9ndut`; прод из `main` — мержить PR'ом. Пересборка
> статики: `design/premium_v2/build.sh` (Tailwind-шаг из КОРНЯ репо), затем синк в `src/premium_assets/`.

---

## −7. Premium V2 — мёртвые кнопки, waterfall-сходимость, пустые боксы (2026-06-28, раунд 7) — Lead UI/Quant

> **Контекст:** замечания пользователя — нерабочие кнопки/сломанная логика графиков, неровный waterfall
> (диверсификация не сходится), пустой бокс «Ожидаемый эффект», ещё template-литералы. **pytest 484 passed.**

### Было / Стало

| # | Узел | Было | Стало | Файлы |
|---|---|---|---|---|
| B1 | **Период-селектор (BASE Performance)** | менял `state`, но НИЧЕГО не читало → чип подсвечивался, числа не двигались | читает выбранный период из `p.periods` → headline/карточки/подсветка строки обновляются (1М −10.8% · 3М +7.8% · 12М +14.3%) | `portfolio-performance.jsx` |
| B2 | Декоративные «↗» кнопки (3 карточки) | `<button>` без onClick — мёртвые | удалены (нет функции, не нужны для масштабирования) | `portfolio-overview.jsx` |
| B3 | Бейдж декомпозиции «14.2%» | ХАРДКОД (template, не совпадал) | реальный `riskDecomp.total` → 18.7% | `portfolio-overview.jsx` |
| W1 | **Waterfall «Риск портфеля»** | 4 бара (Σ≈20) + диверсификация → 7.7 ≠ Итог 18.7 — **не сходился, диверсификация «висела»** | бар **«Прочие» (= sumStandalone − Σпоказанных)** достраивает до 31 → −12.3 → 18.7 Итог — waterfall СХОДИТСЯ | `*-charts.jsx` |
| W2 | Waterfall высота | `maxV` по полному sum → пустота сверху | `maxV` по running-пику бар·1.06 | `*-charts.jsx` |
| I1 | BASE «Ожидаемый эффект» бокс | рендерился ПУСТЫМ (effect/sources = `[]` у всех идей) | бокс/источники рендерятся ТОЛЬКО при наличии контента | `portfolio-ideas.jsx` |
| P2 | EffectGrid «разносторонность» | — (проверено) | вердикт честный «Компромисс: сниж. волат. за счёт роста концентрации»; Max TRC помечен красным — **советы разносторонни** | (verify only) |

**Принцип (подтверждён):** кнопка без функции = убрать (если не нужна для масштабирования); любой
литерал-число в JSX = template-leak. Период-селектор теперь функционален. Waterfall честно сходится:
ΣstandAlone(31.0) + diversification(−12.3) = total(18.7).

> **Деплой:** ветка `claude/stoic-mccarthy-LM18P`; прод из `main` — мержить PR'ом.

---

## −6. Premium V2 — шаблонные данные, цвета, мобильный UX (2026-06-28, раунд 6) — Lead UI/Quant

> **Контекст:** скриншоты пользователя (мобайл) выявили **template-данные вместо реальных**, чёрный
> сектор-график, наложения чисел и неочевидный гор. скролл таблиц. **pytest 482 passed, 10 skipped.**

### Было / Стало

| # | Узел | Было | Стало | Файлы |
|---|---|---|---|---|
| T1 | AI-инсайт (BASE Overview) | ХАРДКОД «AI · HAIKU · Индекс 62 — верх умеренной зоны · 2 бумаги из 9 · RAG: GS_Q2_2026» | реальные `meta.aiModel` + `verdict.riskIndex/riskTier/sub` | `portfolio-overview.jsx` |
| T2 | AI-сводка идей (BASE) | ХАРДКОД «…риск собран в одном углу — 62% в технологиях» | реальный `verdict.headline` | `portfolio-ideas.jsx` |
| T3 | **Performance (BASE)** | ХАРДКОД +14.2% / **+5.1пп vs S&P** / S&P +9.1% / vol 14.8% — **противоречило реальности** (книга ОТСТАЁТ) | реальные: +14.3% / **−6.9пп (Отставание)** / S&P +21.2% / vol 18.7% (`performance.summary`) | `portfolio-performance.jsx`, `premium_payload.py` |
| T4 | Период-таблица + excess | `+{p.toFixed}` → «+-10.8%»; `d` excess = 0; всегда зелёный | знак ±, цвет по знаку; `d = p − s` выводится | `portfolio-performance.jsx`, `premium_payload.py` |
| C1 | **Сектор-график** | `hue=#1c1b1a` у ВСЕХ → весь чёрный, неразличимый | палитра `_sector_hue` (gold/sage/rust/ink + семантика Gold/Silver/Bonds) | `premium_payload.py` |
| C2 | Waterfall цвета | i==1 жёлтый, Итог чёрный — против легенды | отдельно=чёрный, диверсификация=rust, итог=gold | `*-charts.jsx` |
| C3 | Waterfall пустота | `maxV=max(sumStandalone,total)·1.15` → бары на 55% высоты | `maxV` по ВИДИМОМУ пику бар·1.08 | `*-charts.jsx` |
| N1 | top-hotspot P/L | «+-2.7%», «+$-0.05K» (always-+) | знак ±, $ без K на <10k, цвет по знаку | `portfolio-overview.jsx` |
| U1 | **Гор. скролл таблиц неочевиден** | пользователь не знал, что таблицу можно листать | CSS scroll-shadow (Lea Verou, без JS) + чип «↔ листайте таблицу» | `custom.css`, `*.jsx` |
| U2 | Раскрытый фундаментал (holdings) | наследовал `max-content` ширину → пустые ячейки, видно 3 из 6 | `.mob-detail` sticky на ширину вьюпорта → все 6 метрик | `*-holdings.jsx`, `custom.css` |
| U3 | DEEP конвейер идей | 4 узла сжаты/обрезаны на телефоне | стек вертикально `<sm`, стрелки скрыты | `deep-plan.jsx` |
| U4 | Perf badge | «+5.1пп vs S&P» наезжал на легенду | `flex-wrap` шапки графика | `portfolio-performance.jsx` |

**Принцип (закреплён):** премиум-маппер ЧИТАЕТ payload; компоненты НЕ хранят данных. Любой литерал-число/текст
в JSX-компоненте = баг (template-leak). Математика проверена реальной: книга действительно отстаёт от S&P
(+14.3% vs +21.2% = −6.9пп) — отчёт теперь честен и совпадает с AI-вердиктом.

> **Деплой:** ветка `claude/stoic-mccarthy-LM18P`; прод из `main` — мержить PR'ом.

---

## −5. Premium V2 — production rollout + аудит живых отчётов (2026-06-26 → 28) — Lead UI/Quant

> **Контекст:** Premium V2 (React+Tailwind) включён в прод. Серия живых отчётов U148046720
> (`2026-06-26 → 28`) выявила цепочку дефектов **доставки → данных → вёрстки**. **pytest 480 passed, 10 skipped.**

### Было / Стало

| # | Слой | Было | Стало | Файлы |
|---|---|---|---|---|
| D1 | Деплой-флаг | `gcloud run deploy --set-env-vars` ЗАМЕНЯЕТ все env → `PREMIUM_REPORT_ENABLED`, выставленный руками, **стирался** каждым CI-деплоем → прод отдавал v3 | Флаг **запинен** в `--set-env-vars` cloudbuild → переживает редеплой | `cloudbuild.yaml` |
| D2 | Ассеты премиума | Dockerfile COPY `src/` без `design/` → `render_premium` падал `FileNotFoundError` → тихий v3 | Рантайм-ассеты в `src/premium_assets/` (едут с `src/`); фоллбэк → ERROR-лог | `premium_renderer.py`, `build.sh` |
| 3PD1 | SEC EDGAR фундаментал | маппер читал несуществующий `assets[].fundamentals` → пусто у всех 13 позиций | джойн из `fundamental_layer[]` по тикеру → ROE/маржа/долг/рост/Altman-Z | `premium_payload.py` |
| 3PD2 | FRED макро-драйверы | маппер `.items()` по адаптированной панели → `[]` всегда | чтение `macro_drivers.series[]` → 6 чипов (level ⊕ темп) | `premium_payload.py` |
| M1 | Мандат-панель | ключи `target_vol`/`value`/`state` (нет таких) → `–`/0.0 | `target_vol_pct`/`actual`/`status` + `breaches` | `premium_payload.py` |
| M2 | «Ожид. эффект» | сырые float `0.1869…` | формат per-metric → `18.7%`/`0.34`/`49`/`−2.6 пп` | `premium_payload.py` |
| M3 | Конвейер идей | утечка Python-кортежа `('RAG','…')` | `_pipe_step` → деталь-строка (метку даёт компонент) | `premium_payload.py` |
| M4 | BASE «AI · Haiku» | захардкоженная строка | `window.PORTFOLIO.meta.aiModel` (Sonnet 4.6) | `portfolio-ideas.jsx` |
| M5 | BASE top-hotspot | `hotspots` (список СТРОК) индексировался как dict → `–`/0 | вывод из актива с макс. Euler-TRC | `premium_payload.py` |
| M6 | Action Plan score/hot | ключи `score_total`/`hotspot` (нет) → 0/false | джойн `score_breakdown.total` + `assets.hotspot` | `premium_payload.py` |
| M7 | Сектор-предупреждения | `str(dict)` → `{'sector': …}` в UI | `_warn_text` → поле `text` | `premium_payload.py` |
| M8 | ИИ-подтверждение режима | сигналы-строки → 6 пустых ⚠ (компонент ждёт `{ok,t}`) | `_signal_obj` парсит `✓/⚠/✗ текст` → `{ok,t}` | `premium_payload.py` |
| U1 | Leverage/долг | строка валидатора «плечо ≈Nx» при cash≥0 | gated на `is_leveraged` (скрыто на немаржинальной книге) | `data_lineage.py` |
| U2 | BLOCK 5 — портфельная E[r] | отсутствовала | `Σwᵢ·E[rᵢ]+cash·rfr` + фвд-Sharpe в gauge + v3 | `investment_logic.py`, шаблоны |
| MOB1 | Мобайл: hero/overflow | гор. скролл 613–701px, заголовок 54–58px обрезан | `@media≤640`: clamp-типографика, contain-bleed, 0 page-scroll | `custom.css` |
| MOB2 | Таблицы (holdings/action/stress) | `minmax(0,fr)` схлопывался → **наложение** колонок | `.mob-scroll-x` (гор. скролл, натуральная ширина) — десктоп без изменений | `*.jsx` + `custom.css` |
| MOB3 | Идея-карточки | белый текст на белом (невидимо) | `TickerCard dark` проп (явные цвета, не arbitrary-variant) | `portfolio-ideas.jsx` |

**Математика (проверено, реальные данные):** Base Currency Approach USD (RFR 4.5%); Sharpe/Sortino/фвд-Sharpe валютно-согласованы;
SEC EDGAR 10-K (AAPL ROE 151.9%), FRED live (VIX 18.9, 10Y−2Y +0.31пп), Tradernet-цены. AI-вывод (BASE «продать MSFT/ORCL/QCOM»)
совпадает с action-plan сигналами (MSFT −2.3/ORCL −5.3/QCOM −2.0 → SELL) — **BLOCK 4 single-source-of-truth держится**. Режим «partial»
логичен: 5/6 сигналов risk-on, но ВВП (темп −2,7) замедляется → приоритет rate-of-change (BLOCK 2).

> **Деплой:** все правки на `claude/stoic-mccarthy-LM18P`; прод деплоится из `main` — мержить ветку PR'ом, отчёты регенерируются после сборки.

---

## −4. Sprint 6 — BLOCK 1–4 аудит/рефакторинг (2026-06-16) — Lead Quant Architect / AI-Engineer

> **Метод:** «проанализируй задание перед выполнением, убери ненужное на данном этапе». 8 задач в 4 блоках,
> хирургические правки + честный прунинг тяжёлого/платного. **pytest 440 passed, 10 skipped.**

### Прунинг-вердикт (что НЕ делалось и почему — «точно не нужно на данном этапе»)

| Запрошено | Решение | Причина |
|---|---|---|
| Model Routing → `claude-3-5-sonnet-latest` / `claude-3-opus-latest` | **Отклонены ID, исполнен ИНТЕНТ** на актуальных `claude-sonnet-4-6` / `claude-opus-4-8` | Это модели 2024 г. — пин был бы **регрессом** ниже текущего Haiku 4.5/Sonnet 4.6 и ДОРОЖЕ |
| Факторы: PCA / ортогонализация | **Диагностика κ + max\|corr\| вместо принудительной PCA** | `Σ=B·F·Bᵀ+D` уже несёт полную ковариацию F → двойного учёта НЕТ; PCA лишь убила бы интерпретируемость именованных факторов |
| Smart Money: политики + госстимулы + live Form-4 парсинг | **Фундамент + дизайн (gated, OFF)**; платное/тяжёлое отложено | Конгресс-сделки/USASpending — платные агрегаторы / крупный ингест; не нужны на данном этапе |
| CPI/PCE YoY, PMI в макро | **UNRATE + Real-GDP добавлены; CPI/PCE YoY и PMI отложены** | На FRED нет чистой одиночной серии «CPI YoY» (только уровни), PMI **снят с публикации**; форвард-инфляция уже покрыта T10YIE |

### Что сделано (Было / Стало)

| # | Блок | Было | Стало | Файлы |
|---|---|---|---|---|
| 1.2 | Model Routing | BASE=Haiku 4.5, DEEP=Sonnet 4.6 | **BASE=Sonnet 4.6, DEEP=Opus 4.8** (env-overridable); гард `_model_supports_temperature` опускает `temperature` на Opus | `ai_narrative.py` |
| 1.1 | Динамика идей | только `temperature=0.5` (исчезает на Opus) | + **директива свежести** в `ideas_rule` (период `YYYY-MM`, запрет «по привычке») — работает и без `temperature` | `ai_narrative.py` |
| 2.3 | Идеи→Action→Эффект | «Ожидаемый эффект» считался на полном BL-векторе | **только высокоприоритетные** (не-deferred Buy/Sell/Trim) action-строки → `high_priority_target_weights()`; UI помечает скоуп (`scoped_to_high_priority`, список тикеров) | `simulate.py`, `investment_logic.py`, `pdf_payload.py` |
| 3.4 | Рыночный режим | 4 FRED-серии; ETF-классификатор | + **UNRATE + Real-GDP (SAAR)** в каталог; **gated overlay** в `regime.classify(macro=…)` (env `REGIME_MACRO_OVERLAY`, OFF по умолч., аддитивно/ограниченно) | `macro_data.py`, `regime.py`, `investment_logic.py` |
| 3.5 | Smart Money | отсутствовал | новый `finance/smart_money.py`: `InsiderSignal`, детерминир. скорер, интерфейс Form-4 (gated), CoVe-хук + бриф прогнозных моделей | `smart_money.py`, `data_lineage.py` |
| 4.6 | Мультиколлинеарность | F не проверялась | **диагностика** κ (condition number) + max\|corr\| факторов, warn-лог при κ>30/\|corr\|>0.95, в `portfolio_metrics.factor_diagnostics` + CoVe-строка | `investment_logic.py`, `data_lineage.py` |
| 4.7 | 4-Pillar | `composite_risk_score`/`fundamentals_score` доверяли входу | **`_finite()`-гард**: NaN/±Inf нейтрализуются в 0 (а не саморандомный min/max и не отравление Total NaN-ом) | `scoring.py` |
| 4.8 | CoVe | 14 строк | + **LLM-чекеры** (галлюцинации, проверка вычислений), **Smart-Money** (gated), **факторная независимость**; убрана мёртвая ссылка на PMI | `data_lineage.py` |

📝 **Остаётся (за флагом / след. фаза):** живой SEC Form-4 провайдер (gated `SMART_MONEY_INSIDERS=1`); включение `REGIME_MACRO_OVERLAY` после валидации на проде; реализованный CPI/PCE YoY как производная серия; ML-тиры инсайдер-сигнала (кросс-секционный ранкер, regime-conditioned).

---

## −4.1. Пост-деплой аудит живых отчётов 06-18/06-19 (Sprint 6) — 4 фикса

> Скачаны прод-отчёты `2026-06-19/base.html` + `2026-06-18/deep.html` (после деплоя −4). Routing
> подтверждён вживую: **BASE = Claude Sonnet, DEEP = claude-opus**. Найдены 2 скрытых дефекта + 1
> аналитическое улучшение + 1 плановая правка.

| ID | Sev | Где | Дефект (живой факт) | Стало |
|---|---|---|---|---|
| **F1** | 🔧 | `ai_narrative._REPORT_TOOL.input_schema` | В BASE карточка **«CVaR 95%» БЕЗ ИИ-комментария** (Sharpe/MDD есть). Причина: `ai_cvar_note` (а также `ai_4pillar/stress/action/effect_comment`) **не объявлены** в `properties` tool-схемы — Sonnet строго следует схеме и опускает их; Opus (DEEP) добавлял из текста промпта, поэтому в DEEP cvar был | Объявлены все 5 недостающих полей → структурный вывод детерминирован для обеих моделей |
| **F2** | 🔧 | `pdf_payload._model_display_name` | DEEP CoVe: **«Anthropic · claude-opus-4-8»** (сырой id) — карта не имела записи для opus-4-8 (внёс при роутинге −4) | +`"claude-opus-4-8":"Claude Opus 4.8"` → «Anthropic · Claude Opus 4.8» |
| **F3** | 📈 | `finance/regime._macro_nudges` | Макро-overlay читал только **УРОВЕНЬ** (безработица vs 4.5, GDP vs 2.0). Для режима важнее **темп роста/падения**: 4.1% безработицы *растущей* = слабость, *падающей* = сила | Сигнал = LEVEL ⊕ RATE-OF-CHANGE (Δ по истории серии: безработица −3 мес, GDP −1 кв). 50/50 при наличии истории, иначе только уровень. Δ-компоненты в `signals` для QC |
| **F3-UI** | 📈 | `report_deep_v3.html` (Regime · «Сигналы-драйверы») + `pdf_payload._macro_series_trend` | Темп роста/падения не был виден в отчёте | По КАЖДОЙ FRED-серии добавлен чип темпа (▲/▼/▬ + Δ за окно по cadence: daily 1м, monthly 3м, quarterly 2кв). AI-пак тоже получил `trend` + серии unemployment/gdp → `ai_regime_comment` рассуждает о направлении, не только уровне |
| **F4** | 🎯 | `report_deep_v3.html` (Ожид. эффект) | Плановая правка 2.3: панель называла источник «BL target weights», без пометки приоритетности | Бейдж «⚡ Δ только по высокоприоритетным идеям: <тикеры>» по флагам `scoped_to_high_priority`/`high_priority_tickers`; подпись «BL target weights» убрана |

**Математика отчётов — сверка пройдена:** DEEP CVaR 3.25%×$11 326 = $368 ✓; DEEP MaxDD −24%→$11 326×0.76≈$8 600 ✓;
BASE MaxDD −24.25%×$11 273 = $2 734 ✓; Sharpe 0.52(BASE)/0.55(DEEP) консистентны для разных дат. ИИ-комментарии
грамматически чисты и **связывают секции** (концентрация→плечо→конкретный тикер NVDA→action; Sharpe→бенчмарк;
CVaR→плечо) — правила DATA-DRIVEN + ЦЕПОЧКА АНАЛИЗА работают.

**Аудит md-файлов:** 7 нужны (`SYSTEM_PROMPT.md` — **грузится в рантайме** `ai_narrative`/`advisor_bot`; `CLAUDE.md`,
`README.md`, `AUDIT.md`, `REPORT_SECTIONS.md`, `REPORT_SECTIONS_AUDIT.md`, `MCP_STRATEGY.md`). **4 чужих**
(`PARITY.md`/`PHILOSOPHY.md`/`ROADMAP.md`/`USAGE.md` — документы проекта «Claw Code», Rust-харнесс: нет `rust/`,
нет `claw`, 0 ссылок) → **УДАЛЕНЫ** (подтверждено владельцем).

pytest **446 passed, 10 skipped** · оба тира рендерятся.

---

## −4.5. Дизайн V2 (Premium re-skin) — cream/gold/ink, Manrope + JetBrains Mono, мобайл (2026-06-25)

> Запрос: применить дизайн «Portfolio Premium DEEP» к BASE и DEEP, аккуратно, с мобильной адаптацией;
> старый сохранить как V1. **pytest 461 passed.**

**Важно — почему re-skin, а не 1:1 клон:** присланный `Portfolio Premium DEEP (standalone).html` (1.5 МБ) —
это **собранный React-бандл** (пустой `<body>` + 1.4 МБ минифицированного JS, «Unpacking…»), который рендерит
UI на клиенте. Он архитектурно несовместим с текущим пайплайном (server-side Jinja → статический подписанный
HTML → ссылка в Telegram, без JS): дроп бандла сломал бы доставку (пустой отчёт без JS), весил бы 6×, и потерял
бы 56 data-bindings. Лэйаут из минифицированного бандла headless извлечь нельзя. Поэтому из бандла извлечён
**дизайн-язык** и применён к рабочим шаблонам.

| Что | Стало |
|---|---|
| Палитра | синий акцент `#2F6FB3` → **золото** (`--tech-blue` = тёмное золото `#9A7A10` для текста, `--gold #F5D04E` для заливок, золотые тинты); крем `#FAF9F5`/`#FBF8F1`, ink `#1C1B1A`, AI-блок золотисто-кремовый; негатив тёплый красный |
| Типографика | IBM Plex Sans/Mono → **Manrope + JetBrains Mono** (Google Fonts), оба тира |
| Чарты (Python) | спарклайны `#2F6FB3` → золото `#9A7A10` (`tg_bot`, `html_renderer`) |
| Мобайл | единый `<head>`-блок `@media (max-width:600px / 380px)` с `!important`: все grid → 1 колонка, таблицы — горизонтальный скролл, SVG/гейджи `max-width:100%`, шрифт-скейл H1/KPI |
| V1 | **сохранён**: `report_basic_v1_design_prototype.html`, `report_deep_v1_design_prototype.html` (старый синий/IBM-Plex дизайн нетронут) |

Объём: все hardcoded синие хексы + IBM-Plex заменены bulk-картой по обоим шаблонам (4 `:root`-блока/тир + инлайн);
0 синих рефов в живых шаблонах. Структура/данные/231 binding не тронуты. Оба тира рендерятся, 461 тест зелёный.

📝 Если нужен **точный** премиум-лэйаут (а не язык дизайна) — нужен дизайн как обычный HTML/CSS (не React-бандл)
ИЛИ посекционная пересборка в Jinja (≈спринт). React-SPA-как-отчёт = сломанная статик-доставка + 1.5 МБ, не рекомендуется.

---

## −4.4. Аудит живых отчётов 06-24/25 (Sprint 6.4) — Expected Effect buy-side, чекеры, сортировка риска

> Прод-отчёты `2026-06-24/deep` + `2026-06-25/base`. Независимый мат-аудит подтвердил все формулы
> (Эйлер-ERC=100%, CVaR/MaxDD %↔$ сходятся, gauge 48/100 воспроизведён, выпуклый кап) и нашёл **1 баг
> высокой severity** + точечные. **pytest 461 passed.**

| # | Где | Дефект (живой факт) | Стало |
|---|---|---|---|
| 1 | `simulate.high_priority_target_weights` | Идея «Ожид. эффект» = **только продажи** (кап турновера заполняется продажами → покупки отложены в кэш) — не видно что покупаешь, концентрация растёт | Если идея sells-only — **реинвест** освободившегося веса в топ-BL-покупки среди УЖЕ держимых имён (≤+12пп/имя, остаток в кэш) → панель показывает Продать **и** Купить, метрики отражают полный ребаланс |
| ❌2 | `report_*_v3.html` обложка «Где сосредоточен риск» | top-5 сортировался по **строке** `euler_risk` (`"9.6%">"33.9%"` лексикографически) → порядок мусорный, **MSFT (14.8% TRC, реальный #4) выпадал**, SLV (3.0%) показывался | добавлен числовой `euler_risk_pct`; сортировка по нему (оба тира) |
| 🔧3 | `pdf_payload._build_integrity_checks` | чип «Факторная модель · 100% покрытие» **зелёный ✓**, хотя CoVe на стр.6 флагует те же факторы `warn` (κ=61) | чип → **⚠** при `near_collinear && !orthogonalized` (показывает κ) — панели согласованы |
| 🔧4 | `ai_narrative` confidence | панели режима **75%**, проза ИИ **74%** (модель округляла сама) | в промпт идёт `confidence_pct` = `int(round(conf*100))`, ИИ берёт **вербатим** |
| 🔧5 | `ai_narrative` промпт | ИИ протекал внутренние имена полей в прозу: «(см. ai_holdings_comment)» | правило: НЕ упоминать `ai_*_comment` и «(см. …)» — писать законченную прозу |

✅ **Подтверждено аудитом:** Smart-Money чекер рендерится **нейтральным «–»** (не красный ✗) — фикс 6.3 работает; Expected-Effect вердикт `tradeoff` теперь согласован с ИИ-комментарием (промпт-фикс 6.3 работает); все CoVe/integrity-строки правдивы.

📝 Остаётся (минор): дисплей-округление дельты карточек Эфф. (±0.1); таксономия «Облигации 21% (мандат) vs TLT 16%» — разные бакеты, не баг.

pytest **461 passed, 10 skipped** · оба тира рендерятся.

---

## −4.3. Аудит живых отчётов 06-23 (Sprint 6.3) — вёрстка, чекеры, Smart Money, Expected Effect

> Скачаны прод-отчёты `2026-06-23/{base,deep}.html` + два независимых аудита (мат-движок/чекеры
> read-only + ревизия per-page доков). Найдены 1 вёрстка-баг, 1 чекер-баг (вводит в заблуждение),
> 1 само-противоречие панели, + точечные правки. **pytest 460 passed.**

| # | Где | Дефект (живой факт) | Стало |
|---|---|---|---|
| 1 | `report_deep_v3.html` Regime | F3-чип темпа добавил **4-й** элемент в 3-колоночный grid `reg-signal-row` → `state` переносился на новую строку (пустые области) | value+темп в ОДНОЙ ячейке (inline-flex) → grid снова 3 колонки, мобильный оверрайд цел |
| 3 | idea cards | 4-я карточка «Режимная ставка» | → **Smart Money** (институционалы + инсайдеры): `regime_play`→`smart_money` в промптах, фолбэке (BRK.B/BLK/BX), билдере, пайплайнах; рендерится в bucket `rotation` |
| 4/16 | CoVe + Smart Money | `missing` для **выключенного** слоя инсайдеров рендерился как **красный ✗ «источник недоступен»** (= как сбой). 5-state→3-state коллапс в шаблоне | `disabled`/`missing` → нейтральный **«–»** (отдельный класс/легенда «источник не запрашивался / выключен»); `insider_lineage_row` отдаёт `disabled` |
| 5/19 | Expected Effect | Панель **сама себе противоречила**: индекс 49→51, Max TRC 35%→**53%** (концентрация растёт), а ИИ-нарратив + обложка заявляли снижение риска | (а) панель спеллит **идею**: «Продать NVDA −Nпп / Купить … +Nпп» с цветом и пояснением направления; (б) промпт `ai_effect_comment` ОБЯЗАН согласовать с `rebalance_verdict` (kind=tradeoff → явно назвать рост Max TRC, не заявлять односторонне снижение) |
| 4 (мат) | `pdf_payload` $-поля | MaxDD «−20.2%» рядом с **$2 759** (=20.15%×NAV) — $ из сырой доли, не из показанного % | `_dollar(frac, decimals)` берёт ТОТ ЖЕ округлённый % (1 знак), что и заголовок → CVaR/VaR/MaxDD $ консистентны с % |
| 17 | CoVe LLM-чекеры | «held-filter… **активны**» — claim только по факту наличия ИИ, без реального pass/trip | «…**настроены»** (configured) — без переоценки гарантий |
| — | шаблоны | DEEP source-strip «FRED … **PMI**» (снят с публикации); BASE waterfall-комментарий с прототипными PLTR/CRWD | PMI→реальные 6 серий; комментарий помечен «данные из движка, не статика» |
| docs | `docs/REPORT_PAGES_*` | 6 ошибок (temp 0.5→0.7, месячный→дневной якорь, `factor_betas`→`factor_scores`, подпись EWMA, формула темпа = OLS-наклон, квартальный lag 2→3) | исправлены отдельным проходом |

✅ **Подтверждено аудитом мат-движка:** CVaR-bootstrap, Sharpe/Sortino (гео-аннуализация + downside-deviation), MaxDD, Эйлер-ERC=100%, composite-gauge (воспроизведён 49/100), 4-Pillar клипы/hotspot/`_finite`, выпуклый кап стресса, %↔$ стресса, BL, режим — **все корректны**. CoVe-строки data-driven и правдивы (κ=61.59 честно `warn`, CDS 9/10 `warn`, стресс 2 proxy `warn`).

pytest **460 passed, 10 skipped** · оба тира рендерятся.

---

## −4.2. Аудит живых отчётов 06-22 (Sprint 6.2) — идеи, токены, темпы, Smart Money, мультиколлинеарность

> Скачаны прод-отчёты `2026-06-22/{base,deep}.html`. **κ факторов = 61.85** вживую (10 факторов) —
> мультиколлинеарность подтверждена. Smart Money — только gated-строка CoVe, секции нет. BASE-идеи
> стоят месяц. По итогам — 5 направлений.

| # | Блок | Было (живой факт) | Стало | Файлы |
|---|---|---|---|---|
| 1.1 | Динамика идей BASE | Идеи BASE статичны месяц + дежурные мегакэпы (V/MA/GS/UNH). Корень: якорь свежести **`%Y-%m`** (месячный!) + бан-лист не покрывал quality-мегакэпы | Якорь стал **ДНЕВНЫМ** (`%Y-%m-%d`) + **дневной «угол ротации»** (7 углов по dayofyear); бан-лист +V/MA/GS/UNH/PG/AVGO; temp 0.5→**0.7** (band 0.5–0.85) | `ai_narrative.py` |
| 1.2 | Токены / логика ИИ | — (проверка) | Комментарии **НЕ обрезаются** (max 158/205 знаков, 0 truncation) → токенов хватает; BASE max_tokens 4500→**5000** (запас под Sonnet). Логика связывает секции (концентрация→плечо→NVDA→action) ✓ | `ai_narrative.py` |
| 2.3 | Темпы макро (≥3 изменений) | F3-темп считался по **1 изменению** (latest−prior) — переворачивается от одного выброса | **OLS-наклон по ≥4 точкам = 3 изменения** (`regime.series_trend`, общий для движка/UI/AI); <4 точек → только уровень | `regime.py`, `pdf_payload.py`, `ai_narrative.py` |
| 2.4 | Smart Money «не работает» | Только gated-строка CoVe; **секции в DEEP нет** — модуль не вызывался | Подключён в `analyze_all` → `results["smart_money"]`; билдер `_build_smart_money`; **DEEP-блок** рендерит таблицу или плашку «источник не активирован»; **создан `SMART_MONEY.md`** (архитектура+модели) | `investment_logic.py`, `pdf_payload.py`, `report_deep_v3.html`, `smart_money.py` |
| 3.5 | Мультиколлинеарность (κ=61.85) | Только диагностика; PCA отклонялась | **Иерархическая ортогонализация** (gated `FACTOR_ORTHOGONALIZE=1`): style/EM-факторы резидуализируются против core (Market/Rates/Commodities) — «макро→суб-факторы стилей», имена сохранены (НЕ PCA); тест: κ падает | `investment_logic.py` |

**Прунинг:** PCA по-прежнему отклонён в пользу **именованной** иерархии (Momentum остаётся Momentum, market-neutral). Ортогонализация и macro-overlay — **gated OFF** до валидации на проде. Live SEC Form-4 провайдер + ML-тиры инсайдеров — след. фаза (см. `SMART_MONEY.md`).

pytest **456 passed, 10 skipped** · оба тира рендерятся. Доки: `SMART_MONEY.md`, `docs/REPORT_PAGES_{BASE,DEEP}.md` (посекционно), `REPORT_SECTIONS.md`, этот раздел.

---

## −3. 360° Аудит (2026-06-14, tip `bfc8baf`) — CTO / Quant / Security

> **Метод:** 3 независимых прохода (математика · код/edge-cases · безопасность/LLM) + посекционный разбор живых отчётов `2026-06-14/{base,deep}.html`. **Критических блокеров — НЕТ.** Готовность ≈ **88/100** (production-ready под надзором).

**Дашборд:** Математика **93** · Архитектура/код **82** · Инфра/Безопасность **88** · UI/UX **87**.

| ID | Sev | Где | Суть | Статус |
|---|---|---|---|---|
| U-1 | 🟡 | `ai_narrative._soft_trim` | Комментарии ИИ резались **мид-слово** в проде («увеличивать Fi…», «недооце…») | ✅ Исправлено `bfc8baf` (откат на границу слова перед «…») |
| A-3 | 🟡 | `pdf_payload.py:584` | `risk_pct`-фолбэк `vol/0.40·100` без clamp → перекрут стрелки гейджа `(risk_pct−50)·1.8°` | ✅ Исправлено `bfc8baf` (`min(100,max(0,…))`) |
| S-2 | 🟢 | `ai_narrative._summarise_for_prompt` | Макро-поля FRED (`status/as_of/unit`) в промпт без `_safe_text` (риск~0) | ✅ Исправлено `bfc8baf` (defense-in-depth) |
| M-1 | 🟡 | `investment_logic.py:794-797` | Sharpe на **модельной** σ, Sortino/доходность — на **реализованной** → несравнимость источника vol | ⏳ Action Plan A (выбрать один σ или задокументировать) |
| A-1 | 🟡 | `tg_bot.py:808-916` | Мёртвый v1-путь сборки отчёта **внутри Telegram-слоя** (дубль `build_payload`, свой `iterrows`/`risk_pct`) за `REPORT_VERSION=v1` (дефолт v2) | ⏳ Action Plan A (удалить ветку) |
| A-2 | 🟡 | `scoring_orchestrator.py:232`, `stress.py:274` | Векторизуемые `iterrows` на горячем пути (`_compute_valuation_ratios` — чистая колоночная арифметика; стресс — 7 проходов/отчёт) | ⏳ Action Plan A (векторизовать + golden-тест) |
| S-1 | 🟢 | `tg_bot.py` | Латентный `{exc}` брокера в **мёртвом** `_build_analysis_payload` (нарушает F-6) | ✅ Исправлено 2026-07-16 (раунд −22 ревью): `_build_analysis_payload` + `_fetch_and_analyze_sync` удалены целиком |
| — | 🟢 | `simulate.py:34`, `black_litterman.py:193` | Устаревший докстринг (`|CVaR|/0.10`); пост-loop ренорм = no-op | ⏳ Action Plan B (косметика) |

**✅ Подтверждено отлично (3 прохода):** нет валютной утечки в ковариацию (неконвертируемые активы дропаются до Σ, T-1 lag); BL-постериор / Эйлер-ERC=100% / выпуклый кап / детерминированный bootstrap-seed / гео-аннуализация — корректны; биллинг атомарен (списание только после Checkpoint-3 + refund на всех фейлах); гигиена секретов (логи только `*_present`); защита от prompt-injection (regex+strip+`<untrusted_data>`); edge-cases (пустой/одно-актив/σ=0/÷0/NaN) защищены; SSOT-консолидация реальна; SVG в границах viewBox; семантика цвета верна (SELL=красный).

**Action Plan:**
- **Спринт A (чистота, низкий риск):** удалить v1-путь + `_build_analysis_payload` (A-1+S-1 разом); векторизовать `_compute_valuation_ratios`/`stress` (A-2) с golden-тестом; свести Sharpe/Sortino к одному σ (M-1).
- **Спринт B (полировка):** запрет сырых чисел режима в прозе ИИ; докстринг `simulate.py`.
- **Спринт C (стратегия):** денежный леджер → Cloud SQL/Firestore (durable, снимает gcsfuse OutOfOrderError на корню); hash-lock зависимостей через CI; MCP-фаза (`MCP_STRATEGY.md`).

---

## −2.4. Sprint 5.4 — панель мандата (раскладка+мобайл), CoVe-полнота, инфра-вердикт (2026-06-13)

> **СТАТУС: реализовано, 412 тестов зелёные.** Сверено с живыми отчётами `2026-06-13/{base,deep}.html`
> (после деплоя 5.3 — вердикт коротк., язык простой, «95% CL/ДИ» убраны — всё подтвердилось вживую).

**Замечания владельца:**

| № | Замечание | Стало | Файл |
|---|---|---|---|
| 1 | «Соответствие мандату» переместить ПОСЛЕ комментариев ИИ; ровно/красиво + мобайл | Панель перенесена в `cover-main`, сразу за пунктами ИИ (закрывает блок «Вердикт по портфелю»). Партиал переписан: адаптивная карточка с **мини-барами** (зелёная полоса допустимого диапазона + цветной маркер факта: ok/under/over), самодостаточный `<style>` с `@media ≤480px`, «допустимое отклонение» вместо «tracking error» | оба шаблона, `_mandate_compliance.html` |
| 2 | CoVe — добавить верификаций для полной картины? | Да, **одна значимая дыра — валютный слой**. Добавлена строка «Валютный слой (конверсия + ставка)» (`_fx_status`): FX-провайдер + Base-валюта + источник ставки, `ok`/`warn` по покрытию/фолбэку. CoVe 13→**14**. Подпись EWMA исправлена (`λ≈0.94`→`hl=63,λ≈0.99`). Прочие кандидаты отклонены (покрыты integrity / малоценны) | `data_lineage.py` |

📝 `Expansion` ещё в **статической** панели режима (reg-name) и легенде квадранта — это подписи чарта,
не проза ИИ; русификацию чарта вынес в бэклог (по запросу).

### 🖥 Инфра-вердикт (P-I-C-O про Webhooks + SQLite) — проанализировано, к реализации НЕ принято

Владелец просил «убрать ненужные моменты, убедиться что точно не нужны на данном этапе». Проверено по факту:

- **TelegramConflictError → Webhooks: НЕ нужно.** `cloudbuild.yaml` фиксирует `--min-instances=1
  --max-instances=1 --concurrency=1 --no-cpu-throttling` (комментарий в файле: «single instance — SQLite
  is not multi-writer safe»). Бот **одно-инстансный по дизайну**; «балансировщик с несколькими инстансами»
  — ложная посылка. 409 в `12:36:40` — это оверлап деплоя 5.3 (старая ревизия ещё поллит, новая стартует),
  **транзиентный и самоустраняющийся**: уже есть `_RetryingSession`, `_CONFLICT_MAX_RETRIES=5` (10с backoff)
  и graceful-shutdown (`dp.stop_polling()`+`session.close()` ≤2.5с → освобождение getUpdates). Переход на
  webhooks — крупный риск-рефактор, противоречащий single-instance + long-polling архитектуре. **Отклонено.**
- **SQLite-on-GCSFuse OutOfOrderError: уже смягчено, миграция — стратегическая, не сейчас.** В
  `db_tokenomics.py` уже: `journal_mode=DELETE` (НИКОГДА WAL на gcsfuse), `synchronous=FULL`,
  `busy_timeout=5000`, `BEGIN IMMEDIATE` для записей, очистка осиротевших `-wal/-shm`. Код прямо
  комментирует «gcsfuse raises OutOfOrderError». Лог `…will now use legacy staged writes` = gcsfuse
  **сам деградирует на staged-writes** (поддерживают out-of-order) — запись проходит, не потеря данных.
  Паттерн «копировать в /tmp + синк при shutdown» для **денежного** леджера **опасен** (SIGKILL без
  graceful → потеря коммитов). Durable-решение — Cloud SQL/Firestore (это уже стратегический пункт F-5),
  отдельный инфра-проект, а не быстрый код-патч. **К реализации сейчас не принято; current-mitigation
  верифицирована как корректная.**

---

## −2.3. Sprint 5.3 — UX-замечания по обложке/языку (живые отчёты, 2026-06-13)

> **СТАТУС: реализовано, 408 тестов зелёные.** Три замечания владельца по живым отчётам
> `2026-06-12/{base,deep}.html` — все закрыты. Касается ТОЛЬКО подачи (текст ИИ + раскладка),
> математика не тронута.

| № | Замечание (из живого отчёта) | Стало | Файл |
|---|---|---|---|
| 1 | Секция «Вердикт по портфелю» перегружена текстом (вердикт 3–4 предложения + summary 3–4) | Вердикт — **1 короткое предложение**, summary — **макс 2**; лимиты в промпте + soft-trim ужат (300/400 → 150/230); fallback-вердикт переписан коротко и просто | `ai_narrative.py` |
| 2 | Жаргон в комментариях ИИ: «trailing 12M», «ДИ: −3.85…−2.61%», «Expansion», CVaR/Sharpe/TRC/Beta как голые термины | `plain_rule` в ОБА промпта: режим — только по-русски («экономика растёт»), запрет trailing/ДИ/CVaR/… с заменами на простые слова; в KPI-подписи «95% CL/CI» → «худшие 5% дней / разброс» | `ai_narrative.py`, оба шаблона |
| 3 | «Соответствие мандату» стояло в секторной секции, оторвано от профиля | Перенесено в зону «Вердикт по портфелю» — сразу под индексом риска (гейджем) | оба шаблона |

📝 Режимные ярлыки в SVG-квадранте (легенда чарта) оставлены на латинице — это подписи графика,
а не текст для чтения; в прозе ИИ теперь только русские формулировки.

---

## −2.2. Sprint 5.2 — аудит ЖИВЫХ прод-отчётов 2026-06-12 и фиксы (8 дефектов)

> **Метод:** скачаны и посекционно распарсены реальные отчёты `r/148046720/2026-06-12/{base,deep}.html`
> (сгенерированы прод-сборкой ПОСЛЕ Sprint 5/5.1, портфель с маржой 131%/1.16x).
> Полный посекционный разбор + маппинг — **`REPORT_SECTIONS_AUDIT.md`** (новый файл).
> Подтверждено вживую: живая точка режима (+0.11/+0.07 = Expansion ✓), R3-плашка, мандат-панель
> с маржой −16% и нарушением Stocks_US 80%>60, нейтральный it_share, data-driven идеи DEEP
> (MSCI/ANET/META/SPGI — без зацикленности). Тесты: **402 passed**.

| ID | Секция | Было (в живом отчёте 2026-06-12) | Стало | Файл |
|---|---|---|---|---|
| LV-1 | Обложка (оба тира) | H1 «Вердикт» — захардкоженный прототип («…весь риск собран в двух позициях») для любого портфеля; `ai_verdict` в DEEP не рендерился нигде | H1 ← `ai_verdict`; статик — только no-AI fallback без выдуманных фактов | оба шаблона |
| LV-2 | Обложка (оба тира) | `ai_bullets` (4–7 оплаченных инсайтов) не рендерились ни в одном тире | Список инсайтов на обложке (top-4 BASE / top-5 DEEP) | оба шаблона |
| LV-3 | Гейдж-бейдж | «Умеренный» (3-бакет) противоречил панели «Умеренно-агрессивный» на той же странице | Бейдж показывает реальное имя профиля при наличии user_profile | `pdf_payload.py` |
| LV-4 | Идеи BASE | Пики Haiku осушены held-фильтром → подставлен фолбэк-каталог июня (JNJ/PLTR/MTUM/KO) **без пометки** | `held_rule` в промптах (тикеры портфеля запрещены в пиках) + пометка «· резервный каталог» у бэкфилла | `ai_narrative.py` |
| LV-5 | Идеи BASE | Заголовки карточек — сырые ключи `boost_alpha`/`rebalance` | label==ключ → каноническое русское имя категории | `pdf_payload.py`, `ai_narrative.py` |
| LV-6 | Margin-предупр. BASE | Haiku: «прибыль **удваивается**» при плече 1.16x (матнеправда) | Промпт диктует множитель ≈{ratio}x и запрещает «удваивается» при плече ≠2x | `ai_narrative.py` |
| LV-7 | Ожидаемый эффект | «+0.0 пп» горел зелёным (дельта 0.04 пп округлялась на дисплее) | Отображаемый ноль (|Δ|<0.05 пп) → нейтральный цвет | `pdf_payload.py` |
| LV-8 | Бенчмарки / CoVe | Карточка «Профильный бенчмарк» без имени ETF; CoVe: «Anthropic Claude claude-sonnet-4-6» | «Профильный бенчмарк · Nasdaq 100»; «Anthropic · Claude Sonnet 4.6» | `pdf_payload.py`, `data_lineage.py` |

📝 Наблюдение (кандидат следующей фазы): BL-кап на ВЕС (20%) не ограничивает ВКЛАД В РИСК — живой
отчёт показал Max TRC +12.2 пп под таргетами (вердикт «компромисс» честно это пометил). TRC-aware
ограничение — в бэклог.

---

## −2.1. Sprint 5.1 — закрытие остаточных находок по секциям отчётов (2026-06-11)

> **СТАТУС: реализовано, 393 теста зелёные.** Закрыты ВСЕ открытые находки секций Deep/Base из разбора Session-9 (S2/S4/A2/A3/L1/L2 + идеи + §4.2 + EWMA-док). MCP-задачи (§5.3/5.5/5.6) — сознательно отложены до следующей фазы. Карта секций для правок — новый `REPORT_SECTIONS.md`.

| ID | Секция отчёта | Было | Стало | Файл |
|---|---|---|---|---|
| **S2** | 4-Pillar (F) | Кросс-секционный z почти всегда мёртв (<5 имён сектора) → молча правили статик-константы «2020-25» | Трёхступенчатый каскад: когорт портфеля → **live SEC-когорт лидеров сектора** (`_SECTOR_REPRESENTATIVES`, lru-кэш, kill-switch `SECTOR_COHORT_DISABLED`) → статик только last-resort с логом | `scoring_orchestrator.py`, `investment_logic.py` |
| **S4** | 4-Pillar / Gatekeeper | Порог Hotspot TRC>20% дублировался в 2 модулях (риск рассинхрона 🔥-флага и GK-1) | SSOT: `finance.scoring.HOTSPOT_TRC_PCT`; оба места импортируют | `scoring.py`, `scoring_orchestrator.py`, `gatekeeper.py` |
| **A2** | Ожидаемый эффект | `it_share` помечен «меньше=лучше» → любое урезание теха зелёное (для агрессивного мандата — задом наперёд); нулевая дельта рендерилась красной | `it_share` **нейтрален** (`_NEUTRAL_METRICS`, improved=None); нулевая дельта нейтральна; шаблон рендерит None без цвета | `simulate.py`, `report_deep_v3.html` |
| **A3** | Action Plan | Стопы/тейки одинаковы для консерватора и агрессора | ATR-дистанции масштабируются мандатом ×0.75/1.00/1.25 (`MANDATE_LEVEL_SCALE`); SMA-якоря не тронуты; MODERATE = прежние числа | `action_plan.py`, `investment_logic.py` |
| **L1** | Секторный пай | Gross 135% и «Leverage 1.18x» рядом без объяснения разных баз | Бейдж подписан: Gross = (лонг+\|маржа\|)/капитал · Плечо = лонг/капитал | оба шаблона |
| **L2** | Gatekeeper / Мандат-панель | Плечо считалось, но не риск-чекалось; маржа не видна в панели мандата | **Check 10 GK-10-LEVERAGE** (warning при марже, critical при gross>150%); строка «Маржинальный долг −X%» в панели; leveraged ⇒ не compliant | `gatekeeper.py`, `pdf_payload.py`, `_mandate_compliance.html` |
| **Идеи** | AI Ideas / «Идеи ИИ» | Промпт праймил «дежурные» имена (PLTR/JNJ/KO…); фолбэк — замороженный 1-кандидатный каталог | Правило **DATA-DRIVEN** в обоих промптах (идеи обязаны опираться на режим/недовесы/4-Pillar/RAG; шаблонные имена запрещены без данных); фолбэк — 3 кандидата/слот + детерминированная **месячная ротация** | `ai_narrative.py` |
| **§4.2** | Нарратив (вся генерация) | `temperature` слался всегда → миграция DEEP на Opus 4.7/4.8 дала бы HTTP 400 | `_model_supports_temperature`: на Opus 4.7/4.8 параметр опускается — миграция подготовлена | `ai_narrative.py` |
| **EWMA-док** | Math Inventory | Коммент «halflife=63 ≈ λ=0.94 RiskMetrics» неверен (hl=63 ⇒ λ≈0.989; λ=0.94 ⇔ hl≈11д) | Комменты и Inventory исправлены: hl=63 — сознательно плавнее RiskMetrics | `investment_logic.py`, AUDIT row 5 |
| **§4.3-хвост** | Документация | CLAUDE.md описывал несуществующий `rust/`+cargo | CLAUDE.md переписан под Python/pytest-реальность; добавлен `REPORT_SECTIONS.md` (карта секций → builder → шаблон) | `CLAUDE.md`, `REPORT_SECTIONS.md` |

---

## −2. Sprint 5 + UX Overhaul — «Было / Стало» (commits `783e703` + `0232f5a`)

> **СТАТУС 2026-06-11: реализовано, протестировано (376 passed), запушено на ветку.** PR ещё не открыт (PAT без права `Pull requests: write`). Оба коммита на GitHub подписаны и **Verified** (`verified=True, reason=valid`).

Цель спринта — убрать «бутафорию» (захардкоженную / мёртвую логику из предыдущего разбора) и сделать так, чтобы **математика реально управляла отчётом**, а мандат пользователя — оптимизацией и UX.

### A. Движок и математика

| Компонент | Было (до) | Стало (сейчас) | Файл |
|---|---|---|---|
| **Black-Litterman ↔ мандат** | BL полностью игнорировал мандат (δ=2.5, turnover=0.25, без cap на имя) — консерватор и агрессор получали одинаковые таргеты | Мандат задаёт `risk_aversion` (4.0/2.5/2.0), turnover (0.15/0.25/0.35) и **cap на имя** (0.10/0.20/0.30); cap — water-fill, no-op при `n·cap≤1` | `black_litterman.py`, `investment_logic.py` (`_MANDATE_BL_CONSTRAINTS`) |
| **V-пиллар (оценка)** | Только P/E + P/B (две коррелированные мультипликаторы), EV/EBITDA выключен | + **FCF-yield** (price-aware, signed-z допускает отрицательный yield); экспорт SEC `EBIT`/`FCF` | `scoring.py`, `scoring_orchestrator.py`, `sec_edgar.py` |
| **Несменяемость идей** | `temperature=0.1` → один и тот же набор идей месяц за месяцем при стабильном режиме | `temperature=0.5` (env-clamp 0.4–0.6); числа движка остаются детерминированными | `ai_narrative.py` |

### B. Отчёт (секции)

| Компонент | Было | Стало | Файл |
|---|---|---|---|
| **График режима (R1)** | Точка квадранта захардкожена в `(177,110)` = всегда «Expansion», противоречила ярлыку Recession/Slowdown | `cx/cy` вычисляются из `growth/cycle` (`_regime_dot_coords`, 625 px/ед) — точка ложится в правильный квадрант | `pdf_payload.py`, `report_deep_v3.html` |
| **Расхождение режима (R3)** | Ярлык режима (моментум) и FRED-сигналы не сверялись — risk-on мог стоять рядом с инверсией кривой | Детерминированный флаг `_build_regime_consistency`: инверсия 10Y−2Y / HY>550 б.п. / VIX>25 → плашка «⚠ Расхождение» | `pdf_payload.py`, `report_deep_v3.html` |
| **Соответствие мандату** | Лимиты мандата нигде в отчёте не показывались (мандат — мёртвый груз) | Новая панель (`_mandate_compliance.html`) в BASE/DEEP: профиль + факт-аллокация vs лимиты, over/under | `pdf_payload.py`, оба шаблона |
| **Выбор бенчмарка** | Сохранённый выбор игнорировался — отчёт всегда брал дефолт профиля | Берётся сохранённый `benchmark_ticker`, в отчёт через слот «Профильный бенчмарк» | `tg_bot.py`, `pdf_payload.py` |

### C. Плечо / маржа

| Компонент | Было | Стало | Файл |
|---|---|---|---|
| **Классификация кэша** | Отрицательный кэш (маржа) считался «Bonds» → искажал проверку мандата под плечом | Отдельный класс `Cash`; проверка мандата не искажается | `gatekeeper.py` |
| **AI-предупреждение** | ИИ не предупреждал о плече | `has_leverage` → промпт требует предупреждение о Margin Call (есть и в fallback); красная карточка в отчёте | `ai_narrative.py`, оба шаблона |

### D. Telegram UX

| Компонент | Было | Стало | Файл |
|---|---|---|---|
| **Текстовый ввод** | Любой текст «проваливался» в обработку | Жёсткий фильтр: текст только для ключей Freedom, остальное → мягкий редирект на кнопки | `tg_bot.py` |
| **Меню бенчмарков** | 12 кнопок сразу | 1 рекомендованная + «Изменить» (раскрытие по запросу) | `tg_bot.py` |
| **Прогресс / Gatekeeper** | Спам «Шаг 1…/2…/3…/4…» + страшные ⛔/⚠️ алерты в чат | **Одно** обновляемое сообщение; алерты Gatekeeper — только в лог сервера | `tg_bot.py`, `gatekeeper.py` |

### Не сделано / частично (честно)
- 🟡 **Live-метрики секторного ETF** (часть Task 6) — **невозможно** текущими данными: SEC EDGAR не даёт фундаментал по ETF (нужен отдельный провайдер). Вместо этого — FCF-yield.
- 🟡 **RAG напрямую из GCS без рестарта** (часть Task 9) — **НЕ делал**: перезапись открытой ChromaDB рискует повредить базу; вживую здесь не протестировать. Нужен отдельный коммит за env-флагом (default off) + атомарная подмена каталога.
- ❌ **PR не открыт** — у предоставленного PAT нет права `Pull requests: write` (push прошёл, PR — 403).

### Файлы и тесты
11 файлов изменено · `templates/_mandate_compliance.html` (новый партиал) · `tests/test_phase18_sprint5.py` (+21 кейс). Полная сюита: **376 passed, 10 skipped.**

---

## −1. Follow-up 360° аудит (commit `32a3a55` / merge PR #48) — остаточные находки

> **СТАТУС 2026-06-10: ВСЕ находки F-1…F-12 ЗАКРЫТЫ** (спринт ремедиации ниже). Тесты: 355 pytest + 326 deploy-gate зелёные; F-1/F-3 дополнительно верифицированы функционально.

Полный повторный аудит пост-фикс состояния (два углублённых прохода: ядро + инфра). Критических блокеров **не осталось** (денежный путь атомарен, секреты чисты).

| ID | Sev | Где | Суть | Статус |
|---|---|---|---|---|
| F-1 | 🟡 HIGH | `investment_logic.py:329` | `_marginal_var` бампил вес ±h без ренормировки → плечевая, не бюджет-нейтральная чувствительность | ✅ Бамп фондируется pro-rata из остатка книги (Σw константа); single-asset edge — задокументированный фолбэк |
| F-2 | 🟡 HIGH | `investment_logic.py:~1116` | Expected_Return складывал гео-simple факторы + арифм-лог alpha (рассинхрон единиц) | ✅ Вся ожидаемая доходность собирается в daily-log пространстве (α+Σβμ), ОДНА геометрическая аннуализация |
| F-3 | 🟡 HIGH | `investment_logic.py:576/610` | Нет guard `N≥K` перед Ridge(10)/LedoitWolf → переобученные беты на коротком окне | ✅ `len(returns) < max(2K, 30)` → graceful skip (тот же контракт, что существующие guard'ы) |
| F-4 | 🟡 HIGH | `cloud_function/main.py` | ChromaDB ingest-гонка (gen2 без лимитов инстансов) | ✅ `--max-instances=1 --concurrency=1` в cloudbuild + MANDATORY-док в main.py |
| F-5 | 🟡 HIGH | `db_tokenomics.py` | SQLite **WAL на gcsfuse** для денег/кредов; окно перекрытия ревизий | ✅ `journal_mode=TRUNCATE` + `synchronous=FULL` (persistent + per-connection); миграция существующих WAL-БД автоматическая |
| F-6 | 🟡 MED | `tg_bot.py:1681/1784/1869` | `str(exc)` тёк юзеру в 3 catch-all (через них — `resp.text` апстрима) | ✅ Все 3 переведены на error-UUID паттерн |
| F-7 | 🟡 MED | `simulate.py:137` | Sharpe арифметически аннуализирован вопреки комменту | ✅ `exp(mean·252)−1` — единая база с headline |
| F-8 | 🟡 MED | `Dockerfile:7`, `requirements.txt` | База без digest; неприпиненные `>=` | ✅ Частично: база pinned `@sha256:a3ab0b…` (текущий прод-образ). Hash-lock deps — **осознанно отложено**: lock, не провалидированный реальной сборкой образа, может сломать deploy; делать через CI-сборку (pip-audit в CI пока advisory) |
| F-9 | 🟡 MED | `.github/workflows/` | Нет PR-CI | ✅ `python-ci.yml`: pytest+unittest-gate (блокирующие) + pip-audit + gitleaks (advisory до lock'а) |
| F-10 | 🟢 LOW | `src/*.py` | ~36 мёртвых модулей клона в `src/`/образе | ✅ Удалены 33 модуля + `reference_data/` (верифицировано: 0 импортёров); `test_live_api.py` сохранён (это легитимная RAMP-утилита live-теста брокера), `batch_reports.py` — намеренный scaffold |
| F-11 | 🟢 LOW | `scoring_orchestrator.py:386`, `black_litterman.py` | Мёртвый `if True:`; равновесная π как forward return | ✅ Dedent + честный контракт (mutate-in-place); `BLResult.n_views` — при 0 активных views simulate падает на realised-оценку вместо циркулярной π |
| F-12 | 🟢 LOW | `action_plan.py:119`, `tg_bot.py` | take_target=max(дальних) не actionable; устаревший коммент concurrency=4 | ✅ Primary = trend/ATR-таргеты; 52w-high только «поднимает» цель внутри резистанс-зоны или last-resort; коммент исправлен |

**Дашборд (после ремедиации):** Математика **88** · Архитектура **78** · Инфра/Безопасность **84** · UI/UX **84** · Готовность ≈ **86/100**.
Остаточный план до 90+: hash-lock зависимостей через CI-сборку (F-8 хвост), Cloud SQL/Firestore для леджера (стратегич. апгрейд F-5), миграция DEEP→Opus 4.8, MCP-роадмап (`MCP_STRATEGY.md`, ревизия §14 от 2026-06-10).

---

## −0.5. Прод-хотфиксы (commit `dfd908b` / merge PR #51) + Mathematical Inventory

**Хотфиксы из логов прода (все закрыты, 355 тестов зелёные):**
- ✅ `pymupdf4llm` восстановлен в `requirements.txt` (был ошибочно удалён → `No module named 'pymupdf4llm'`); импорт в `rag_engine` сделан ленивым (query-путь бота не падает без ingest-зависимости).
- ✅ SQLite на gcsfuse: `journal_mode=DELETE` форсится на **каждом** соединении (`_get_conn`/`_get_conn_tx` → hardened asynccontextmanager), `synchronous=FULL`, очистка осиротевших `-wal/-shm` перед коннектом и на старте. WAL категорически устранён (vault уже на дефолтном DELETE).
- ✅ Graceful shutdown: SIGTERM/SIGINT → `dp.stop_polling()` + `bot.session.close()` в пределах **2.5с** (abort in-flight getUpdates → нет 409 при редеплое); никаких долгих фоновых задач.
- ✅ Gatekeeper-телеметрия: каждое критическое нарушение логируется с **ID правила + причиной** (`GK-1-EULER`/`GK-3-CVAR`/`GK-5-VOL`/`GK-8-MANDATE`).

### 📐 Mathematical Inventory — что движок РЕАЛЬНО считает (верифицировано построчно)

| # | Модель / формула | Реализация (file:line) | Статус |
|---|---|---|---|
| 1 | Лог-доходности `ln(Pₜ/Pₜ₋₁)` | `investment_logic.py:549,1149` | ✓ |
| 2 | **Геометрическая аннуализация** `exp(μ_log·252)−1` | `:726`, `period_returns:238`, `simulate:137` (F-7) | ✓ |
| 3 | Аннуализация vol `σ·√252`, var `·252` | `:628,662,719` | ✓ |
| 4 | **Факторная Ridge-регрессия** `r=α+Σβₖfₖ+ε` (α=0.001) | `:613`; **guard N≥max(2K,30)** `:563` (F-3) | ✓ |
| 5 | **Факторная ковариация**: EWMA(halflife=63 ⇒ λ_daily≈0.989 — сознательно плавнее RiskMetrics 0.94) ⊕ 70/30 Ledoit-Wolf | `:647,654` | ✓ (док исправлен 5.1) |
| 6 | **Структурная ковариация** `Σ=B·F·Bᵀ+D` | `:661` | ✓ |
| 7 | **PSD-floor** `max(w'Σw,0)` перед √ | `:677` (BLK-3) | ✓ |
| 8 | Портфельная vol `√(w'Σw·252)` | `:678` | ✓ |
| 9 | **Декомпозиция Эйлера** `MCTR=Σw/σₚ`, `ERC%=w·MCTR/σₚ` | `:682-687` | ✓ |
| 10 | Sharpe `(r_ann−rfr)/σ_ann` | `:730` | ✓ |
| 11 | **Sortino** `√(mean(min(excess,0)²))·√252` (стандарт) | `:718-719` (H-4) | ✓ |
| 12 | CVaR-95 историч.: среднее нижних 5% | `:728-729` | ✓ |
| 13 | **Bootstrap-CVaR**: stationary block (Politis-Romano), blk=√N, 2000, **детерм. seed из hash данных** | `:258,287,305` | ✓ |
| 14 | **Marginal VaR** (budget-neutral, fund pro-rata) | `:316,341-353` (F-1) | ✓ |
| 15 | Max Drawdown `min(eq/cummax−1)` | `:744` | ✓ |
| 16 | **Base Currency / FX** `P_base=P_local·FX` (мультипл., до returns), неконвертируемые **дропаются**, T-1 lag+ffill | `currency.py:381,262`; H-1 | ✓ нет утечки валют |
| 17 | Динамический **RFR** (валютно-сопоставлен, гео-daily `(1+r)^(1/252)−1`) | `currency.py:104,123` | ✓ |
| 18 | **Black-Litterman**: prior `π=δΣw`; posterior `μ=[(τΣ)⁻¹+PᵀΩ⁻¹P]⁻¹[(τΣ)⁻¹π+PᵀΩ⁻¹Q]`; `w*=(δΣ)⁻¹μ`; δ=2.5,τ=0.05; **n_views=0 → realised fallback** | `black_litterman.py:119,136,149`; F-11 | ✓ |
| 19 | **Expected return** `exp((α+Σβₖμₖ)·252)−1` (единое лог-пространство) | `:1134-1138` (F-2) | ✓ |
| 20 | **Convexity-cap стресса** `x'=sgn·(T+(C−T)(1−e^(−(|x|−T)/(C−T))))`, T=0.20,C=0.35 | `stress.py:98-117` | ✓ монотонно, C¹, ≤−100% |
| 21 | Composite risk (mandate-weighted CVaR/vol/concentr.) | `scoring.py:_crs` | ✓ |
| 22 | **4-pillar scoring**: robust-z `(x−median)/(1.4826·MAD)` | `scoring.py:37-62` | ✓ |
| 23 | HHI концентрация `Σw²·10000`, DOJ-полосы | `pdf_payload.py:_build_concentration` | ✓ |
| 24 | TE `std(rₚ−r_b)·√252`, IR `Δr_ann/TE` | `period_returns.py:237,243` | ✓ |
| 25 | Technicals: RSI/ATR (Wilder RMA), Bollinger, SMA, MACD, 12-1 momentum | `technicals.py` | ✓ |
| 26 | Regime: growth/cycle квадрант, confidence=magnitude×directional-agreement | `regime.py` | ✓ |
| 27 | Altman-Z (банкротство) | `scoring_orchestrator/sec_edgar` | ✓ |
| 28 | Sector super-group SSOT (Tech-комплекс) | `pdf_payload.build_sector_groups` | ✓ |

**Дашборд (текущий, dfd908b):** Математика **90** · Архитектура **82** · Инфра/Безопасность **88** · UI/UX **86** · **Готовность ≈ 87/100**. Регрессий и ошибок размерности в матричных операциях **не выявлено**.



## 0-bis. Спринт модернизации (2026-06-09) — выполнено

- ✅ **Sprint 1.1 — секторный SSOT.** Расхождение Tech 55% (BASE) vs 80.8% (DEEP) устранено: добавлен канонический super-group `Tech-комплекс (Technology+Semiconductors)` в `pdf_payload` (SSOT), который рендерится в **обоих** отчётах через `sector_warnings`, а `ai_narrative` получает `sector_complex` + директиву «не суммируй сектора сам, бери число ВЕРБАТИМ». §4.1 закрыта.
- ✅ **Sprint 1.2 — RAG-статус 3-state.** Вместо бинарного «не использован» теперь `used` / `no_match` («запрошен — релевантных отчётов не найдено») / `unavailable` («база недоступна/пуста»), корректно проброшено из `_fetch_rag_context` → `ai_summary.rag_status` → integrity-панель для **обоих** тиров.
- ✅ **Sprint 2.4 — Prompt Caching.** Системный промпт (~25 КБ) кэшируется (`cache_control: ephemeral`) — повторные вызовы читают префикс на ~0.1×.
- ✅ **Sprint 2.5 — Structured Outputs.** Нарратив форсится через типизированный tool (`emit_report`, `tool_choice`); `parsed = tool_use.input` — **детерминированный** JSON, костыль `_repair_truncated_json` убран из пути.
- ✅ **Sprint 2.6 — Batch-скелет.** `batch_reports.BatchNarrativeGenerator` — заготовка ночной генерации через Batch API (−50% токенов), переиспользует тот же кэш+tool.
- ✅ **Sprint 3.7 — `MCP_STRATEGY.md`** — полная стратегия MCP/Skills/коннекторов + честный разбор Gemini vs Anthropic + экономика.
- Тесты: **355 passed / 10 skipped** после всех правок.

---

## 0. Что изменилось с прошлого аудита

**2026-07-07 — Scenario-тир + 5-летнее окно + паритет BASE + документация.**
- **Lookback 730→1825 кал. (≈1260 торговых ≈ 5 лет)** — `HISTORY_LOOKBACK_DAYS`
  default поднят, совпал со `SCENARIO_LOOKBACK_DAYS`; FX-окно 750→1900 (покрыть
  ценовое). **Было:** 2-летнее окно недооценивало хвосты CVaR/MaxDD, не видело
  медвежий 2022. **Стало:** полный цикл 2021–2026, стабильнее беты; молодые
  активы защищены sparse-drop + `min_obs`; лейблы окна динамические. Анализ
  репрезентативности/качества — `ROADMAP_DATA_RESILIENCE §4`.
- **Scenario Analysis — отдельный тир бота (1 токен).** `finance/scenario_report`
  поверх `scenario_engine`: Панель A (5 core-метрик, Euler-MCTR Σ%=100, 3 макро-
  режима, funding-звенья) + Панель B (walk-forward, look-ahead guard). Свой Jinja
  `report_scenario_v3.html` (минуя Premium). Токен-тариф base 1 / scenario 1 /
  deep 2; кнопка в меню + inline-CTA под BASE/DEEP с per-user кэшем `results`
  (0 LLM-вызовов, billing после доставки). 20 тестов.
- **Паритет BASE↔DEEP (RAG-наблюдаемость).** BASE-футер прежде статично заявлял
  «GS/MS/JPM» даже без RAG. **Стало:** та же честная quality-полоса, что в DEEP
  (integrity_checks · RAG 3-state · факторное покрытие); банк-источники — только
  при реально консультированном RAG. Контракт BASE 12→13 ключей. Логика риска
  BASE не тронута.
- **Документация:** новый `docs/TELEGRAM_BOT.md` (полный флоу `/start`→отчёт) +
  `docs/INDEX.md` (мастер-карта доков по значимости); актуализированы
  ROADMAP-и, REPORT_SECTIONS §11, CLAUDE.md. Suite **601 passed, 10 skipped**.



**2026-07-02 — факторная декомпозиция дисперсии (additive layer).**
**Было:** риск-разложение только ПО АКТИВАМ (Euler-TRC, риск-водопад); секция «Факторное разложение (β)» показывала одни портфельные беты; AI-комментарий секции не имел числовой опоры (в промпт уходил только `beta_mkt` на позицию).
**Стало:** новый чистый модуль `finance/factor_decomposition.py` — Euler-декомпозиция дисперсии ПО ИСТОЧНИКАМ риска (σ² = bᵀFb + wᵀDw, b = Bᵀw; группы Рынок/Стили/EM/Ставки/Сырьё + идио) + marginal overlap («факторные двойники» c systematic-corr ≥ 0.90 и unique-risk на позицию). Аддитивный хук в `calculate_structural_risk` (try/except → `portfolio_metrics["factor_decomposition"]`, существующая математика не тронута; сверка: √(total·252) == `Total_Volatility_Ann` до 1e-10). Панель «Откуда берётся риск» в DEEP (Jinja v3 + Premium React `FactorVariance`, контракт deep = 30 ключей). `ai_factor_comment` (Opus) переведён на 4-шаговую институциональную схему (источник риска → двойники → наклон vs режим → чего не хватает) с данными `summary.factor_decomposition`; на no-API-пути — детерминированный `_fallback_factor_comment`. Тесты: `tests/test_factor_decomposition.py` (22 кейса).


PR #47 (Sprint 1–4) **смержен в `main`** и задеплоен — отчёты от 2026-06-09 сгенерированы уже исправленной сборкой. Прод-вывод подтверждает, что фиксы работают вживую (см. §3). Этот документ обновляет аудит с учётом результата и добавляет стратегический разбор по **MCP / сервисам Anthropic** (§5) и **уникальности продукта** (§6).

> ⚠️ Ссылку `claude.ai/share/cd4cb0bf-…` прочитать машинно не удалось (это JS-SPA — статический HTML отдаёт только слово «Claude»). Раздел про MCP/Anthropic построен на авторитетном референсе Claude API/Agent SDK + актуальном обзоре экосистемы финансовых MCP-серверов (источники в §5), а не на содержимом той переписки. Если пришлёте текст/скриншот — уточню под конкретику.

---

## 1. Executive Summary

Проект перешёл из «сильной беты» в **«production-ready ядро под надзором»**. Три финансовых блокера и слой High-фиксов закрыты и **подтверждены в проде**:

- ✅ Биллинг атомарен (нет TOCTOU/двойных списаний).
- ✅ Числовая гигиена держит: в отчётах **нет `nan`/пустых полей**, RFR и валюта показаны.
- ✅ Стресс-тесты с дисклеймером выпуклого ограничителя, HOTSPOT — из движка.
- ✅ AI-нарратив связный, на русском, без следов prompt-injection.

Осталась **не-блокирующая** работа: одна расхождение в данных между тирами (Tech 55% vs 80.8%, §4.1), хвост High/Medium из прошлого аудита, и **стратегический разрыв**: бот **никак не использует** современный стек Anthropic (prompt caching, tool use, MCP, batch, structured outputs) — это и главный источник экономии, и главный рычаг дифференциации (§5–6).

**Готовность к Production:** **~80/100** (было 68). Можно работать с реальными деньгами под мониторингом; до институционального GA — закрыть §4 и реализовать caching + structured-outputs из §5.

---

## 2. Scoring Dashboard

| Категория | Было | Стало | Комментарий |
|---|---:|---:|---|
| 🧮 Математика и алгоритмы | 80 | **86** | BLK-2/3 и H-1/H-4 закрыты; прод-отчёт показывает корректные CVaR-bootstrap (Politis-Romano, 2000 блоков), Эйлер-TRC, гео-аннуализацию. Минус — §4.1 и латентные edge-кейсы. |
| 🏗️ Архитектура и качество | 60 | **74** | Слои чисты (H-2/H-3), мёртвый порт/rust вырезаны, шаблоны консолидированы на v3. Минус — ~32 инертных модуля «порта» ещё в `src/`, нет CI-теста на PR. |
| 🔒 Инфраструктура и безопасность | 68 | **82** | Атомарность денег, MultiFernet-ротация, маскирование логов, non-root, M-9 fail-fast. Минус — gcsfuse+SQLite, неприпиненные зависимости. |
| 🎨 UI/UX и шаблоны | 70 | **84** | RFR/валюта/FX-аудит видны, narrative-комментарии рендерятся, graceful-degradation + error-UUID, SVG NaN-safe. Минус — §4.1 и хаос версий (исторический). |
| 🤖 **AI / LLM-стек (новая ось)** | 35 | **62** | ✅ Prompt-caching системного промпта + ✅ structured outputs (детерминированный JSON через forced `tool_choice`, костыль убран) + ✅ Batch-скелет. Осталось: MCP-клиент/сервер, batch-wiring, provider-abstraction для A/B (§5–6, `MCP_STRATEGY.md`). |

---

## 3. Закрытые задачи (Sprint 1–4) — с подтверждением из прод-отчётов

| ID | Фикс | Подтверждение в отчёте 2026-06-09 |
|---|---|---|
| BLK-1 | Атомарный биллинг (guarded UPDATE + `BEGIN IMMEDIATE` + WAL + ON CONFLICT) | Юнит-инвариант: 15 параллельных списаний на балансе 10 → ровно 10, баланс 0 |
| BLK-2 | `Return_Pct` без деления на ноль | Нет `inf`/`nan` в P&L; доходности корректны (порт +11.4% vs S&P +18.0%) |
| BLK-3 | PSD-floor перед `sqrt` | Vol=22.2%, Sharpe=0.79, Sortino=1.10 — конечные, не NaN |
| H-1 | Дроп активов без FX-кросса | Портфель USD-only → FX no-op, «Валюта отчёта: USD», без mixed-currency |
| H-4 | Стандартный Sortino (RMS/total-N) | Sortino=1.10 отображается консистентно |
| H-2 | HOTSPOT из движка (`a.hotspot`) | 🔥 NVDA (TRC 32.2%), ORCL (TRC 21.4%) — флаги из движка, не из шаблона |
| H-3 | Фасад `prefetch_market_data` | Прод стабилен; бот не лезет в `engine.*` |
| H-6/H-7 | Маскирование ключей + MultiFernet | — (не видно в отчёте; верифицировано тестом ротации) |
| M-1/M-2 | Graceful UI + error-UUID | Отчёт сформирован без падений; данные полные |
| M-3/M-4/M-8 | SVG: высота легенды, NaN-guard, `exp(cumsum)` | Графики (equity curve, сектора) рендерятся без обрезки |
| M-5/M-9 | Non-root Docker + amnesia-guard | Verified (non-root smoke; M-9 4 кейса) |
| L-1/L-2/L-8 | Удаление rust/ + порта, README, шаблоны v3 | Репо bot-focused; прод рендерит `report_*_v3.html` |

**Прозрачность данных в отчёте (то, чего раньше не было видно):** data-quality панель показывает `Tradernet · 501 день · daily CLOSE · Ridge β 10/10 факторов · Ledoit-Wolf 70/30 · CVaR Politis-Romano 2000 блоков · Claude Haiku 4.5 / Sonnet 4.6`, **«Валюта отчёта: USD · RFR 4.50% · default[USD]=0.045»**, дисклеймер выпуклого ограничителя (±20% линейно → асимптота ±35%).

---

## 4. Остаточные и новые находки

### 🟡 4.1 (НОВОЕ, MEDIUM) — Расхождение секторной концентрации между тирами
**✅ РЕШЕНО (Sprint 1.1).** Причина: движок корректно **разделяет** Technology (AAPL/MSFT/GOOGL/ORCL ≈55%) и Semiconductors (NVDA ≈25.8%, отдельный SOXX-фактор), панель показывала split, а **LLM свободно лумпил** их в «80.8% Technology» в прозе. Фикс — единый источник истины: `pdf_payload.build_sector_groups` считает канонический `Tech-комплекс (Technology+Semiconductors)=80.8%` ОДИН раз; он рендерится в обоих отчётах через `sector_warnings`, и `ai_narrative` получает `sector_complex` + директиву использовать число ВЕРБАТИМ и не агрегировать сектора самостоятельно. Числа панели и прозы теперь совпадают. (Тест-санити: `build_sector_groups` → 80.8%.)

### 🟡 4.2 (MEDIUM) — Снапшот-пиннинг базовой модели и `temperature`
`ai_narrative.py`: `MODEL_BASE="claude-haiku-4-5-20251001"` (снапшот), `MODEL_DEEP="claude-sonnet-4-6"`, `temperature=0.1`. Снапшот безопасен, но «застывает»; обе текущие модели `temperature` принимают, однако при апгрейде DEEP до Opus 4.7/4.8 `temperature`/`budget_tokens` дадут **400** — закладывайте миграцию (см. §5.4).

### 🟢 4.3 (LOW) — Хвост из прошлого аудита (статус 2026-06-11)
- ✅ H-5 marginal_var без ренормировки — закрыто F-1 (budget-neutral pro-rata).
- ✅ Expected-return рассинхрон единиц — закрыто F-2 (единое лог-пространство).
- ⏳ gcsfuse+SQLite — для GA рассмотреть Firestore/Cloud SQL для денежного леджера (стратегич., отложено).
- ⏳ Неприпиненные зависимости → hash-lock через CI-сборку (см. F-8, отложено сознательно).
- ✅ ~32 инертных модуля порта — закрыто F-10 (33 модуля удалены).
- ✅ Нет PR-CI — закрыто F-9 (`python-ci.yml`).
- ✅ `CLAUDE.md` cargo/rust — закрыто Sprint 5.1 (переписан под Python/pytest).

---

## 5. Стратегия: MCP и сервисы Anthropic — как это повлияет на проект

Сейчас бот использует Anthropic **минимально**: один `client.messages.create()` на отчёт, без кеша/инструментов/MCP/батча. Это и «дешёвая» победа по стоимости, и стратегический рычаг. Ниже — по приоритету влияния.

### 5.1 ✅ Prompt Caching — ВНЕДРЕНО (Sprint 2.4) · экономия до ~90% на префиксе
`SYSTEM_PROMPT.md` (~25 КБ) + RAG-контекст отправляются **целиком на каждый** запрос (а на пользователя их два — base и deep). Кешируя стабильный префикс (system + RAG), вы платите за него ~0.1× при повторных чтениях.
- **Эффект:** base+deep одного юзера и повторные прогоны → кеш-хиты; на объёме это кратно режет LLM-косты.
- **Как:** `cache_control: {type: "ephemeral"}` на последнем блоке system (и на RAG-блоке), волатильные данные (тикеры/дата) — после брейкпоинта. Проверять `usage.cache_read_input_tokens`.
- **Поддержка:** Haiku 4.5 и Sonnet 4.6 — мин. кешируемый префикс 4096 / 2048 токенов соответственно.

### 5.2 ✅ Structured Outputs — ВНЕДРЕНО (Sprint 2.5) · костыль `_repair_truncated_json` убран из пути
Сейчас нарратив возвращает JSON, который чинится вручную (`_repair_truncated_json`, обрезка по `max_tokens`). `output_config: {format: {type: "json_schema", schema: …}}` (через `client.messages.parse()` с Pydantic) **гарантирует** валидную схему.
- **Эффект:** удалить repair-хак, исключить класс «битый JSON → пустой отчёт».
- **Поддержка:** Haiku 4.5 / Sonnet 4.6 — да.

### 5.3 🔵 MCP — два направления (главный стратегический выбор)
**Model Context Protocol** — открытый стандарт Anthropic для подключения LLM к инструментам/данным. Для этого проекта он работает в **обе стороны**:

**(A) Бот как MCP-КЛИЕНT (потреблять данные).** Заменить/дополнить кастомные интеграции (Tradernet/FRED/SEC-EDGAR) готовыми финансовыми MCP-серверами (Alpha Vantage, Financial Modeling Prep, Financial Datasets, FactSet). Либо дать Claude доступ к свежим котировкам прямо в нарративе через `mcp_servers` (MCP-коннектор в Messages API) или SDK-хелперы `anthropic.lib.tools.mcp`.
- **Эффект:** шире покрытие активов/фундаментала меньшими силами; нарратив может «дотягивать» свежие факты.
- **Риск:** не отдавать LLM торговые/исполнительные MCP (Alpaca) без жёсткого human-in-the-loop.

**(B) Бот как MCP-СЕРВЕР (отдавать аналитику) — это и есть ваш ров.** Обернуть MAC3-движок (Эйлер-декомпозиция, Bootstrap-CVaR, Black-Litterman, 4-pillar, стресс) в MCP-сервер: тогда «посчитай мой CVaR/риск-вклад/стресс» становится инструментом, доступным Claude Desktop, корпоративным агентам и самому боту. **Готовых MCP-серверов институционального риск-разложения на рынке нет** — данные коммодитизированы, аналитика нет (§6).

### 5.4 🔵 Выбор модели и thinking
- **DEEP-тир** для институционального нарратива можно поднять до **Opus 4.8** (`claude-opus-4-8`, $5/$25, 1M ctx) с **adaptive thinking** (`thinking:{type:"adaptive"}`, `effort:"high"`) — заметный прирост качества рассуждений. ⚠️ На Opus 4.7/4.8 `temperature` и `budget_tokens` **удалены** (вернут 400) — убрать их при миграции (см. migration-гайд).
- **BASE-тир** оставить Haiku 4.5 ($1/$5) — быстро/дёшево.

### 5.5 🔵 Batch API — для офлайн-генерации отчётов
Отчёты складываются по датам (`r/<uid>/<date>/base|deep.html`) — это похоже на расписание. Несрочную генерацию (плановые/ночные прогоны) гнать через **Batch API** = **−50%** к стоимости токенов.

### 5.6 🟣 Claude Agent SDK / Managed Agents — стратегический горизонт
Если продукт двинется в сторону «спроси портфель диалогом / автономный ребаланс-ассистент», **Managed Agents** дают серверный agent-loop, песочницу, MCP-коннектор и **vaults** (хранилище MCP-кредов с авто-refresh — секреты не попадают в песочницу). Сейчас избыточно (бот — это «движок→адаптер→шаблон»), но это естественный путь к agentic-UX.

---

## 6. Уникальность продукта на фоне MCP/сервисов

**Вывод: данные — коммодити, аналитика и регион — ров.**

- **Чего на рынке МНОГО (MCP-серверы данных/исполнения):** Alpha Vantage, FactSet, FMP, Financial Datasets, Alpaca (исполнение), mcp-trader. Все они отдают **котировки/фундаментал/ордера** — это коммодитизированный «водопровод» для агентов.
- **Чего на рынке практически НЕТ и что есть у вас (ров):**
  1. **Институциональное риск-разложение «из коробки»** — Эйлер-TRC, Bootstrap-CVaR (Politis-Romano), Black-Litterman, 4-pillar scoring, многофакторная Ledoit-Wolf ковариация, **Base Currency Approach** (FX в цены до ковариации), стресс с выпуклым ограничителем, regime-детект, **CoVe data-lineage**. Off-the-shelf MCP этого не делают.
  2. **Региональная ниша:** Freedom Broker / Tradernet, KZ-рынок (KASE/AIX), **русскоязычный институциональный нарратив**, KZT/мульти-валютная корректность.
  3. **Доставка:** Telegram + банкер-grade HTML-отчёт с провенансом — низкий порог входа для розницы/private-wealth, недостижимый «голым» MCP-сервером.
  4. **Мнениеобразующая методология:** мандатные пороги, контроль концентрации, дисциплина токеномики.

**Стратегическая рекомендация:** не конкурируйте с дата-MCP — **потребляйте** их (§5.3A) ради покрытия, и **экспонируйте** свой риск-движок как MCP-сервер (§5.3B). Ваша дифференциация = *аналитический слой × регион × доставка × методология*, а не данные.

---

## 7. Roadmap

**Спринт A — LLM-стек (быстрый ROI, низкий риск):**
1. Prompt caching system+RAG (§5.1) — мерить `cache_read_input_tokens`.
2. Structured outputs (§5.2) — убрать `_repair_truncated_json`.
3. Подготовить миграцию DEEP→Opus 4.8 (убрать `temperature`/`budget_tokens`, adaptive thinking) — за флагом.

**Спринт B — данные/качество:**
4. Свести §4.1 (Tech 55% vs 80.8%) к единому базису + тест паритета тиров.
5. Batch API для плановых отчётов (§5.5).
6. PoC «бот как MCP-сервер» — обернуть `analyze_all`/CVaR/стресс (§5.3B).

**Спринт C — гигиена/инфра:**
7. Pin зависимостей (`pip-compile --require-hashes`) + Dependabot.
8. PR-CI `pytest` в GitHub Actions; обновить `CLAUDE.md` (убрать cargo/rust).
9. Вычистить ~32 инертных модуля «порта»; для GA — денежный леджер в Firestore/Cloud SQL.
10. Закрыть хвост §4.3 (marginal_var ренормировка, единицы expected-return).

---

*Документ — живой; обновлять при каждом значимом деплое и новом наборе прод-отчётов.*
