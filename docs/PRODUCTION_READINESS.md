# PRODUCTION_READINESS.md — готовность к выкатке пользователям Freedom Broker

> **Дата:** 2026-07-05 · **База оценки:** живые отчёты BASE/DEEP 07-04…07-05, AUDIT.md §−16…−0.5,
> код на `main`+ветка (pytest 552+, Playwright-верификация), сканы `freedom_portfolio/`,
> `db_tokenomics`, `gatekeeper`, `cloudbuild`.
> **Оценщик:** строгий режим — оценки занижаются при любом недоказанном свойстве.

---

## Вердикт (TL;DR)

| Сценарий | Готовность |
|---|---|
| **Закрытая бета** (десятки пользователей, whitelist, бесплатно/ручные токены) | ✅ **ГОТОВ** — можно выпускать сейчас |
| **Открытый запуск клиентам Freedom Broker** (сотни-тысячи, самообслуживание, деньги) | ❌ **НЕ ГОТОВ** — 5 блокеров (см. §Блокеры) |

Общая оценка: **6.9/10** — сильное аналитическое ядро с образцовой честностью данных,
упирающееся в потолок одиночного инстанса, отсутствие монетизации и эксплуатационной
наблюдаемости.

---

## Оценки по фундаментальным частям

| # | Подсистема | Оценка | Одной строкой |
|---|---|---|---|
| 1 | Квант-ядро (риск/факторы/режим/BL/стресс) | **8.5/10** | математика доказана на живых данных; нет бэктеста сигналов |
| 2 | Слой данных (Tradernet/SEC/FRED/CDS/FX) | **7.5/10** | QualityGate+CoVe образцовые; CDS — прокси, один брокер-источник цен |
| 3 | Интеграция Freedom API | **6/10** | надёжное чтение; торговли НЕТ — Action Plan не исполняется |
| 4 | RAG + LLM-нарратив | **7.5/10** | жив в проде (20 отчётов/2106 чанков), аудит цитат; ручная курация KB |
| 5 | Отчёты (Premium UI) | **8/10** | мобайл 0-overflow верифицирован; только RU; PDF = HTML-ссылка |
| 6 | Telegram-бот (флоу/UX) | **6.5/10** | флоу аудирован, graceful shutdown; хендлеры без e2e-тестов |
| 7 | Биллинг / токеномика | **4/10** | учёт токенов надёжен (SIGKILL-тесты), но ПЛАТЕЖЕЙ НЕТ вообще |
| 8 | Безопасность / комплаенс | **6.5/10** | Fernet-vault+ротация, PII-маскинг, hash-lock; юр.ревью ИИР нет |
| 9 | Инфра / CI/CD | **7/10** | деплой-гейт с тестами в образе; НО max-instances=1 by design |
| 10 | Наблюдаемость / эксплуатация | **4/10** | отличные runbooks, но ноль алертов/метрик/error-tracking |

### 1. Квант-ядро — 8.5/10
**Доказано:** EWMA⊕Ledoit-Wolf ковариация, Euler/MCTR (Σ=100% на live), Politis-Romano CI,
факторная декомпозиция (82.1+17.9≈100), 4-Pillar (F+V+T+C==total, max|Δ|=0.000), Growth×Cycle
режим с macro-overlay (GDP/безработица/инфляция, level⊕темп, ±0.05), BL (τ=0.05, Идзорек),
7 стресс-шоков с выпуклым капом, Base Currency USD + RFR 4.50% без mismatch, детерминизм.
Мультиколлинеарность диагностируется (κ=9.8, ортогонализация ON).
**Честный минус:** сигналы 4-Pillar/BL/идеи **не бэктестированы** — нет ни одного
исторического доказательства, что «Buy/Trim» добавляет альфу; продаётся как аналитика, не как
альфа — это корректно задисклеймлено, но для брокерского продукта это вопрос №1 от компетентного
клиента. Прокси-факторы в 2 из 7 стрессов (нет UUP/TIP).

### 2. Слой данных — 7.5/10
**Доказано:** per-source QualityGate (свежесть по каденции серии + sanity-диапазоны), CoVe на
24 строки — каждый инпут с методом и статусом, FX T-1 с покрытием, SEC-возраст 10-K
warn/stale-пороги, sparse-history дроп с раскрытием.
**Минусы:** цены только Tradernet (нет второго источника/кросс-чека котировок — при сбое брокера
слепнем целиком); CDS — HY-прокси, не настоящие спреды эмитентов; 730d окно ограничивает
длинные метрики; RAG-курация вручную.

### 3. Freedom API — 6/10 (главный разрыв ожиданий)
**Есть:** портфель (`get_portfolio`), user info, история через Tradernet, websocket-подписка,
Cloudflare-retry, ключи в Fernet-vault с ротацией MultiFernet, флоу подключения в боте.
**Нет (жёстко):** исполнения ордеров — «Action Plan» с ценами Buy/Sell/Stop и количествами
это **read-only советы**, пользователь исполняет руками; нет idempotent-ресинка позиций по
websocket в рантайме отчёта; нет обработки корпоративных действий (сплиты/дивиденды в PnL —
от брокера as-is); нет мульти-счетов. Для «advisor» это допустимо, но продукт называется
Action Plan — ожидание исполнения будет.

### 4. RAG + LLM — 7.5/10
**Есть:** ChromaDB (semantic 0.6 ⊕ recency 0.4), секционный чанкинг, метаданные
bank/section/tickers, boot-ingest fallback (обходит хрупкий Cloud Function/Eventarc), инвентарь
базы в CoVe, чекер «ИИ-цитирование» (память модели vs проверенные [RAG]-цитаты),
Structured Outputs (гарантированный JSON), prompt-caching, guard-фильтры (held/противоречия/
stress-cap/leverage-phrasing), grace-обрезка без обрывов мысли. Прод: 20 отчётов, 87 отрывков.
**Минусы:** курация KB — ручная (нет расписания обновлений, нет контроля прав на PDF банков —
юр.риск перепечатки выдержек!); стоимость DEEP на Opus ≈ $0.3–0.7/отчёт; латентность LLM
30–60 c; кросс-язычность retrieval (EN-чанки ↔ RU-запросы) не измерена.

### 5. Отчёты (Premium UI) — 8/10
**Доказано:** 0px overflow на 390/360px (Chromium), честные чекеры/легенды, leverage-правило
(маржа только при cash<0), signed URL 48ч, все секции согласованы (waterfall идентичен
BASE↔DEEP).
**Минусы:** «Скачать PDF» отдаёт HTML, а не PDF; только русский; тёмной темы нет; доступность
(a11y) не проверялась.

### 6. Telegram-бот — 6.5/10
**Есть:** аудированный флоу connect→analyze→report, WhitelistMiddleware (бета-гейт),
graceful shutdown без 409, PII-маскинг в логах, admin `/grant`.
**Минусы:** хендлеры aiogram **не покрыты e2e-тестами** (вся пирамида тестов — payload/математика);
нет per-user rate-limit (whitelist компенсирует только в бете); один админ; тексты ошибок местами
технические.

### 7. Биллинг — 4/10 (блокер монетизации)
**Есть:** честный токен-учёт на SQLite (gcsfuse-hardening: DELETE-journal, SIGKILL-тесты
атомарности), deferred-billing логика, admin-начисление.
**Нет:** платёжного шлюза ВООБЩЕ (ни Stars, ни карты, ни инвойсов) — выручка невозможна без
ручного вмешательства; нет тарифов/квот; SQLite на gcsfuse — осознанный компромисс, который
**навсегда фиксирует max-instances=1** (см. §9).

### 8. Безопасность / комплаенс — 6.5/10
**Есть:** брокер-ключи в Fernet (MultiFernet-ротация, H-7), маскинг PII, изолированный
ingest-бакет (M2: не reports!), hash-locked зависимости + blocking pip-audit (принятый
aiohttp-класс задокументирован), prompt-injection обёртки `<untrusted_data>` для RAG/брокер-данных,
дисклеймеры «не ИИР» во всех отчётах и CoVe.
**Минусы (строго):** дисклеймеры не проходили **юридическое ревью** (ЦБ РК/РФ — инвест-советник?);
права на банковские PDF в RAG не оформлены; секреты — env-vars Cloud Run (Secret Manager не
подтверждён); нет аудита действий админа; нет политики удаления данных пользователя (GDPR-like).

### 9. Инфра / CI/CD — 7/10
**Есть:** Cloud Build: build→test-в-образе→push→deploy(+фолбэк-ревизии Cloud Run), CI GitHub
(lock-consistency, pip-audit), деплой-гейт ловит реальные регрессии (доказано e24e8e9).
**Минусы:** **max-instances=1 · concurrency=1 · 1 CPU** — потолок ≈1 одновременный анализ;
DEEP занимает десятки секунд CPU+LLM → очередь при >5 активных пользователях; fail-soft шаг
Cloud Function маскирует сбои деплоя; staging-среды нет; канареек/автоотката по метрикам нет.

### 10. Наблюдаемость — 4/10
**Есть:** структурные логи (gatekeeper GK-коды, RAG boot-ingest, sync), CoVe как
«наблюдаемость данных» в самом продукте — это сильно; runbooks (RAG_TROUBLESHOOTING).
**Нет:** error-tracking (Sentry), метрик (латентность отчёта, токен-расход, LLM-ошибки),
алертов (упавший бот заметит только пользователь), SLA/SLO, дашборда здоровья.

---

## Блокеры открытого запуска (по приоритету)

| P | Блокер | Подсистема | Оценка усилий |
|---|---|---|---|
| P0 | Платёжный шлюз (Telegram Stars или карта) + тарифы | Биллинг | 1–2 нед |
| P0 | Масштаб: вынести состояние из SQLite/gcsfuse (Cloud SQL/Firestore) → снять max-instances=1; либо очередь заданий (Cloud Tasks) с честным «отчёт готовится ~2 мин» | Инфра | 2–4 нед |
| P0 | Алертинг + error-tracking (минимум: Sentry + uptime-check на health + алерт на 5xx/исключения) | Наблюдаемость | 2–4 дня |
| P1 | Юр.ревью: статус «не ИИР» в юрисдикции клиентов Freedom (РК/РФ), права на выдержки банковских PDF | Комплаенс | внешнее |
| P1 | e2e-тесты критичных хендлеров бота (connect, analyze, оплата) | Бот | 3–5 дней |
| P2 | Бэктест-валидация 4-Pillar/идей — **план готов: `ROADMAP_SCENARIO_TIER.md`** (zero-LLM walk-forward тир по кнопке) | Квант | 2–3 нед |
| P2 | Настоящий PDF-экспорт; второй источник котировок — **план готов: `ROADMAP_DATA_RESILIENCE.md` §1** (yfinance chain-fallback за флагом) | Отчёты/Данные | 1 нед |

## ⟳ Обновление 2026-07-05 (Фаза 4)

- **Закрыто кодом:** инфляционный сигнал добавлен в ОБА чекера режима (ИИ-сверка: обязательный
  7-й чекпоинт breakeven level⊕темп; детерминированная R3: де-анкоринг >3% = стресс);
  режим-специфичные RAG-выдержки теперь подаются В ПРОМПТ (`regime_rag_confirm` →
  fenced-блок + binding-правило «опираться на выдержки, память — дополнение»); чипы очищены
  от tmp-имён/markdown; lookback параметризован `HISTORY_LOOKBACK_DAYS` (default 730,
  мост к 5-летнему окну).
- **Планы внедрения (детальные шаги):** `ROADMAP_SCENARIO_TIER.md` (бэктест-тир, 0 LLM-API),
  `ROADMAP_DATA_RESILIENCE.md` (yfinance-fallback · демо-режим · выключение CDS-прокси
  env-флагом БЕЗ правки математики — `credit_score` уже деградирует gracefully · lookback
  730→1825 с NaN-маскированием), `LLM_STRATEGY_MULTILINGUAL.md` (DEEP→Sonnet как главная
  экономия −40–50%; мульти-вендор отклонён в пользу «Быстрый/Глубокий»; кросс-язычный RAG:
  EN-запросы — инвариант, query-translation и bge-m3 — по этапам).
- Оценки подсистем не повышаются до внедрения планов (строгий режим).

## ⟳ Обновление 2026-07-16 (multi-user, персистентность, бенчмарк)

Пересмотр после серии инцидент-фиксов (AUDIT §−21…−23) и инфраструктурного отката.

**Закрыто:**
- **Потеря данных «2-й юзер → демо»** — устранена и в инфре, и в коде:
  - инфра: сервис переведён на **персистентный GCS-том** (`/mnt/state`, gcsfuse,
    `TOKENOMICS_DB_PATH`/`VAULT_DB_PATH`), Cloud SQL/Postgres удалён (экономия) —
    профили/балансы/ключи переживают рестарты;
  - код: `connection_mode` вынесен в таблицу `user_connection` (UPSERT, не теряется),
    recovery по наличию ключей в vault (само-лечение режима), «демо по умолчанию»
    исключено (неопределённый источник не строит платный отчёт).
- **Биллинг «случайного» демо** — демо-отчёты бесплатны (`_effective_cost`=0),
  списание только за однозначно живой источник; явное демо помечено в UI.
- **Наблюдаемость источника портфеля** — `PORTFOLIO SOURCE`/`KEY SOURCE`/
  `CONN MODE RECOVERED` логи (инцидент 14.07 теперь диагностируем из логов).
- **Бенчмарк отчёта** — линия equity-curve следует выбранному бенчмарку
  (был баг: всегда S&P 500 независимо от выбора; §−23).
- **Сервисный ключ** — не подставляется чужому юзеру (admin-only), закрыт
  латентный cross-account риск; удалён мёртвый код (`_build_analysis_payload`).
- **Cloud Run Logs Check** workflow — выгружает все логи за период в артефакт.

**Пересмотр оценок:**
- Биллинг/токеномика 4→**5/10** (учёт стал брокер-агностичным и честным по демо;
  платёжного шлюза по-прежнему НЕТ — P0 остаётся).
- Telegram-бот 6.5→**7/10** (multi-user изоляция доказана; +тесты phase29/30).
- Наблюдаемость 4→**4.5/10** (source-логи + logs-workflow; Sentry/метрик всё ещё нет).
- Инфра 7/10 — без изменения: данные теперь durable, но **`max-instances=1`
  остаётся by-design** (SQLite один писатель) — потолок конкуррентности не снят.

**Общая оценка: 6.9 → ~7.2/10.** Вердикт по сценариям НЕ меняется: закрытая бета —
✅ готов; открытый запуск — по-прежнему упирается в P0 (платежи, масштаб, алертинг).

**Новый трек (спроектирован, не начат):** `ROADMAP_IBKR_INTEGRATION.md` — второй
брокер (Interactive Brokers) через абстракцию `BrokerConnector`; предусловие —
второй источник котировок и подтверждение доступа к IBKR OAuth Web API.

## Что уже prod-grade (не трогать, беречь)

- Математическое ядро и его **проверяемость**: CoVe/integrity — редкая для ритейл-продуктов
  честность (инвентарь RAG, происхождение каждой цифры, статусы источников).
- Дисциплина поставки: hash-lock, деплой-гейт с тестами в образе, 552 теста, живые аудиты
  по каждому релизу (AUDIT.md §−16…), Do-No-Harm-практика.
- Устойчивость к сбоям данных: graceful-деградация каждого источника без падения отчёта.

## Рекомендуемая последовательность к GA

1. Закрытая бета на whitelist (сейчас) → собрать латентность/стоимость на реальных юзерах.
2. P0-тройка (платежи, масштаб/очередь, алертинг) — параллельно юр.ревью (P1).
3. Расширенная бета (сотни, платная) → метрики удержания.
4. P2 (бэктест/позиционирование, PDF) → публичный запуск.

---
*Отчёт сгенерирован в рамках аудит-раунда §−16/−17; факты перепроверяемы по указанным файлам
и живым отчётам. Оценки намеренно консервативны.*
