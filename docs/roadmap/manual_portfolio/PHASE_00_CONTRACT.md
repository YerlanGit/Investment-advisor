# Фаза 0 — Контракт: снятие противоречий задания

<!-- nav | area:roadmap | code:(документ-контракт, кода нет) | read-before:перед стартом ЛЮБОЙ фазы ручного портфеля -->

| | |
|---|---|
| **Соответствие заданию** | новая фаза (в задании отсутствует) |
| **Зависимости** | нет |
| **Оценка** | 0.5 дня |
| **Артефакт** | правки только в документах; кода не пишется |

---

## 1. Зачем

Задание `RAMP_ClaudeCode_Task_v2.md` в пяти местах требует взаимоисключающего
(разбор — `README.md §2`). Если начать с Фазы 1, противоречия всплывут в код-ревью
Фазы 2 (К-1) и Фазы 3 (К-4) — то есть после того, как работа уже сделана.
Фаза 0 стоит полдня и снимает их до первой строки кода.

**Выход фазы — согласованный набор инвариантов**, который дальше цитируется всеми
фазами дословно и больше не обсуждается.

---

## 2. Инварианты в исправленной редакции

Ниже — итоговая редакция. Изменённое относительно задания помечено **[изм.]**
с указанием причины.

| # | Инвариант | Автопроверка |
|---|---|---|
| **I-1** | Отчёт `manual` структурно идентичен `freedom`: тот же набор секций, тот же порядок, те же расчёты | Diff набора секций HTML на одинаковом портфеле из двух источников = ∅ |
| **I-2** **[изм.]** | Ниже `DataFrame[Ticker, Quantity, Purchase_Price]` не меняется ни один модуль: `pdf_payload.py`, `html_renderer.py`, `ai_narrative.py`, `scoring.py`, `scoring_orchestrator.py`, `stress.py`, `regime.py`, `black_litterman.py`. **`investment_logic.py` из списка исключён** | `git diff --stat` по восьми путям = 0 |
| **I-2b** **[нов., уточнён 2026-07-27]** | `investment_logic.py` меняется **только** в четырёх поимённо названных местах: `MAC3RiskEngine.__init__` (добавление параметра `price_source` с дефолтом), `_get_tradernet_client` → `_get_price_provider`, `get_market_data` (вызов провайдера), `UniversalPortfolioManager.__init__`/`prefetch_market_data` (проброс источника). Вся математика — `calculate_structural_risk`, `analyze_all` и ниже — без изменений | I-5 + ревью `git diff` по списку функций. **Прежняя редакция ограничивала диапазоном строк 653–772 — отброшена: номера строк едут от первой же вставки, и проброс источника до конструктора в них не помещается** |
| **I-3** | `demo` остаётся бесплатным шаблоном, `_mock_portfolio()` не трогать | Существующие тесты демо зелёные без правок |
| **I-4** **[изм.]** | `manual` тарифицируется как `freedom` | `_effective_cost("deep", "manual") == TIER_COST["deep"] == 2` (было: несуществующий `_price_for` и ключ `"DEEP"` — ошибки E-1/E-2) |
| **I-5** | Путь `freedom` не деградирует | Снапшот-тест: тот же портфель до/после ветки → числа побитово равны |
| **I-6** | `KNOWN_SPLITS` и `_detect_and_adjust_splits` остаются включёнными для всех провайдеров; они идемпотентны | Тест: скорректированный ряд на входе → выход равен входу |
| **I-7** | Одна конвенция корректировок в одной ковариационной матрице | `test_mixed_convention_rejected` |
| **I-8** | Секреты не попадают в логи, в текст ошибки пользователю и в лог URL | grep по новым `logger.*`; тест на маскирование `apikey` |
| **I-9** | При `MANUAL_PORTFOLIO_ENABLED=off` поведение бота идентично текущему | E2E-прогон существующих команд при выключенном флаге |
| **I-10** **[нов.]** | Ни один чекер данных не блокирует отчёт, который успешно строился до ветки | Профили строгости (§3) + снапшот-набор портфелей из `LEGACY`-профиля |
| **I-11** **[нов.]** | `ProviderResult` duck-совместим с `HistoryResult`: поля `data, loaded, failed, retried` в том же порядке и с той же семантикой; `convention` — строго последним | Тест: 6 модулей-потребителей (`pdf_payload`, `scenario_report`, `portfolio_series`, `data_lineage`, `tg_bot`, `investment_logic`) работают на обоих типах |
| **I-12** **[нов. 2026-07-27, юридический]** | **Ни один байт данных Tradernet не участвует в отчёте `manual`** — ни цены позиций, ни 10 факторных ETF, ни 3 бенчмарка, ни ATR, ни fallback-звено цепочки. Провайдер-цепочка `manual` не содержит `TradernetProvider` ни в одном звене | `test_manual_chain_has_no_tradernet` (§4) — проверяет состав цепочки И то, что `TradernetClient` не инстанцируется за время сборки ручного отчёта |

**Не-цели** (без изменений от задания): миграция на total-return; отключение Tradernet;
Redis, микросервисы, PDF, очереди; изменения токеномики сверх I-4.

---

## 3. Профили строгости чекеров (снятие К-4)

Причина — находка **F-5**: движок сегодня штатно переживает потерю части факторов
и работает на окне от 30–60 наблюдений. Чекеры блока C в редакции задания строже
в 4–8 раз и, будучи включёнными для всех источников, заблокируют отчёты,
которые сегодня строятся.

Вводится `CheckProfile` с двумя значениями. Полные таблицы порогов — `PHASE_03 §3`;
здесь фиксируется только принцип и разделительная линия.

| | `LEGACY` (источник `freedom`, `demo`) | `STRICT` (источник `manual`) |
|---|---|---|
| Смысл | «не хуже, чем сегодня» | «данных достаточно для честной математики» |
| C-1 (13 инструментов) | BLOCK только при потере **рыночного фактора** (`SPY.US`) **или** при потере всех факторов; остальное — DEGRADE | BLOCK при потере любого из 10 факторных ETF; потеря `QQQ/AGG/URTH` (бенчмарки, не факторы) — DEGRADE |
| C-2 (окно) | BLOCK при `< max(2·K, 30)` — ровно порог `investment_logic.py:931` | BLOCK при `< 252` |
| C-5 (`T ≥ 3N`) | DEGRADE | BLOCK |
| C-6 (PD-матрица) | DEGRADE (движок и так делает PSD-проекцию) | BLOCK |
| Блоки A, B | одинаково в обоих профилях | одинаково |

**Правило по умолчанию:** источник, не объявивший профиль явно, получает `LEGACY`.
Иначе любой забытый вызов автоматически ужесточает поведение — ровно та ошибка,
которую I-10 запрещает.

---

## 3a. Юридическая граница источников (решение 2026-07-27)

Использовать персональный/сервисный API-ключ Freedom Broker (Tradernet) для загрузки
и показа рыночных данных пользователям, **не являющимся клиентами брокера**, запрещено.
Это не приоритетное, а **архитектурное** ограничение: оно определяет, какой провайдер
допустим для какого источника портфеля, и потому вынесено в инвариант I-12.

### 3a.1 Матрица «источник портфеля → допустимый провайдер»

| Источник | Пользователь | Основной провайдер | Fallback | Обоснование |
|---|---|---|---|---|
| `freedom` | клиент брокера (доказано наличием ключей в vault) | Tradernet | Stooq — **допустим** | Данные брокера идут клиенту брокера |
| `manual` | статус неизвестен и **неверифицируем** | **Stooq** | ❌ **Tradernet запрещён** | Нельзя доказать, что пользователь — клиент |
| `demo` | кто угодно, включая не-клиентов | Tradernet (сегодня) | — | ★ Тот же класс проблемы, см. §3a.3 |

**Почему для `manual` нет исключения для клиентов брокера.** Технически можно было бы
разрешить Tradernet тем manual-пользователям, у кого есть ключи в vault. Отвергнуто:
(1) наличие ключа доказывает привязку счёта, но не его актуальность; (2) это породило бы
**два разных источника цен для одной и той же фичи**, то есть два набора чисел
в одинаковых на вид отчётах и невоспроизводимую поддержку; (3) конвенции источников
могут различаться → нарушение I-7 внутри одного тира. Один источник для `manual` — всех.

### 3a.2 Граница проходит по всей ценовой матрице, а не по позициям пользователя

Ошибка, которую легко допустить: «позиции грузим из Stooq, а факторные ETF — как раньше».
Так нельзя по двум независимым причинам:

1. **Юридически** — факторные ETF грузятся тем же сервисным ключом Tradernet и точно так же
   участвуют в отчёте, который видит не-клиент. Запрет распространяется на них полностью.
2. **Математически** — `SPY.US`, `MTUM.US`, …, `URTH.US` входят в ту же ковариационную
   матрицу, что и позиции (`calculate_structural_risk`: `needed_cols = [*factor_tickers.values(),
   *resolved_assets]`). Разные источники в одной матрице — прямое нарушение I-7.

Следовательно, для `manual` **из Stooq грузятся все 13 обязательных инструментов**
(10 факторных + 3 бенчмарка) плюс тикеры пользователя. Отсюда критичность проверки
покрытия в Фазе 8: непокрытый фактор-ETF означает, что `manual` не запускается вообще,
а не что «одним фактором меньше».

### 3a.3 Демо-режим — незакрытая часть той же проблемы

`demo` сегодня строится на живых данных Tradernet под сервисным ключом и доступен любому,
включая не-клиентов (`broker_api.py:206-210` → `_mock_portfolio`, далее обычный
`get_market_data`). Формально это тот же запрет, что и для `manual`.

**В объём этой ветки не входит** (I-3 замораживает демо), но зафиксировано как ★ в
`README.md §7`. Практическое следствие для плана: демо-тикеры (`AAPL`, `KSPI.KZ`,
`BTC-USD`) включены в проверку покрытия Фазы 8 — чтобы решение «перевести демо на Stooq»
можно было принять по данным, а не гадать.

---

## 4. Прочие решения фазы

| Решение | Формулировка | Снимает |
|---|---|---|
| **Источник в отчёте** | Показывается строкой CoVe: `data_lineage._tradernet_status` → `_price_source_status`, подпись источника динамическая. Бейдж в шапке отчёта — **вне объёма ветки**, отдельным PR со снятием заморозки `pdf_payload.py` | К-2, F-6 |
| **E-2 (равенство метрик)** **[обновлено 2026-07-27]** | Два разных теста: **(а) контрактный офлайн** — один портфель через `FreedomConnector` и через ручной ввод на **одной и той же** провайдер-фикстуре → метрики побитово равны (проверяет слой сборки DataFrame); **(б) живой** — `manual` (Stooq) против `freedom` (Tradernet) по порогам §8.6 задания (проверяет источник). Конфигурации «manual на Tradernet в проде» больше не существует | К-3 |
| **Кэш в валюте** | Конвертируется в слое `manual` **до** передачи DataFrame движку. `analyze_all` не трогается. Валюта без курса — позиция исключается с явным сообщением | К-5, F-2 |
| **Порядок фаз** **[обновлено 2026-07-27]** | Stooq — на критическом пути запуска: данные Tradernet не-клиентам **запрещены** (решение владельца), `manual` в проде работает только на Stooq. Ключ добывается в день 1; Фазы 4–7 разрабатываются параллельно на офлайн-фикстурах; «конвенция раньше провайдера» сохранено (8 → 9). Откат `manual` — только `MANUAL_PORTFOLIO_ENABLED=off`, не Tradernet | E-6, F-1 + решение 2026-07-27 |
| **Дата сплита GOOGL** | В процедуре §2.5 использовать **2022-07-18** (`KNOWN_SPLITS`), а не `2022-06-06` (это AMZN) | E-4 |

---

## 5. Базовая линия тестов — замер этого прохода

Замерено на `42ca97f` + пустая ветка, `python -m pytest tests/ -q`, `PYTHONPATH=src`:

```
782 passed, 5 skipped, 1 xfailed, 2 subtests passed in 105s
```

Все 5 пропусков — `tests/test_phase22_rag_boot.py`, причина `google-cloud-storage
not installed` (артефакт локального окружения, в CI пакет присутствует).
`782 + 5 = 787` — **базовая линия `CLAUDE.md` «787 passed, 1 xfailed» подтверждена.**

Каждая фаза обязана предъявить `787 + N passed, 1 xfailed`, где `N` — число новых тестов
фазы. Просадка ниже 787 — гейт не пройден, независимо от причины.

---

## 6. Гейт выхода фазы

- [ ] `README.md` этой папки и все `PHASE_*.md` ссылаются на исправленные I-1…I-11
- [x] Право на данные сервисного ключа — **закрыто 2026-07-27: запрещено** →
      Stooq обязателен для `manual` (`README.md §4`)
- [x] Порядок фаз решён 2026-07-27: Stooq на критическом пути, ключ добывается
      в день 1; Фазы 4–7 — параллельно на офлайн-фикстурах
- [ ] Владелец продукта закрыл остальные пункты **★** из `README.md §7`
      (F-2 на freedom-пути; бейдж; отчётов на пользователя; демо на данных
      Tradernet для не-клиентов)
- [ ] `docs/INDEX.md §5` пополнен ссылкой на `docs/roadmap/manual_portfolio/`

Кода в фазе нет — прогон тестов не требуется; базовая линия §5 зафиксирована.
