# Manual Portfolio + миграция источника цен — разбор задания и план фаз

<!-- nav | area:roadmap | code:src/finance/price_providers.py,src/finance/data_checks.py,src/finance/manual_portfolio.py,src/freedom_portfolio/history.py,src/tg_bot.py,src/freedom_portfolio/client.py | read-before:прежде чем трогать ручной ввод портфеля, провайдеры цен или чекеры данных -->

> **Что это.** Разбор задания `RAMP_ClaudeCode_Task_v2.md` (ревизия v2, 2026-07-26) против
> фактического состояния кода на 2026-07-27 (ветка `claude/task-analysis-planning-q7e7a7`,
> база `42ca97f`) и разбиение работы на 11 фаз с отдельным md-файлом на каждую.
> **Витрина котировок из Freedom API выделена в ОТДЕЛЬНЫЙ проект** решением
> владельца 2026-08-09 → [`../freedom_warehouse/`](../freedom_warehouse/README.md).
> Здесь остаются 11 фаз: ручной ввод и **Stooq как его собственный источник**.
>
> **Статус на 2026-08-10 — ПЕРЕПРОВЕРЕН ПО КОДУ** (`AUDIT §−76`). Доставлено
> **6 фаз из 11**. Ручной ввод собран целиком до кнопки «Рассчитать», но в проде
> выключен флагом: провайдера цен для него нет до Фазы 09.
>
> 🔴 **Этот файл соврал в ТРЕТИЙ раз, и лечится это одним правилом.** Первый раз —
> «`C-6` не реализован» при работающем чекере (`§−69`). Второй — устаревший §2.5
> плана (`§−73`). Третий, найден 2026-08-10: строка Фазы 09 называла блокером
> «в пакете Stooq акции без ETF», хотя ETF лежат в **соседних подпапках** архива,
> и присланный владельцем `vgit.us.txt` сам является ETF. Общее у всех трёх — в
> статусе стояло УТВЕРЖДЕНИЕ вместо ссылки на перепроверяемый артефакт. Правило
> прежнее и теперь применено ко всем строкам таблицы: **статус обязан называть
> файл, функцию или команду, которой его можно опровергнуть за один шаг.**
>
> | Фаза | Артефакт в коде | Состояние |
> |---|---|---|
> | **00** Контракт, I-1…I-13 | — | ✅ документ; юридическая граница зафиксирована |
> | **01** Изоляция кэша | `freedom_portfolio/history.py` | ✅ **в проде**, сверено 2026-08-06: ключ `{provider}__{convention}__{ticker}_{N}d.pkl` (`_cache_path`), запись через `os.replace` (атомарна на POSIX), eviction по TTL |
> | **02** Протокол провайдера | `finance/price_providers.py` | ✅ **в проде**; I-12 fail-CLOSED (A-3): `_KNOWN_SOURCES = {freedom, demo, manual}`, неизвестный источник → `ProviderUnavailable` до сетевых вызовов; для `manual` дополнительно `_MANUAL_FORBIDDEN` и внятный отказ, пока Stooq не реализован |
> | **03** Чекеры A/B/C | `finance/data_checks.py` | ✅ **подключена 2026-08-02**: `check_price_matrix`, `check_portfolio_sufficiency` и `check_covariance_health` вызываются в конвейере, отказ без списания токена, DEGRADE условной строкой CoVe (`AUDIT §−49`). **`C-6` РЕАЛИЗОВАН** (`§6b`): `_last_psd_repair` пишется в `engine/risk_engine.py`, читается в `_stage_risk_model`. Блок A написан ДО своего потребителя ОСОЗНАННО — он живёт внутри HTTP/CSV-провайдера и включится Фазой 09 (`PHASE_03 §5`) |
> | **04** `ManualPortfolioSource` | `finance/manual_portfolio.py` | ✅ **ЗАКРЫТА 2026-08-06** (`§−70`, `§−71`): `parse_portfolio_text` → `ParseReport{valid, auto_resolved, failed}` · валюта сделки колонкой `Currency` (пересчёт за движком, −37) · `preflight_coverage` по кэшу без сети · уровни в валюте торговли (`AssetActionRow.*_local`) · строка CoVe «Ручной ввод» · golden-фикстура `results_golden_manual.json`. Шаг «экран подтверждения» относится к Фазе 05 |
> | **05** FSM-флоу | `tg_bot.py`, `db_tokenomics.py`, `finance/manual_portfolio.py` | ✅ **ЗАКРЫТА 2026-08-06** (`§−72`): `ManualPortfolio{Input,Confirm}` · `manual_portfolio_enabled()` (дефолт **off**) · третья кнопка `connect:manual` · ветка `manual` ДО проверки vault в `_resolve_portfolio_source` (F-4) · экран подтверждения `build_confirmation` (доли, кэш блоком, курс с датой, прокси отдельной строкой) · черновик `manual_portfolio_draft` · `user_slot` (Т-9). Перед фазой починены три дефекта MP-04 — см. `§−72` |
> | **06** Проводка источника | — | ⏳ |
> | **07** Загрузка файлом | — | ⏳ |
> | **08** Конвенция Stooq | `scripts/stooq_coverage_probe.py`, `docs/audit/STOOQ_CONVENTION.md` | 🟢 **US ПОКРЫТ 2026-08-09** (`§−74`, артефакт `§3.7`): группа A **10/10 — стоп-фактор запуска снят**, B/D/E/F закрыты; KZ-бумаг в Stooq нет. Качество проверено на трёх подряд днях: идемпотентно, 0 битых строк, 0 дублей. Семь классов ловушек измерены. ⛔ Остаётся конвенция корректировок — нужен архив ИСТОРИИ. 🔴 До прода проверить условия `/db/` |
> | **08.5** Хранилище котировок | `finance/{market_calendar,stooq_symbols,stooq_ingest,stooq_store}.py`, `scripts/stooq_ingest.py`, `tests/test_stooq_price_store.py` | 🟢 **СЛОЙ ДАННЫХ ДОСТАВЛЕН 2026-08-10** (`§−77`): 63 теста, 20 мутаций, живая проверка на трёх дневных срезах (24 077 баров, `C-1 10/10`, повтор идемпотентен). Рынок и валюта — колонки справочника, поэтому KZT/JPY/EUR/crypto добавляются строками карты суффиксов без миграции. ⛔ Провайдер (ЧК-08.5-4) ждёт замера конвенции: база честно хранит `convention='unmeasured'` |
> | **09** `StooqProvider` | — | ⏳ **архитектура сбора базы спроектирована 2026-08-09** (`PHASE_09 §9`), уточнена 2026-08-10 (`PHASE_08B`): бутстрап архивом истории + ежедневная дельта файлом `YYYYMMDD_d.txt` (701 КБ) → SQLite, бот читает ТОЛЬКО на чтение, сети на пути отчёта нет. ✅ Прежняя запись «блокер: в пакете акции без ETF» **ОТОЗВАНА 2026-08-10** — ETF лежат в соседних подпапках архива (`nasdaq etfs/`, `nyse etfs/`), а присланный `vgit.us.txt` сам является ETF (4 197 баров с 2009); в дневном срезе присутствуют все 10 факторов и 3 бенчмарка C-1 |
> | **10** EODHD | — | ⏳ |
>
> Что осталось непройденным (перепроверено 2026-08-06 по коду):
> `src/finance/stooq_provider.py` отсутствует, поэтому
> `provider_for_source("manual")` честно бросает `ProviderUnavailable`, а
> `MANUAL_PORTFOLIO_ENABLED` по умолчанию **выключен**. Кнопка «Рассчитать»
> ручного портфеля доводит до явного отказа без списания токена — это ГРАНИЦА
> ФАЗЫ, а не заглушка: проводка источника в расчёт — Фаза 06, и она требует
> Фазы 09.
>
> **Порядок работ пересмотрен аудитом:** до Фазы 4 требовалось закрыть A-1
> (склейка дубликатов позиций), A-2 (гард нечислового `Quantity`), A-3 (allowlist
> источников) и подключить Фазу 3 — **все четыре сделаны**. Парсер ручного ввода
> обязан опираться на эти чекеры, а не заводить свои копии.
>
> 🔴 **Почему этот файл разошёлся с кодом и что с этим сделано.** Прошлая
> редакция утверждала «`C-6` не реализован», ссылаясь на `PHASE_03 §6b` — а
> §6b уже был озаглавлен «✅ Чекер `C-6` РЕАЛИЗОВАН 2026-08-02». Документ
> противоречил сам себе, и вместе с чекбоксом-«🔴» в том же файле убеждал
> НЕ проверять код. Это тот же класс, что `AUDIT §−64`: устаревшее обоснование
> опаснее отсутствующего. Правило на будущее: **строка статуса в этой таблице
> обязана называть АРТЕФАКТ (файл, функцию, символ), по которому её можно
> перепроверить за одну команду** — тогда расхождение видно сразу.

---

## 0a. Где мы остановились и ПОЧЕМУ (сверено с кодом 2026-08-01)

**Фазы 4 и 5 закрыты 2026-08-06.** Все семь шагов Фазы 4 сделаны — седьмой
(экран подтверждения) закрыт вместе с Фазой 05.

> 🔴 **Ревизия MP-04 перед Фазой 05 нашла три молчаливых дефекта** (`§−72`), и
> это главный урок обеих фаз: парсер, признанный закрытым, клал в портфель
> ПРОКСИ вместо бумаги пользователя (AIX-нота становилась `VWOB` — цена ETF
> вместо цены покупки, чужой тип инструмента, склейка двух разных нот в одну),
> отвергал маржинальный заём как короткую продажу и спотыкался на разрядах в
> строке кэша (`CASH:KZT 500 000`). Все три чинились до первой строки Фазы 05.
> Правило на будущее: **фаза, стоящая на предыдущей, начинается с чтения
> предыдущей ПО КОДУ, а не с её галочки в roadmap.**

| Шаг Фазы 4 | Состояние |
|---|---|
| 1. H-1 — прокси неликвидных бумаг + раскрытие подмены | ✅ **сделано и подтверждено живьём** (отчёт 30.07: CoVe «FFSPC→VWOB», цена ноты 102.80 от брокера) |
| 1b. Контракт входного фрейма: склейка дубликатов + гард количества (A-1/A-2) | ✅ **сделано 2026-08-01** — предпосылка §3.2, реализована в движке для обоих путей |
| 2. Единый `TICKER_MAP` с KZ/AIX-синонимами (B-1) | ✅ **2026-08-06** (`§−70`): синонимы, включая кириллицу, дополнены в СУЩЕСТВУЮЩИЙ словарь движка; попутно починен латентный разрыв — маппинг шёл ПОСЛЕ прокси |
| 3. Парсер → `ParseReport{valid, auto_resolved, failed}` | ✅ **2026-08-06** (`§−71`): `finance/manual_portfolio.py` |
| 4. Конверсия валюты сделки (A-2) до склейки дубликатов | ✅ **2026-08-06** (`§−71`): парсер проставляет `Currency`, пересчёт делает движок (−37). Требование «до склейки» выполнено гардом: смешение валют внутри тикера отвергается, а не усредняется |
| 5. Pre-flight по кэшу Фазы 1 (B-2) | ✅ **2026-08-06** (`§−71`): `preflight_coverage`, без сети |
| 6. Экран подтверждения (покрытие из C-8, прокси отдельной строкой, 3 кнопки) | ✅ **2026-08-06** (`§−72`, Фаза 05): `build_confirmation` + `_format_manual_confirmation`. Покрытие из C-8 показать НЕЛЬЗЯ по построению — C-8 считает последствия после загрузки цен; на экране pre-flight, и он молчит на холодном кэше |
| 7. `action_plan` в валюте покупки + строка CoVe | ✅ **2026-08-06** (`§−71`): `AssetActionRow.*_local` + строка «Состав портфеля / Ручной ввод» |

🔴 **Что оказалось неверным в плане этой фазы.** §5c.6 требовала конвертировать
цену В ПАРСЕРЕ, потому что «`analyze_all` заморожен». Верно на 2026-07-29 —
неверно с `§−45`: Base Currency Approach научил движок переводить каждую строку
с колонкой `Currency`. Буквальное исполнение дало бы ДВОЙНОЙ курс. План фазы —
гипотеза, датированная днём написания, а не приказ.

### Почему остановились именно здесь — три причины, все содержательные

**1. Шаг 1 оказался втрое больше, чем выглядел.** H-1 формулировался как «показать
пользователю, что бумагу подменили». При реализации выяснилось, что подменённая
бумага ещё и **получала цену прокси**: нота при брокерских 103.5 оценивалась в 65
(цена `VWOB`) — минус треть стоимости портфеля и сфабрикованный убыток −35%. Там же
всплыло, что портфель, ДЕРЖАЩИЙ факторный ETF (`SPY`, `IWM`, `EEM`…), **не строил
отчёт вообще** (`ValueError: Length mismatch`). Оба дефекта — приоритетнее ручного
ввода, потому что бьют по живым пользователям Freedom прямо сейчас. Шаг занял
весь слот реализации и дал +36 тестов.

**2. Сквозной аудит (`AUDIT_360_2026-07-30.md`) нашёл блокеры ВЫШЕ Фазы 4 по
приоритету.** Строить парсер поверх них означало бы закладывать дефекты в фундамент:
- **A-1**: движок падает на дубликате позиции — а `PHASE_04 §3.2` требует склейки
  дубликатов и МОЛЧА предполагает, что она уже есть. Парсер, написанный на этом
  предположении, унаследовал бы падение.
- **A-2**: `NaN` в количестве тихо обнуляет Vol/Sharpe/Return. Ручной ввод — самый
  вероятный источник кривых чисел, то есть именно он этот дефект и активирует.
- **Фаза 3 не подключена**: 526 строк чекеров качества данных и 56 тестов, ноль
  вызовов в проде. Парсер должен опираться на них (покрытие C-8 на экране
  подтверждения — прямое требование §5b E), а опереться пока не на что.

**3. Жёсткий внешний блокер: без Фазы 9 ручной ввод физически неработоспособен.**
Инвариант **I-12** (решение владельца 2026-07-27) запрещает отдавать рыночные данные
Tradernet не-клиентам Freedom. Значит `manual` обязан ходить в Stooq — а
`StooqProvider` не написан. Сейчас `provider_for_source("manual")` честно бросает
`ProviderUnavailable`. **Даже полностью готовый парсер не смог бы построить ни одного
отчёта.** Фаза 4 без Фазы 9 — код, который нельзя включить.

Отдельно: **A-3 ✅ ЗАКРЫТ 2026-08-02.** Аудит показал, что тот же гард работал
**fail-open** — `provider_for_source('stooq')` молча возвращал `TradernetProvider`,
и юридическая граница нарушилась бы незаметно ровно в момент появления Фазы 9.
Теперь гард **fail-closed**: allowlist `_KNOWN_SOURCES = {freedom, demo, manual}`,
неизвестный источник → `ProviderUnavailable` с текстом I-12 ДО сетевых вызовов и
до списания токена. Фаза 9 обязана внести `stooq` в allowlist ЯВНО — это и есть
точка принятия юридического решения.

### Честная готовность ручного ввода

| Измерение | Оценка |
|---|---|
| Проектная проработка (11 документов, ~280 КБ, инварианты, ловушки, разбор ТЗ) | **9/10** |
| Инфраструктура под ним (кэш по провайдеру, протокол провайдера, чекеры) | **8/10** — Фаза 2 fail-closed (A-3 ✅), Фаза 3 подключена в конвейере 2026-08-02 |
| Собственно ручной ввод (парсер, FSM, экран, валюта) | **8/10** — парсер, валюта, pre-flight, локальные уровни, FSM, экран подтверждения и черновик сделаны; не хватает загрузки файлом (Фаза 07) и проводки источника в расчёт (Фаза 06) |
| Возможность включить в проде | **0/10** — нет `StooqProvider` (I-12), плюс 🔴 предусловие MP-09: бумага без ценового ряда сегодня исчезает из портфеля МОЛЧА (`§−73`) |

**Функциональная готовность фичи «пользователь вводит портфель руками»: ~45%.**
Пользовательский путь собран целиком до кнопки «Рассчитать» и на ней честно
останавливается. Оставшееся упирается не в объём работы по ручному вводу, а в
непройденную Фазу 9: без независимого источника цен включать нечего.
>
> **Обновление 2026-07-27:** владелец продукта закрыл главный открытый вопрос —
> использовать клиентский/сервисный API-ключ Freedom Broker (Tradernet) для выдачи
> рыночных данных пользователям-не-клиентам **запрещено**. Следствие: Stooq обязателен
> для запуска `manual`, порядок фаз пересмотрен. Полный разбор следствий — §4.

---

## 0. Вердикт: ломает ли задание логику проекта

**Замысел — нет, не ломает.** Ручной ввод портфеля ложится ровно на существующий шов:
движок и так принимает `DataFrame[Ticker, Quantity, Purchase_Price]`, а `analyze_all`
уже умеет строить отчёт из любого источника этого контракта. Второй провайдер цен —
не новая идея, а уже запланированный пункт `docs/roadmap/ROADMAP_DATA_RESILIENCE.md §1`
(там же описаны `PriceProvider`, `TradernetProvider`, `ChainProvider`). Правка ключа
кэша (§5.2) — настоящий, воспроизводимый дефект. Чекеры данных (§4) закрывают реальный
пробел. Направление задания правильное.

**Текст задания — да, в шести местах.** Инварианты I-2/I-4, гейт E-2 и уровни строгости
блока C противоречат либо друг другу, либо фактическому коду. Если исполнять их буквально,
ветку невозможно ни собрать (I-2 vs Спринт 1), ни принять (C-1/C-2 против I-5). Всё это
чинится правкой формулировок — см. **Фазу 0**, она это и делает, до первой строки кода.

**Единственный по-настоящему тяжёлый пункт — не тот, о котором задание предупреждает.**
Это не Stooq и не чекеры, а `CASH:KZT` в шаблоне ввода (§3.2): он вскрывает существующий
дефект оценки кэша, который уже сейчас искажает отчёты freedom-пользователей с тенговым
остатком. Разбор — §3 ниже и `PHASE_04`. После решения 2026-07-27 (§4) добавился второй
тяжёлый пункт: Stooq стал обязательным условием запуска, а его покрытие KZ/IL-тикеров
и крипто неизвестно — это риск №1 запуска, проверяемый Фазой 8 поимённо.

---

## 1. Что проверено в коде (претензии задания → факт)

Всё ниже проверено чтением файлов, а не по памяти. Ссылки — на реальные строки.

### 1.1 Подтвердилось

| Претензия задания | Факт |
|---|---|
| `history.py:553` — `_cache_path(ticker, days)` без провайдера в ключе | ✅ ровно строка 553, `f"{safe}_{days}d.pkl"` |
| Межпровайдерное отравление кэша при втором провайдере | ✅ реально: `_read_cache`/`_write_cache` (559/573) ключуются только тикером+днями, TTL 3600 с (`_CACHE_TTL_SECONDS`) |
| `NON_RISK_ASSETS = ['USD','EUR','CASH','RUB','KZT']` | ✅ `investment_logic.py:327` |
| `resolve_tickers()` пропускает кэш до провайдера | ✅ `investment_logic.py:678` |
| Цена кэша выставляется `1.0` приоритетом (1) | ✅ `investment_logic.py:1681-1683` |
| Форма денежной строки `FreedomConnector` | ✅ `broker_api.py:343-350`, поля совпадают дословно |
| «13 обязательных инструментов» | ✅ 10 факторных (`factor_tickers`, 462-473) + 3 бенчмарка (`BENCHMARK_EXTRA`, 441) = 13; список тикеров в задании верен |
| `ThreadPoolExecutor(max_workers=6)` у Tradernet | ✅ `history.py:136` |
| `MemoryStorage` в `Dispatcher` (черновик теряется при рестарте) | ✅ `tg_bot.py:3035` |
| `_detect_and_adjust_splits`: порог `log(1.5)`, снап `2..50` и обратные | ✅ `history.py:485,525` |
| `BTC-USD` в демо-портфеле (ловушка Т-3) | ✅ `broker_api.py:365` |
| `KNOWN_SPLITS` и эвристика идемпотентны (срабатывают по факту разрыва) | ✅ `_apply_known_splits` проверяет наличие ступеньки (471-473), эвристика — порог по log-доходности |
| `pd.read_csv(url)` нигде не используется | ✅ в `src/` таких вызовов нет — запрет S-4 профилактический, регресса нет |

### 1.2 Не подтвердилось — фактические ошибки задания

| # | Утверждение задания | Факт | Последствие |
|---|---|---|---|
| **E-1** | I-4: `_price_for("DEEP","manual") == TIER_COST["DEEP"]`; §Приложение относит `_price_for` к `tg_bot.py` | Функции `_price_for` **не существует** (`grep` по `src/` и `tests/` — 0 совпадений). Цену считает `_effective_cost(tier, source)` (`tg_bot.py:316`) | Инвариант I-4 нельзя проверить как написано |
| **E-2** | Ключи тарифа — `"DEEP"` | `TIER_COST = {"base": 1, "scenario": 1, "deep": 2}` (`tg_bot.py:215`) — **нижний регистр** | `TIER_COST["DEEP"]` → `KeyError` |
| **E-3** | §3.5: `fx_feed.py` поддерживает **только** USD↔KZT | Поддерживает ещё GBP↔USD (`DEXUSUK`, `fx_feed.py:61-62`, добавлено F-7 2026-07-10) | Вывод про EUR/RUB («конвертировать нечем») остаётся верным; неверна только формулировка |
| **E-4** | §2.5: «Сплит 20:1, **2022-06-06**, `googl.us`» | 20:1 у GOOGL — **2022-07-18** (`KNOWN_SPLITS`, `history.py:51`). `2022-06-06` — это дата сплита **AMZN** (строка 50) | **Опасно для §2.5**: процедура пойдёт искать ступеньку не в той дате, не найдёт её и сделает вывод «ряд скорректирован» — при любом фактическом состоянии данных |
| **E-5** | §8.1: «при ~100 MAU выручка ≈ $1 660/мес, валовая прибыль ≈ $1 570» — со ссылкой на `ECONOMICS.md` | В `ECONOMICS.md §4` эта строка называется **«Бета (сейчас)»** и задана как **300 отчётов/мес**, а не как 100 MAU | Пересчёт «300 отчётов ≈ 100 MAU» = 3 отчёта на пользователя в месяц — допущение автора задания, а не цифра из документа. Вывод «$399 ≈ 25% валовой прибыли» держится только на нём |
| **E-6** | §2.5: конвенцию Stooq определить эмпирически | Процедуру **нельзя выполнить в этом окружении**: сетевая политика отдаёт 403 на CONNECT к `stooq.com` (и к `tradernet.com`). Плюс сам ключ, по §2.2, добывается человеком через капчу | Фазы 8–9 исполняются оператором вне CI, а не агентом. См. §4 ниже |

### 1.3 Найдено сверх задания (в коде, задание не заметило)

| # | Находка | Почему важно |
|---|---|---|
| **F-1** | **Историю цен уже сейчас грузит СЕРВИСНЫЙ ключ, а не ключ пользователя.** `MAC3RiskEngine._get_tradernet_client()` (`investment_logic.py:653-659`) читает `FREEDOM_API_KEY`/`FREEDOM_API_SECRET` из env. Ключи пользователя используются **только** в `FreedomConnector.fetch_portfolio` | Технически **`manual` не нуждается в Stooq, чтобы заработать** — ручной портфель может ехать на том же Tradernet-пути, что и freedom. **Но юридически путь закрыт 2026-07-27** (данные Tradernet не-клиентам запрещены): вывод сохраняет силу только для офлайн-разработки и тестов; живой `manual` — только Stooq. Итоговый порядок — §4 |
| **F-2** | **Кэш в не-базовой валюте не конвертируется вообще.** FX применяется к матрице ЦЕН (`_apply_fx_conversion`, 774), а строки кэша до неё не доходят: их цена жёстко `1.0` (1682) | `CASH:KZT 2 500 000` из шаблона §3.2 войдёт в портфель как **2 500 000 у.е.** и съест ~99% весов USD-книги. Это **существующий дефект freedom-пути** (тенговый остаток с брокерского счёта ведёт себя так же), который ручной ввод делает воспроизводимым в один шаг. Т-5 задания описывает симптом, но не находит его источник |
| **F-3** | `HistoryResult` — `NamedTuple(data, loaded, failed, retried)`, но `investment_logic.py:1746` читает `getattr(history_result, 'ohlc_data', None)` — поля нет, **ATR всегда идёт по close-only fallback** | `ProviderResult` из §5.1 обязан остаться duck-совместимым: те же 4 имени в том же порядке, `convention` — только в хвост. Иначе тихо ломаются 6 модулей-потребителей (`pdf_payload`, `scenario_report`, `portfolio_series`, `data_lineage`, `tg_bot`) |
| **F-4** | `_resolve_portfolio_source` (`tg_bot.py:283`) проверяет vault **раньше**, чем `stored == "template"`: «ключи в vault ВСЕГДА побеждают» | Ветка `manual` обязана стоять **до** проверки vault — иначе пользователь с привязанным брокером не сможет построить отчёт по ручному портфелю: режим будет само-лечиться в `freedom` |
| **F-5** **[уточнено 2026-07-27]** | Движок реализует **градуированную политику для молодых листингов**, а не снисходительность: актив с историей < 60 дней исключается из модели, но его вес разбавляет риск (`MIN_OVERLAP_TDAYS=60`); ниже `max(2·K,30)` модель не строится (`931`); ниже 252 = `engine.trading_days` гаснет форвардная панель (`1907-1912`, F-14); на коротких окнах σ/ρ подаются с ДИ (`inference.py`). Пороги 30/60 приняты **сознательно ради `SPCX` (≈20 дней) и `SKHY`** | Чекеры блока C надо **зеркалить с движком, а не назначать свои числа**. Прежняя редакция «`STRICT`: BLOCK при окне <252» отменена — она строже самого движка. Профили расходятся не по длине окна, а по его ПРИЧИНЕ: молодой листинг (норма) vs усечение выгрузки (отказ источника). Детали — `PHASE_00 §3.1-3.3` |
| **F-7** **[нов. 2026-07-27]** | **Демо уже сейчас не декоративно.** `BTC-USD` — тикер из самого демо-портфеля — резолвится в несуществующий `BTC-USD.US` (ветка 5 `resolve_tickers` ловит только голые `BTC`/`ETH`), цена не находится, и `dropna(subset=['Current_Price'])` **молча удаляет строку**. Воспроизведено численно: демо из 3 позиций становится 2 | Витрина показывает не то, что обещает. Чинится отдельным PR **до** включения чекеров, иначе профиль `DEMO` (который не вправе блокировать) замаскирует дефект навсегда. Разбор — `PHASE_00 §3.5` |
| **F-8** **[нов. 2026-07-27]** | Воронка загрузки цен **ровно одна** — `get_market_data`. `EXTERNAL_DIVERSIFIERS` берут колонки из уже загруженного `all_data` (отдельной загрузки нет), `tool_plugins.py` — мёртвый код без вызывающих | I-13 («один отчёт — один источник») достижим одной точкой контроля. Единственный оставшийся путь смешения — межпровайдерный кэш, он закрывается Фазой 1 |
| **F-6** | Бейджа источника в слое отчёта **не существует** (`grep` по `pdf_payload/premium_payload/html_renderer` — только комментарий). Строка CoVe «Цены и история активов» жёстко подписана `Tradernet (Freedom Broker)` (`data_lineage.py:104,117`) | «Бейдж» из Спринта 7 — это новая фича слоя отчёта, то есть правка `pdf_payload.py`, **который I-2 замораживает**. Дешёвая и достаточная замена — динамический источник в строке CoVe: `data_lineage.py` в списке I-2 не значится |

---

## 2. Внутренние противоречия задания (и как их снять)

Все пять — блокирующие: без правки формулировки соответствующая фаза не может выйти зелёной.

| # | Противоречие | Разрешение (фиксируется в `PHASE_00`) |
|---|---|---|
| **К-1** | I-2 требует `git diff --stat` = 0 по `investment_logic.py`. Спринт 1 требует «`get_market_data()` ходит через провайдера» — а `get_market_data` живёт в `investment_logic.py:722` | Убрать `investment_logic.py` из списка нулевого диффа. Для него — **поведенческий** инвариант I-5 (снапшот чисел побитово), а не текстовый. Остальные 8 модулей списка остаются под `git diff --stat = 0` |
| **К-2** | Спринт 7 требует «бейдж» источника → это слой отчёта → `pdf_payload.py`, замороженный I-2 | Источник показывать **строкой CoVe** (`data_lineage.py`, вне списка I-2). Бейдж в шапке отчёта — отдельным PR после Фазы 6, с явным снятием заморозки |
| **К-3** | E-2 требует «одинаковые метрики риска» для одного портфеля через `freedom` и `manual`. §2.5 и I-7 при этом допускают, что конвенции источников различаются, и запрещают их смешивать | Пока `manual` едет на Tradernet (Фазы 4–7) — E-2 выполним **строго** (один и тот же провайдер, побитово). После переключения на Stooq — E-2 становится тестом с допусками §8.6, а не на равенство. Формулировку развести по фазам |
| **К-4** | C-1/C-2/C-5/C-6 объявлены BLOCK и вызываются «между провайдером и `analyze_all`», то есть для всех источников. Это строже текущего поведения (F-5) и роняет отчёты, которые сегодня успешно строятся | Ввести **два профиля строгости**: `LEGACY` (freedom — пороги ровно те, что движок реализует сегодня: ≥1 фактор, окно ≥`max(2K,30)`) и `STRICT` (manual). I-5 и I-9 держатся по построению. Пороги — таблицей в `PHASE_03` |
| **К-5** | §3.5 и Т-5 запрещают «молча приравнивать курс к 1.0», но именно это делает `analyze_all:1682` — файл, замороженный I-2 | Конвертировать кэш **до** передачи DataFrame в движок, в слое `manual` (валюта → базовая, `Ticker` остаётся исходным, факт конвертации — в CoVe). I-2 не нарушается. Для freedom дефект F-2 чинится отдельным PR — он существует и без этой ветки |

---

## 3. Отдельно: `CASH:KZT` — самый дорогой пункт задания

Задание оценивает кэш как рутину («не изобретать заново», §3.1) и отводит валютной теме
полтора абзаца. По факту это самый тяжёлый пункт всей ветки, и вот почему.

Строка `CASH:KZT 2500000` из **примера в §3.2** в текущем движке даёт:

```
Current_Price['KZT'] = 1.0            # investment_logic.py:1682, безусловно
Current_Value['KZT'] = 2 500 000      # Quantity × 1.0
```

При книге акций на $10 000 тенговый остаток (≈ $4 800 по курсу из `ECONOMICS.md`)
получает вес **99.6%** вместо ≈32%. Ломается всё, что считается от весов: TRC, CVaR,
факторные беты портфеля, Black-Litterman, «до/после» ребаланса, композитный риск-индекс.
Отчёт при этом выглядит совершенно нормально — ни одного признака ошибки. Ровно тот класс
дефекта, про который §3.4 пишет «опечатка в количестве не видна в цифрах отчёта».

Что важно: **это не дефект ручного ввода.** Тот же путь проходит тенговый остаток
с брокерского счёта (`broker_api.py:336-351` — строка кэша на каждую валюту из `acc`),
то есть любой KZ-клиент Freedom с непустым тенговым балансом уже сейчас получает
искажённый отчёт. Ручной ввод только делает дефект воспроизводимым за один шаг.

**Решение** (детали — `PHASE_04 §5`): слой `manual` конвертирует количество кэша в базовую
валюту отчёта до сборки DataFrame, по тому же `fx_feed`, что и цены. Валюта без курса
(EUR, RUB) — позиция исключается с явным предупреждением на экране подтверждения; молчаливого
`1.0` не остаётся ни на одном пути. Freedom-путь чинится отдельным PR (вне этой ветки,
но с обязательной строкой в `docs/audit/AUDIT.md` — иначе дефект F-2 забудется).

---

## 4. Порядок фаз — обновлено 2026-07-27: Stooq обязателен

Задание фиксирует порядок спринтов 0→8 и отдельно требует: «Порядок не переставлять:
спринт 2 (конвенция) обязан предшествовать спринту 4 (провайдер)». Это требование
сохранено — Фаза 8 всегда предшествует Фазе 9.

**Решение владельца продукта (2026-07-27).** Использовать персональный (клиентский)
API-ключ Freedom Broker (Tradernet) для загрузки и показа рыночных данных пользователям,
не являющимся клиентами брокера, **запрещено**. Открытый вопрос предыдущей редакции
этого раздела закрыт ответом «нет» — и, как здесь и было оговорено, это отменяет
рекомендацию «Stooq в хвост»: Фазы 8–9 возвращаются на критический путь запуска.

Следствия, зафиксированные по фазам:

1. **`manual` в проде работает только на Stooq.** `PRICE_PROVIDER_MANUAL=stooq` —
   стартовая конфигурация, а не опция после сверки (`PHASE_09 §4`). Отдельного
   «клиентского» исключения нет: статус клиента брокера у manual-пользователя
   неверифицируем, поэтому путь один для всех manual-портфелей.
2. **Откат `manual` — только выключение фичи.** При сбое или деградации Stooq ручные
   отчёты честно не строятся (`MANUAL_PORTFOLIO_ENABLED=off`, токен не списан);
   тихая подмена на Tradernet юридически недоступна.
3. **Ключ Stooq — операция дня 1.** Добыча `STOOQ_API_KEY` через капчу (`PHASE_08 §0`)
   начинается немедленно, параллельно Фазам 0–1: это единственный шаг, который нельзя
   ускорить кодом, и теперь он лежит на критическом пути.
4. **Разработка и запуск разведены.** Находка F-1 (историю грузит сервисный ключ)
   остаётся технически верной, поэтому Фазы 4–7 разрабатываются и тестируются
   на офлайн-фикстурах параллельно с 8–9 (CI и так без сети). Но флаг
   `MANUAL_PORTFOLIO_ENABLED=on` в проде включается только после гейта Фазы 9.
5. **E-2 меняет форму окончательно** (уточнение К-3, `PHASE_00 §4`): побитовое
   равенство остаётся контрактным офлайн-тестом слоя сборки DataFrame (одни фикстуры,
   один провайдер — проверяется корректность фрейма, а не источник); живой `manual`
   (Stooq) против `freedom` (Tradernet) сверяется по порогам §8.6 задания.
6. **Покрытие Stooq — риск №1 запуска.** manual-пользователи вводят KZ/IL-тикеры
   (`KSPI.KZ`, `KAP.IL`, `HSBK.IL`) и крипто (`BTC-USD` есть даже в демо-портфеле);
   их наличие в Stooq неизвестно и проверяется Фазой 8 поимённо (`PHASE_08 §2.5`).
   Непокрытая позиция честно исключается на экране подтверждения, а C-8 (покрытие
   по стоимости) решает, строить ли отчёт вообще.
7. **Демо-режим — тот же класс проблемы, вынесен в ★.** Демо-отчёт сегодня строится
   на живых данных Tradernet под сервисным ключом и показывается в том числе
   не-клиентам. Вероятное решение — перевести демо на Stooq после Фазы 9
   (демо-тикеры включены в проверку покрытия Фазы 8); решение за владельцем,
   в объём этой ветки не входит — новый пункт ★ в §7.

---

## 5. Карта фаз

| Фаза | Файл | Соответствие заданию | Дней | Блокировки |
|---|---|---|---:|---|
| 0 | [`PHASE_00_CONTRACT.md`](PHASE_00_CONTRACT.md) | **новая** — снятие К-1…К-5, правка I-2/I-4/E-2 | 0.5 | — |
| 1 | [`PHASE_01_CACHE_ISOLATION.md`](PHASE_01_CACHE_ISOLATION.md) | Спринт 0 (§5.2) | 0.5 | — |
| 2 | [`PHASE_02_PROVIDER_PROTOCOL.md`](PHASE_02_PROVIDER_PROTOCOL.md) | Спринт 1 (§5.1) | 1.5 | Фаза 1 |
| 3 | [`PHASE_03_DATA_CHECKS.md`](PHASE_03_DATA_CHECKS.md) | Спринт 3 (§4) | 2.5 | Фаза 2 |
| 4 | [`PHASE_04_MANUAL_SOURCE.md`](PHASE_04_MANUAL_SOURCE.md) | Спринт 5 (§3.1–3.2) + кэш-FX | 3.5 | Фаза 3 |
| 5 | [`PHASE_05_FSM_FLOW.md`](PHASE_05_FSM_FLOW.md) | Спринт 6 (§3.4, §5.3) | 2.5 | Фаза 4 |
| 6 | [`PHASE_06_SOURCE_WIRING.md`](PHASE_06_SOURCE_WIRING.md) | Спринт 7 (I-4, CoVe) | 1.5 | Фазы 5 + 9 |
| 7 | [`PHASE_07_FILE_UPLOAD.md`](PHASE_07_FILE_UPLOAD.md) | Спринт 8 (§3.3) | 3 | Фаза 6 |
| 8 | [`PHASE_08_STOOQ_CONVENTION.md`](PHASE_08_STOOQ_CONVENTION.md) | Спринт 2 (§2.5) | 1 | 🔴 капча — ключ добывается в **день 1** |
| 9 | [`PHASE_09_STOOQ_PROVIDER.md`](PHASE_09_STOOQ_PROVIDER.md) | Спринт 4 (§2.3, S-1…S-8) | 2.5 | Фазы 2, 3, 8 |
| 10 | [`PHASE_10_EODHD.md`](PHASE_10_EODHD.md) | §8 — отложено до +100 пользователей | — | коммерческий договор |

**Порядок исполнения (обновлён 2026-07-27; 🔴 — критический путь запуска):**

| Шаг | Работа | Примечание |
|---|---|---|
| 1 (день 1) | 🔴 Оператор: `STOOQ_API_KEY` через капчу → Secret Manager | старт немедленно, параллельно шагу 2 |
| 2 | Фаза 0 → Фаза 1 → Фаза 2 | разработка |
| 3 | 🔴 Фаза 8 — конвенция + покрытие (13 ETF / KZ / IL / крипто / демо-тикеры) | как только есть ключ |
| 4 | Фаза 3 → 🔴 Фаза 9 | сверка Фазы 9 §4.1 — предзапускной контроль качества |
| 5 | Фазы 4 → 5 — параллельно шагам 3–4, на офлайн-фикстурах | разработка |
| 6 | Фаза 6 (нужны 5 и 9) → Фаза 7 | запуск: `PRICE_PROVIDER_MANUAL=stooq` |

Суммарная оценка прежняя (~18.5 дней работы: 18 из задания + 0.5 на Фазу 0 и кэш-FX),
но дата живого теста определяется не объёмом кода, а днём получения ключа
и прохождением сверки Фазы 9 §4: **без Stooq запуск `manual` невозможен ни в какой
конфигурации.** Счётчики тестов в гейтах фаз рассчитаны для исполнения в порядке
номеров; при параллельном исполнении действует инвариант «787 + Σ новых тестов
завершённых фаз».

---

## 6. Общие правила для всех фаз

Действуют поверх `CLAUDE.md` и `src/finance/CLAUDE.md`, повторены здесь, чтобы фазу
можно было брать в работу, не восстанавливая контекст.

1. **Один PR на фазу**, зелёный CI, обратная совместимость по умолчанию.
2. **Базовая линия тестов — 787 passed, 1 xfailed** (`python -m pytest tests/ -q`
   из корня, `PYTHONPATH=src`). Фаза не выходит, если базовая линия просела.
   Замер этого прохода — `PHASE_00 §5`.
3. **Новые модули не импортируют `tg_bot`** (гейт §10 задания). Направление зависимости:
   `tg_bot` → `finance/*`, никогда обратно.
4. **Флаги отката** — `MANUAL_PORTFOLIO_ENABLED`, `PRICE_PROVIDER_CHAIN`,
   `PRICE_PROVIDER_MANUAL`. Все по образцу `factor_orthogonalize_enabled()`
   (`investment_logic.py:119`): чтение через функцию, а не константу модуля, — иначе
   значение защёлкивается на импорте и тест не может его переопределить.
   Схема SQLite — только `CREATE TABLE IF NOT EXISTS`.
5. **Секреты** (`STOOQ_API_KEY`, `EODHD_API_KEY`) — только Secret Manager, в логах
   маска `apikey=***`, в тексте ошибки пользователю — `error_id`, никогда `str(exc)`
   (действующая практика: `tg_bot.py:2097`).
6. **Тесты и код правятся вместе**, крупное изменение → строка Было/Стало
   в `docs/audit/AUDIT.md`.
7. При правке `src/finance/*` сначала читать `docs/math/MATH_ENGINE.md`;
   при правке `design/premium_v2/*` — обязательный `bash design/premium_v2/build.sh`.

---

## 7. Что осталось открытым (закрывать запросом, не додумывать)

К списку §11 задания добавлено четыре пункта из разбора; помечены **★**.

| Пробел | Кто закрывает | Блокирует |
|---|---|---|
| ~~★ Право использовать данные сервисного ключа Freedom для не-клиентов~~ — **закрыт 2026-07-27: НЕТ, запрещено** → Stooq обязателен для `manual` (§4) | владелец продукта | — |
| ★ Демо-режим на данных Tradernet для не-клиентов — тот же класс проблемы (§4 п.7): переводить ли демо на Stooq | владелец продукта | вне объёма ветки; вход — покрытие демо-тикеров из Фазы 8 §2.5 |
| ★ Дефект F-2 (кэш в валюте ≠ базовой) на freedom-пути — чинить в этой ветке или отдельно | владелец продукта | объём Фазы 4 |
| ★ Показывать ли источник портфеля бейджем в отчёте (снятие заморозки `pdf_payload.py`) | владелец продукта | объём Фазы 6 |
| ★ Реальное соотношение «отчётов на пользователя в месяц» (E-5) | аналитика бота | точность триггера +100 польз. |
| Условия коммерческого использования Stooq | запрос в Stooq | Фаза 9 (прод) |
| Стабильность и срок жизни `apikey` Stooq | наблюдение в проде | Фаза 9 |
| Семантика параметра `c` у Stooq | эмпирически | Фаза 8 |
| Конвенция корректировок Stooq | эмпирически | Фаза 8 → 9 |
| Наличие в Stooq всех 13 обязательных ETF **+ KZ/IL-тикеров + крипто + демо-тикеров** | проверка Фазы 8 §2.5 (перенесена вперёд из смоука Фазы 9) | запуск `manual` |
| Цена тарифа Custom у EODHD | запрос в sales | Фаза 10 |
| Противоречие EODHD: биржевые контракты vs VWAP-агрегация | письменный вопрос до подписания | Фаза 10 |
| Покрытие KASE/AIX у EODHD | проверка по списку бирж | Фаза 10 |
| Фактическое время отчёта на 25 ручных позициях | замер в Фазе 5 | гейт §10 «не хуже +20%» |
