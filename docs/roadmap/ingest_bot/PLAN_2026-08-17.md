# RAMP Ingest Bot — обновление базы котировок из Telegram

<!-- nav | area:roadmap | code:src/finance/stooq_ingest.py,src/finance/stooq_store.py,src/services/report_storage.py,src/tg_bot.py,cloudbuild.yaml | read-before:прежде чем писать второго бота, трогать публикацию prices.sqlite или давать облаку право записи в базу котировок -->

> **Задание владельца (2026-08-17).** Отдельный Telegram-бот, в который владелец
> или админ загружает `.txt` с дневными дельтами; бот их чистит, обновляет
> `prices.sqlite` и публикует базу в Google Cloud. Бот — **другой**, только для
> загрузки данных.
>
> **Статус:** проект, кода нет. Ниже — логика, границы и пошаговое внедрение.
> **Всё численное — из кода и из уже сделанных замеров**, источник указан
> построчно. Оценки помечены словом «оценка».
>
> Смежные документы: [`DATA_SURFACE.md`](DATA_SURFACE.md) — что ещё можно
> обновлять этим ботом · [`../manual_portfolio/OPERATOR_STOOQ.md`](../manual_portfolio/OPERATOR_STOOQ.md)
> — действующий ручной регламент, который этот проект частично заменяет.

---

## 0. Одна страница

| | |
|---|---|
| **Что строим** | второй Cloud Run сервис из **того же образа**, свой Telegram-токен, свой сервис-аккаунт |
| **Что он делает** | принимает `YYYYMMDD_d.txt` → `parse_daily_file` → `apply_batch` → публикует базу в GCS |
| **Чего он НЕ делает** | бутстрап (архив 0.84 ГБ через Telegram не проходит), отчёты, токеномику |
| **Новых зависимостей** | **ноль**: `aiogram` и `google-cloud-storage` уже в проекте |
| **Новой логики разбора** | **ноль**: `finance/stooq_ingest.py` уже написан, покрыт 87 тестами и лежит в образе |
| **Главный технический риск** | одновременная запись базы (бот ↔ оператор) → решается CAS по `generation` GCS |
| **Главный риск безопасности** | писатель в бакете, где лежат балансы и ключи брокера → решается **отдельным бакетом** |
| **Что закрывает** | риск «оператор забывает применять файлы» (вероятность **высокая**, `PHASE_08B §9`) |

**Ключевая мысль всего проекта.** Ежедневный цикл сегодня — это `git clone`,
Python, venv, `gcloud`, три команды и компьютер под рукой. Бот сводит его к
«переслать файл в чат». Логика разбора при этом **не пишется заново**: она уже
есть, проверена и лежит в `src/` — ровно потому, что фаза MP-08.5 положила её
туда осознанно, а не в `scripts/`.

---

## 1. Разбор задания: что в нём уже решено кодом, а что придётся решить

| Фраза задания | Состояние |
|---|---|
| «загружает txt с дневными дельтами» | ✅ формат разобран, 9 правил фильтра измерены |
| «бот это всё чистит» | ✅ `parse_daily_file` — чистка уже написана, дописывать нечего |
| «создаёт prices.sqlite» | 🔴 **уточнение: не создаёт, а ОБНОВЛЯЕТ.** Создание = бутстрап из архива 0.84 ГБ; см. §3 |
| «обновляет базу Google Cloud онлайн» | 🟡 надо спроектировать: read-modify-write объекта GCS с защитой от гонки |
| «бот должен быть другим» | ✅ и это правильно по пяти независимым причинам — §2 |

🔴 **Единственная фактическая правка к заданию.** «Создаёт `prices.sqlite`»
неверно для дневного цикла: дневной срез содержит **только бары за один день**
по ~12 000 тикеров. Создать из него базу нельзя — истории в нём нет. База
рождается бутстрапом из архива истории и дальше только **дописывается**.

Практическое следствие: `apply_batch` вызывается с `allow_new=False`
(`stooq_ingest.py:480-533`). Состав базы определяет бутстрап; дельта лишь
дописывает бары уже отобранным бумагам. Обратное однажды уже случилось: первый
`apply` затащил в базу 11 196 бумаг с историей в один день.

---

## 2. Почему бот ОТДЕЛЬНЫЙ — пять причин, все из кода

Владелец потребовал этого прямо. Требование верное, и вот чем оно держится:

| # | Причина | Факт |
|---|---|---|
| 1 | **Главный бот открывает базу `mode=ro`** | запрет технический, не договорной (`stooq_store.connect`, URI `mode=ro`). Дать ему писателя — сломать инвариант «гонок нет по построению» |
| 2 | **Память** | у главного бота `--memory=2Gi`, и её едят pandas и sklearn. `/tmp` на Cloud Run — это RAM; база 58.6 МБ + журнал в том же адресном пространстве конкурируют с отчётом |
| 3 | **Радиус поражения** | краш ingest-а положит бот, у которого `min-instances=1, max-instances=1`, то есть **всех пользователей** |
| 4 | **Права** | боту нужен read котировок; загрузчику — write. Разные сервис-аккаунты возможны только у разных сервисов |
| 5 | **Аудитория** | у главного бота `WhitelistMiddleware` **fail-OPEN** при пустом списке (`tg_bot.py:3389-3397`) — сознательное решение для беты. Загрузчику нужен **fail-CLOSED**, и смешивать два разных дефолта в одном процессе нельзя |

### 2.1 Четыре варианта размещения и почему выбран второй

| Вариант | Приговор |
|---|---|
| A. Второй Dispatcher внутри главного бота | ❌ снимает причины 2–5 разом ради экономии одного сервиса |
| **B. Отдельный Cloud Run сервис, ТОТ ЖЕ образ, другой `--command`** | ✅ **принято** |
| C. Отдельный репозиторий и образ (как `freedom-etl/`) | ❌ пришлось бы копировать `stooq_ingest.py` — вторая копия, которая однажды разойдётся молча |
| D. Cloud Function на GCS-триггере (как RAG-ингест) | ❌ по адресату, а не по механике — §2.2 |

Вариант B работает потому, что `Dockerfile` заканчивается
`CMD ["python", "/app/src/entrypoint.py"]`, а Cloud Run умеет переопределять
команду при деплое. Образ уже содержит `src/`, `tests/` и все зависимости.

### 2.2 Честное сравнение с Cloud Function-триггером

Проект **уже умеет** принимать файлы через GCS-триггер: `cloud_function/main.py`
слушает бакет с банковскими PDF. Тот же приём подошёл бы и здесь — оператор
кладёт `.txt` в бакет, функция применяет.

| | Cloud Function | Telegram-бот |
|---|---|---|
| Лимит на размер файла | нет | **20 МБ** (Bot API `getFile`) |
| Ретраи | Eventarc | свои |
| Кому приходит сводка | **в логи** | **человеку, в чат, немедленно** |
| Нужен компьютер | да (консоль GCP) | нет |
| Опыт проекта | 🔴 путь оказался хрупким: регион триггера, загрузка модели, fail-soft шаг деплоя — и `entrypoint.py` получил boot-ingest как страховку | — |

**Вывод.** Бот выигрывает по адресату обратной связи, а не по механике. Это
надо признать прямо: сводка, которую видит только Cloud Logging, не заставит
оператора перекачать недокачанный файл. А правило 9 (`0 < принято < 5000` →
файл отвергнут целиком) существует ровно для того, чтобы человек **узнал** и
перекачал.

При этом **гибрид принят**: бот кладёт полученный `.txt` в бакет как
аудит-след, и только потом применяет. След и интерактив не конкурируют.

---

## 3. Границы: что бот делает и чего не делает НИКОГДА

| Делает | Не делает | Почему |
|---|---|---|
| дневная дельта `YYYYMMDD_d.txt` | **бутстрап** | архив 0.84 ГБ / 11 842 файла; Bot API качает ≤ 20 МБ |
| файл истории одной бумаги (добор) | ре-бутстрап | тот же архив |
| `prune`, `verify-universe`, `/status` | `measure-convention` | нужен архив |
| публикация базы в GCS | заливка собранной базы | 58.6 МБ > 20 МБ лимита |
| — | **любое чтение токеномики и vault** | §7: у сервиса нет и не будет таких прав |

🔴 **Бот никогда не создаёт базу с нуля.** Если скачанного объекта нет или в нём
ноль инструментов — это **отказ с громким сообщением**, а не «создам пустую и
залью». Пустая база, уехавшая в бакет поверх настоящей, стирает труд оператора
и при этом выглядит как успешная операция.

---

## 4. Архитектура

```
  ВЛАДЕЛЕЦ / АДМИН                     RAMP INGEST BOT                    ГЛАВНЫЙ БОТ
  ═══════════════                      (Cloud Run, тот же образ)          ═══════════
                                       ┌──────────────────────────┐
  20260818_d.txt  ──── Telegram ──────▶│ 1 admin-гейт (fail-closed)│
  ≈ 700 КБ                             │ 2 лимиты ДО скачивания    │
                                       │ 3 getFile → /tmp          │
                                       │ 4 GCS: download + gen     │──┐
                                       │ 5 parse → apply_batch     │  │
                                       │   (allow_new=False)       │  │
                                       │ 6 upload if_gen_match=gen │  │  CAS
                                       │ 7 сводка человеку         │  │
                                       └──────────────────────────┘  │
                                                                     ▼
                                          gs://ramp-bot-quotes/stooq/prices.sqlite
                                                                     │
                                                          том gcsfuse │ (только чтение)
                                                                     ▼
                                                           StooqStore mode=ro
                                                           → локальная копия /tmp
                                                           → StooqProvider → отчёт
```

Сообщение проходит семь шагов, и каждый из них может отказать **до** того, как
база тронута. Это не украшение: единственный необратимый шаг — шестой.

---

## 5. Главное техническое решение: read-modify-write с CAS

### 5.1 Почему не писать прямо на gcsfuse

Соблазн: у главного бота уже примонтирован том, можно открыть `prices.sqlite`
на запись и не возиться с загрузками.

**Что ломается.** SQLite поверх объектного хранилища не имеет работающих
файловых блокировок. Проект это уже знает и платит за это: `db_tokenomics`
вынужден держать `journal_mode=DELETE` и `synchronous=FULL`, потому что WAL на
gcsfuse **портит базу outright** (`-shm` требует произвольной перезаписи, а
gcsfuse отвечает `OutOfOrderError`). Для базы в 1 МБ, которая пишется редко,
это приемлемо. Для 58.6 МБ и миллиона UPSERT-ов в день — нет.

**Решение.** Файл целиком, объектом:

```
скачать prices.sqlite → /tmp    (запомнить blob.generation)
применить дельту локально        (0.2 с — замер)
загрузить обратно с if_generation_match=<generation>
```

### 5.2 Что даёт `if_generation_match`

Это compare-and-swap на уровне GCS. Если между шагами 1 и 3 объект изменил
кто-то другой — загрузка отвергается с 412, база в облаке остаётся его версией,
и бот **повторяет цикл с начала**.

Повтор безопасен, потому что применение идемпотентно **по построению**: запись
идёт по ключу `(instrument_id, trade_date)` через `ON CONFLICT … DO UPDATE`,
и повторное применение того же файла оставляет базу побитово прежней
(проверено тестом фазы MP-08.5).

🔴 Это ровно тот запрет, который сегодня держится на дисциплине: «не собирать
базу на двух машинах одновременно — вторая загрузка молча затрёт первую»
(`OPERATOR_STOOQ §11.7`). CAS превращает договорённость в механизм.

### 5.3 Чего CAS НЕ решает

Оператор льёт базу через `gcloud storage cp` **без** precondition. Значит:

* бот не затрёт операторскую заливку (у него CAS);
* оператор затрёт ботовскую (у него нет).

Отсюда протокол §6 и команды `/freeze` / `/unfreeze`. Механизм, который
защищает только одну сторону, надо назвать односторонним, а не «атомарным».

### 5.4 Курсор вне базы: защита от тихого отката

Если оператор зальёт базу двухнедельной давности, внутри неё всё
непротиворечиво: календарь рынка выводится из тех же строк, что и бары, поэтому
**каждая бумага честно отрапортует свежесть 0**. Это уже известный класс
(`stooq_store.market_staleness_days` заведён именно из-за него).

Курсор в самой базе откатится вместе с базой. Значит он должен лежать **снаружи**:

```
gs://ramp-bot-quotes/stooq/last_seen.json   (< 1 КБ)
{"max_trade_date": 20260818, "generation": "...", "applied_at": "..."}
```

Бот сверяет при каждой операции. `max_trade_date` в облаке меньше запомненного →
**громкое предупреждение оператору**, а не молчаливое продолжение.

---

## 6. Протокол сосуществования с оператором

Ежедневный цикл переезжает в бота. Ре-бутстрап остаётся у оператора — и вот
здесь появляется дефект, которого сегодня нет, потому что писатель один.

**Дефект.** Оператор раз в квартал пересобирает базу из архива и заливает её.
Архив не содержит дней, которые бот применил после его скачивания. Заливка
**сотрёт их** — тихо, и база будет выглядеть здоровой.

**Протокол, который это снимает** (четыре шага, порядок обязателен):

| Шаг | Действие | Кто |
|---|---|---|
| 1 | `/freeze` — бот перестаёт применять и говорит, до какой даты база наполнена | оператор в чате |
| 2 | локальный ре-бутстрап из свежего архива + `verify-seam` | оператор |
| 3 | `gcloud storage cp` — заливка собранной базы | оператор |
| 4 | `/unfreeze` + **переслать боту дневные файлы за период разрыва** (они лежат в `applied/`) | оператор |

Шаг 4 работает именно благодаря идемпотентности: файлы можно слать в любом
порядке и повторно.

🔴 `/freeze` — не блокировка ради блокировки. Без него шаг 2 занимает минуты, в
которые бот примет файл, применит его к СТАРОЙ базе и загрузит; шаг 3 это
затрёт, и оператор увидит успех дважды при одной потерянной дельте.

---

## 7. Модель угроз и права

### 7.1 🔴 Отдельный бакет — самое важное решение этого раздела (находка **IB-F2**)

Сегодня `STOOQ_DB_PATH=/mnt/state/stooq/prices.sqlite`, то есть база котировок
лежит в `gs://ramp-bot-state` — **рядом с `tokenomics.db` (балансы) и
`users_vault.db` (Fernet-зашифрованные ключи брокера пользователей)**.

Сейчас это безопасно: писателя в бакете нет, файл туда кладёт человек.
**Появление сервиса-писателя меняет картину.** Ошибка в путях, опечатка в
префиксе, скомпрометированный токен бота — и радиус поражения включает деньги
и ключи. Регламент уже кричит об этом заглавными буквами: «эти два файла не
трогать никогда — восстановления нет» (`OPERATOR_STOOQ §11.4`).

**Решение: `gs://ramp-bot-quotes`, отдельный бакет.** Сервис-аккаунт
загрузчика получает `roles/storage.objectAdmin` **только на него**. Тогда
«не трогать» перестаёт быть просьбой и становится отсутствием права.

Цена вопроса — ноль: база 58.6 МБ, бесплатный лимит Standard в `us-central1`
5 ГБ. Главный бот монтирует второй том `--add-volume` (это дополнение к набору
томов, а не замена — в отличие от `--set-env-vars`, см. §9.2).

### 7.2 Кто имеет право говорить с ботом

Три рубежа, все fail-closed:

| Рубеж | Механизм | Дефолт при пустой настройке |
|---|---|---|
| 1 | `INGEST_ADMIN_IDS` — свой список, не общий `ADMIN_USER_IDS` | **никого** |
| 2 | middleware отбрасывает апдейт до хендлера | **никого** |
| 3 | каждая изменяющая команда сверяет ещё раз | отказ |

🔴 **Копировать `WhitelistMiddleware` главного бота нельзя.** Он fail-OPEN:
«misconfiguration should fail OPEN for the beta». Для публичного продукта это
осознанный выбор; для сервиса, который пишет в базу котировок, — открытая
дверь. Свой список нужен и по второй причине: право начислять себе токены и
право менять цены — разные права, и человек, у которого есть первое, не обязан
иметь второе.

### 7.3 Недоверенный файл

Файл приходит из интернета, даже если прислал его владелец.

| Проверка | Когда | Основание |
|---|---|---|
| размер ≤ 2 МБ | **до скачивания**, по `message.document.file_size` | дневной срез 700 КБ; та же логика, что `PHASE_07 §3` |
| расширение `.txt` | до скачивания | — |
| имя вида `YYYYMMDD_d.txt` | до скачивания | из имени берётся дата для правила 1 |
| шапка `<TICKER>` и 8 колонок | при разборе | ловит скачанную HTML-страницу капчи |
| правило 9 (`0 < US < 5000`) | при разборе | ловит **недокачанный** файл — самый дорогой отказ |

Инъекция через содержимое невозможна: символ уходит в **параметризованный**
SQL, а не в строку (это уже свойство `stooq_ingest`, не новое требование).

### 7.4 Секреты

Токен второго бота — новый секрет `RAMP_INGEST_BOT_TOKEN` в Secret Manager.
В образ не попадает, в логи не печатается, в репозиторий не коммитится.

---

## 8. Фазы внедрения

Восемь единиц. Порядок обязателен: каждая следующая проверяется предыдущей.
Оценки — оценки, а не замеры.

### IB-0 · Контракт и решения (0.5 дня, кода нет)

Зафиксировать письменно ДО кода: отдельный бакет (§7.1), CAS (§5.2), курсор
вне базы (§5.4), протокол ре-бутстрапа (§6), fail-closed (§7.2).

**Гейт:** владелец подтвердил отдельный бакет. Это единственное решение, которое
после запуска меняется дорого (переезд пути в двух сервисах).

---

### IB-1 · `src/services/quote_publisher.py` (L1 Data) — CAS-обёртка над GCS

```python
class QuotePublisher:
    def download(self, dest: Path) -> Snapshot     # (path, generation, size)
    def upload(self, src: Path, *, if_generation_match: int) -> int
    def read_cursor(self) -> Cursor | None
    def write_cursor(self, cursor: Cursor) -> None
    def archive_source(self, src: Path, name: str) -> None   # аудит-след
```

* новых зависимостей нет: `google-cloud-storage` уже стоит и уже используется
  двумя модулями (`report_storage`, `entrypoint`);
* `412 Precondition Failed` — **штатный исход**, а не исключение наружу:
  вызывающий обязан повторить цикл;
* отсутствие бакета / объекта → своё исключение, как `StoreUnavailable`:
  «не настроено» и «сломано» — разные состояния с разной реакцией.

**Тесты** (на подменённом клиенте, без сети): скачивание отдаёт generation ·
загрузка передаёт precondition · 412 не превращается в traceback · курсор
переживает round-trip · пустой объект курсора = «не знаю», а не «дата 0».

---

### IB-2 · `src/ingest_service.py` (L2, без Telegram) — оркестратор

Здесь живёт вся логика цикла, и **ни одной строки aiogram**. Это позволит
тестировать её без бота — тот же приём, которым `analyze_all` отделён от `tg_bot`.

```python
def apply_daily(file: Path, *, actor: str) -> ApplyOutcome
def apply_history(file: Path, *, actor: str) -> ApplyOutcome   # добор бумаги
def status() -> StoreStatus
def prune(dry_run: bool) -> PruneResult
```

Цикл `apply_daily`:

```
1  download() → snapshot(generation)
2  🔴 guard: инструментов 0 → отказ «база пуста, бутстрап не делался»
3  🔴 guard: max_trade_date < cursor.max_trade_date → отказ «база откатилась»
4  parse_daily_file(file)         # 9 правил, счётчики
5  fatal? → вернуть причину, база НЕ тронута
6  apply_batch(conn, batch, allow_new=False)
7  upload(if_generation_match=snapshot.generation)
8  412 → повтор с шага 1, но не более N раз
9  write_cursor(), archive_source()
```

**Тесты:** повторное применение того же файла → `rows_written` не меняет базу ·
недокачанный файл → база нетронута и причина названа · 412 на первой попытке →
вторая проходит · пустая база → отказ до разбора · откат базы → отказ ·
`allow_new=False` доказан: тикер вне набора не заводится, попадает в счётчик
«вне рабочего набора».

**Гейт фазы:** ни один тест не ходит в сеть; `TradernetClient` не
конструируется ни разу (I-12 проверяется по факту конструирования).

---

### IB-3 · `src/ingest_bot.py` (L4 Delivery) — скелет бота

* свой `Bot(token=INGEST_BOT_TOKEN)`, свой `Dispatcher`;
* `AdminOnlyMiddleware` — **fail-closed**, отдельный класс, не импорт из `tg_bot`
  (`test_layering` запрещает импортировать `tg_bot` за пределами точки входа —
  и здесь это ограничение работает на нас);
* `asyncio.Lock` на весь цикл применения — сериализация двух файлов подряд;
* команды: `/start`, `/help`, `/status`.

🔴 **`ingest_bot` не импортирует `tg_bot` и наоборот.** Общее у них — только
`env_config` и `finance/*`, то есть L0/L1. Иначе два бота срастутся, и правка
онбординга сломает загрузку котировок.

**Тесты:** не-админ получает отказ и не доходит до хендлера · пустой
`INGEST_ADMIN_IDS` → отказ ВСЕМ · два одновременных апдейта сериализуются ·
`ingest_bot` не импортирует `tg_bot` (расширить `test_layering`).

---

### IB-4 · Приём документа — ядро (1 день)

Хендлер `F.document`:

1. лимиты по метаданным — **до** `getFile`;
2. различение по имени: `YYYYMMDD_d.txt` → дневной срез; иначе `<symbol>.txt`
   → файл истории (`file_date_of` возвращает `None` — это признак, а не ошибка);
3. скачивание в `/tmp`, применение через `ingest_service`, удаление файла;
4. сводка человеку — **та же форма, что печатает CLI** (`OPERATOR_STOOQ §8.1`),
   чтобы регламент и бот говорили на одном языке:

```
✅ применён 20260818_d.txt
   строк прочитано ....... 12 002
   баров записано ........ 11 942
   отброшено:
     индекс/FX (пропуск)....... 56
     вне рабочего набора....... 1 214
   C-1 факторы ........... 10/10 ✅
   база опубликована · поколение 1755499200
```

🔴 **Строка `C-1` обязана быть либо числом, либо явным «НЕ ПРОВЕРЕН».** Молча
её пропустить нельзя ни в одном режиме: это допуск оператора, и его отсутствие
читалось бы как «проверено, всё хорошо». В образе pandas есть, поэтому в боте
она всегда число — в отличие от CLI на голом Python.

**Тесты:** файл 3 МБ отвергнут **без** скачивания · `.zip` отвергнут ·
переименованный файл отвергнут с объяснением про дату в имени · сводка содержит
все счётчики батча · файл удаляется из `/tmp` в `finally`.

---

### IB-5 · Добор бумаги файлом истории (0.5 дня)

Закрывает то, что сегодня требует локальной машины: бумага пользователя не
попала в рабочий набор → `C-8` режет отчёт → лечится `backfill` из архива.

Оператор присылает `aapl.us.txt` из архива (сотни КБ — проходит).
`parse_history_file(window_days=HISTORY_LOOKBACK_DAYS)` + `apply_batch(allow_new=True)`.

🔴 `allow_new=True` здесь **обязателен и безопасен**: это осознанное расширение
состава базы одной бумагой, а не побочный эффект дельты. Разница между двумя
режимами — ровно тот дефект, который однажды затащил 11 196 бумаг.

**Тесты:** история заводит инструмент · дельта — нет · окно обрезается по
`HISTORY_LOOKBACK_DAYS` · символ выводится из имени файла (`vgit.us.txt` → `VGIT.US`).

---

### IB-6 · Диагностика и операционные команды (1 день)

| Команда | Что делает | Опирается на |
|---|---|---|
| `/status` | поколение, число инструментов, последний торговый день по каждому рынку, возраст базы в календарных днях, **сколько дней осталось до блокировки ручного тира** | `StooqStore`, `MAX_MARKET_STALE_DAYS` |
| `/universe` | покрытие 13 бумаг C-1 + глубина истории | `coverage_report`, `data_checks` |
| `/prune` | чистка обрывочных бумаг, `--dry-run` первым | `prune_thin_instruments` |
| `/freeze`, `/unfreeze` | окно ре-бутстрапа (§6) | флаг в курсоре |

🔴 `/status` обязан считать **«осталось дней»**, а не только «возраст».
`MAX_MARKET_STALE_DAYS = 7` календарных, выходные съедают 2 из них бесплатно —
после пятничной заливки ручной отчёт блокируется со следующей **субботы**.
Владельцу нужен ответ на вопрос «когда сломается», а не «когда я обновлял».

---

### IB-7 · Напоминания (0.5 дня) — лечит риск с ВЫСОКОЙ вероятностью

`PHASE_08B §9` оценивает «оператор забывает применять файлы» как риск высокой
вероятности и предлагает митигацию «видно в отчёте». Это митигация постфактум:
узнает пользователь, а не оператор.

Cloud Scheduler → HTTP-эндпоинт сервиса → бот пишет владельцу:

* торговый вечер, файл за сегодня не применён → напоминание;
* до блокировки ручного тира ≤ 2 дней → предупреждение с датой;
* база откатилась или курсор разошёлся → тревога.

Эндпоинт закрыт OIDC-аутентификацией Scheduler-а, а не «секретным путём».

---

### IB-8 · Деплой и регламент (1 день)

Шаг в `cloudbuild.yaml` после `deploy`, деталь — §9. Плюс правка
`OPERATOR_STOOQ.md`: ежедневный цикл §8 переписывается на «переслать файл боту»,
локальный путь остаётся как запасной, §11.7 (не собирать на двух машинах)
заменяется протоколом §6.

---

## 9. Деплой

### 9.1 Второй сервис из того же образа

```
gcloud run deploy ramp-ingest-bot \
  --image=<тот же тег, что у ramp-bot> \
  --command=python --args=/app/src/ingest_entrypoint.py \
  --min-instances=1 --max-instances=1 --concurrency=1 \
  --memory=512Mi --cpu=1 --no-cpu-throttling --no-allow-unauthenticated \
  --set-secrets=RAMP_INGEST_BOT_TOKEN=RAMP_INGEST_BOT_TOKEN:latest \
  --set-env-vars=QUOTES_BUCKET=ramp-bot-quotes,INGEST_ADMIN_IDS=<id>
```

| Параметр | Обоснование |
|---|---|
| `--min-instances=1` | long-polling: бот обязан жить между апдейтами |
| `--max-instances=1` | второй инстанс — второй писатель; CAS его отобьёт, но плодить гонку незачем |
| `--memory=512Mi` | база 58.6 МБ в `/tmp` (RAM) + журнал + буфер загрузки. Оценка с запасом; замерить на первом прогоне |
| `--no-allow-unauthenticated` | публичный эндпоинт не нужен: polling исходящий |

**Альтернатива — webhook вместо polling**: даёт `min-instances=0` и экономит
всегда-живой инстанс, ценой публичного эндпоинта (закрывается заголовком
`X-Telegram-Bot-Api-Secret-Token`) и холодного старта на образе с pandas и
sklearn. Для операции «раз в сутки» это приемлемо. **Рекомендация: начать с
polling** — он повторяет то, что в проекте уже работает, и не добавляет новых
режимов отказа; переключиться, если стоимость простоя окажется заметной.
Экономия здесь — оценка, а не замер: посчитать по калькулятору до решения.

### 9.2 🔴 Две грабли конфигурации

1. **`--set-env-vars` заменяет ВЕСЬ набор переменных.** Эта грабля уже сработала
   в проекте: выставленный руками флаг Premium V2 стирался следующим деплоем, и
   прод месяцами отдавал старый шаблон (`cloudbuild.yaml:118-126`). Переменные
   главного бота (`STOOQ_DB_PATH`, `STOOQ_LOCAL_COPY_DIR` и остальные)
   дописываются **в существующую строку**, а не выставляются отдельной командой.
2. **Второй том — дополнение, не замена.** `--add-volume` добавляет; набор
   `--set-env-vars` — заменяет. Разное поведение у похожих флагов, и путать их
   дорого.

### 9.3 Переменные

| Переменная | Где | Значение |
|---|---|---|
| `RAMP_INGEST_BOT_TOKEN` | ingest | секрет |
| `INGEST_ADMIN_IDS` | ingest | список, пустой = никто |
| `QUOTES_BUCKET` | ingest | `ramp-bot-quotes` |
| `STOOQ_DB_PATH` | главный бот | `/mnt/quotes/stooq/prices.sqlite` (переезд из `/mnt/state`) |
| `STOOQ_LOCAL_COPY_DIR` | главный бот | без изменений |

---

## 10. Тесты и гейты

### 10.1 🔴 Находка **IB-F1**: деплой-гейт не видит ценовой путь

`cloudbuild.yaml` шаг `test` запускает
`unittest discover -s /app/tests -p "test_phase*.py"`.

Под этот шаблон **не попадают**: `test_stooq_price_store.py`,
`test_stooq_provider.py`, `test_stooq_coverage_probe.py`, `test_manual_*.py`
(шесть файлов), `test_layering.py`, `test_repo_hygiene.py`,
`test_contracts_*.py`, `test_engine_*.py`, `test_factor_decomposition.py`.

То есть **весь ценовой путь Stooq и весь ручной ввод проходят GitHub CI, но не
проходят гейт, который решает, ехать ли образу в прод.** Разница между
«зелёный CI» и «безопасный деплой» здесь настоящая, и она в пользу CI.

**Что с этим делать — два действия, оба дешёвые:**

1. новые тесты назвать `test_phase51_ingest_bot.py` — тогда они в гейте с
   первого дня;
2. отдельной задачей расширить шаблон гейта (`-p "test_*.py"`) и убедиться, что
   исключённые брокерские тесты (`test_freedom_*.py`, им нужны pytest и живые
   ключи) действительно не проходят — они исключены сознательно, но шаблон
   `test_phase*` отсекает заодно и всё остальное.

### 10.2 Гейт выхода проекта

**Код:**
- [ ] `PYTHONPATH=src python -m pytest tests/ -q` — прежние 1611 passed + новые, 2 xfailed
- [ ] прогон в зеркале деплой-образа (без `design/`, `docs/`, `scripts/`)
- [ ] golden-фикстура совпала побитово: конвейер отчёта не тронут
- [ ] `test_layering` зелёный: `ingest_bot` не импортирует `tg_bot`, `finance/*` не импортирует `ingest_*`
- [ ] мутационная проверка: каждая новая защита доказала способность падать

**Данные и права:**
- [ ] бакет `ramp-bot-quotes` создан, SA загрузчика имеет права ТОЛЬКО на него
- [ ] SA загрузчика **не имеет** прав на `ramp-bot-state` — проверено попыткой чтения
- [ ] `STOOQ_DB_PATH` главного бота переехал, отчёт строится с нового пути

**Смысл — глазами, на живой базе:**
- [ ] дельта, применённая ботом, доезжает до отчёта (сравнить `/status` и CoVe)
- [ ] недокачанный файл: бот отверг, база не изменилась, `generation` тот же
- [ ] повтор того же файла: сводка честно говорит «ничего не изменилось»
- [ ] `/freeze` → ре-бутстрап → `/unfreeze` → дозалив: ни один день не потерян

---

## 11. Риски

| Риск | Вероятность | Что делаем |
|---|---|---|
| **Оператор льёт базу мимо бота и стирает дельты** | средняя | протокол §6 + курсор §5.4 + `/status` называет разрыв |
| Токен ingest-бота утёк | низкая | `INGEST_ADMIN_IDS` fail-closed: чужой токен даёт доступ к чату, но не к командам |
| Ошибка в путях затирает `tokenomics.db` | **низкая → нулевая** | отдельный бакет: права отсутствуют физически (§7.1) |
| 60-МБ база не влезла в `/tmp` (RAM) | низкая | `--memory=512Mi` с запасом; потолок копирования у главного бота 400 МБ уже стоит |
| Stooq сменил формат | низкая | шапка проверяется, правило 9 ловит усечение — обе защиты уже написаны |
| gcsfuse отдаёт главному боту метаданные с задержкой | низкая | локальная копия обновляется по `(size, mtime)`; задержка кэша метаданных — минуты, цикл суточный |
| Бот отвечает медленно на 60-МБ round-trip | средняя | сообщение «применяю…» сразу, сводка после; ограничить одним файлом за раз |

---

## 12. Чего делать НЕЛЬЗЯ

| Запрет | Что ломается |
|---|---|
| Дать ingest-боту права на `ramp-bot-state` | балансы и ключи брокера в радиусе поражения; восстановления нет |
| Открыть базу на запись через gcsfuse | блокировки не работают; для 58.6 МБ это порча базы, а не медленная запись |
| `allow_new=True` в дневной дельте | 11 196 бумаг с историей в один день — это уже случалось |
| Создать пустую базу и залить, если объекта нет | тихо стирает труд оператора и выглядит успехом |
| Скопировать `WhitelistMiddleware` из `tg_bot` | он fail-OPEN; пустая настройка откроет запись всем |
| Скопировать `stooq_ingest.py` в отдельный репозиторий | вторая копия разойдётся молча — тот же класс, что `cloud_function/rag_engine.py`, но без теста-стража |
| Импортировать `tg_bot` из `ingest_bot` | инверсия слоёв; `test_layering` это уже запрещает |
| Принимать файл без даты в имени как дневной срез | без даты неприменимо правило 1 — главное правило фильтра |

---

## 13. Порядок работ и оценка

| Фаза | Что даёт | Оценка |
|---|---|---|
| IB-0 | решения зафиксированы | 0.5 дня |
| IB-1 | публикация с CAS | 1 день |
| IB-2 | цикл применения без Telegram | 1 день |
| IB-3 | бот, admin-гейт | 0.5 дня |
| IB-4 | **ежедневный цикл работает из чата** | 1 день |
| IB-5 | добор бумаги без локальной машины | 0.5 дня |
| IB-6 | диагностика и `/freeze` | 1 день |
| IB-7 | напоминания | 0.5 дня |
| IB-8 | деплой + регламент | 1 день |
| | **Итого** | **≈ 7 дней** (оценка) |

**Минимально полезный срез — IB-0…IB-4 (4 дня).** После него ежедневный цикл
уже не требует компьютера; остальное — про то, чтобы отказ был виден вовремя.

🔴 **IB-6 и IB-7 не косметика.** Без `/status` оператор не узнает о разрыве, а
без напоминаний — о пропущенном дне; риск «забывает применять файлы» оценён как
**высокий**, и именно он блокирует ручной тир целиком через 7 календарных дней.
</content>
