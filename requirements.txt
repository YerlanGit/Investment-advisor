# ── Telegram Bot ────────────────────────────────────────────────────────────
aiogram==3.7.0
python-dotenv>=1.0.0

# ── Async Database ───────────────────────────────────────────────────────────
aiosqlite>=0.20.0

# ── Finance / MAC3 Risk Engine ───────────────────────────────────────────────
# Цены и история: Tradernet (Freedom Broker) API — см. freedom_portfolio.history.
# Фундаменталка: SEC EDGAR CompanyFacts JSON API (data.sec.gov, без edgartools).
pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.4.0
requests>=2.31.0

# ── Freedom Broker (Tradernet) connector ─────────────────────────────────────
pydantic>=2.0
# Optional extras for the standalone CLI (`python -m freedom_portfolio`).
# Uncomment if you want the live WebSocket feed or the rich-rendered table:
# python-socketio[client]>=5.10
# rich>=13.0

# ── Security / Encryption ────────────────────────────────────────────────────
cryptography>=42.0.0

# ── HTML Report Rendering ────────────────────────────────────────────────────
# Reports are delivered as interactive HTML pages via signed GCS URLs (see
# src/html_renderer.py + src/services/report_storage.py).  Pure Jinja2 — no
# headless-browser / PDF dependency.
jinja2>=3.1.3

# ── AI (advisor_bot / rag_engine) ────────────────────────────────────────────
anthropic>=0.25.0
chromadb>=0.5.0
# pymupdf4llm — REQUIRED by the bot: src/agent/rag_engine.py imports it
# (FinancialRAG is pulled in by tg_bot._fetch_rag_context).  A previous
# "cold-start slimming" pass wrongly removed it as cloud-function-only — a
# HALLUCINATION — which crashed the RAG module with
# `No module named 'pymupdf4llm'`.  Keep it here AND in cloud_function/.
pymupdf4llm>=0.0.10

# ── Cloud Storage (ChromaDB sync from GCS at container boot + report URLs) ───
google-cloud-storage>=2.14.0
