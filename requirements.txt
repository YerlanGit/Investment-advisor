# ── Telegram Bot ────────────────────────────────────────────────────────────
aiogram==3.7.0
python-dotenv>=1.0.0

# ── Async Database ───────────────────────────────────────────────────────────
aiosqlite>=0.20.0

# ── Finance / MAC3 Risk Engine ───────────────────────────────────────────────
# Цены и история: Tradernet (Freedom Broker) API — см. freedom_portfolio.history.
# yfinance удалён 2026-04 (нестабилен из Cloud Run, не покрывает AIX/KZ).
# Фундаменталка: SEC EDGAR через edgartools (10-K/10-Q филинги).
pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.4.0
requests>=2.31.0
edgartools>=3.0.0
# edgartools depends on pytz (timezone handling for SEC filing dates) but
# doesn't always pull it transitively in slim images.  Pin explicitly.
pytz>=2024.1
pyrate-limiter>=2.10.0,<3.0.0

# ── Freedom Broker (Tradernet) connector ─────────────────────────────────────
pydantic>=2.0
# Optional extras for the standalone CLI (`python -m freedom_portfolio`).
# Uncomment if you want the live WebSocket feed or the rich-rendered table:
# python-socketio[client]>=5.10
# rich>=13.0

# ── Security / Encryption ────────────────────────────────────────────────────
cryptography>=42.0.0

# ── PDF Generation ───────────────────────────────────────────────────────────
jinja2>=3.1.3
playwright>=1.44.0

# ── AI (advisor_bot / rag_engine) ────────────────────────────────────────────
anthropic>=0.25.0
chromadb>=0.5.0
pymupdf4llm>=0.0.10
